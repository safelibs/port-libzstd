name: libzstd-safe-validator-workflow
backend: codex
working_dir: /home/yans/safelibs/pipeline/ports/port-libzstd
max_bounces: 999
linear: true
yaml_source_mode: inline-only
verifier_encoding: explicit-phases
vars:
  GOAL: |
    Session Goal:
    validate the libzstd Rust port against the safelibs validator

    Recent Successful Session History Summaries:
    - None.

    Local Planning Request:
    libzstd, originally implemented as /home/yans/safelibs/pipeline/ports/port-libzstd/original, has been translated to a memory-safe Rust
            implementation in /home/yans/safelibs/pipeline/ports/port-libzstd/safe and tested through the prior pipeline phases.

            Run the safelibs validator suite from https://github.com/safelibs/validator against libzstd-safe:

            - Clone https://github.com/safelibs/validator into /home/yans/safelibs/pipeline/ports/port-libzstd/validator (or `git pull` to update if it already exists).
            - Follow the validator repository's README for setup and invocation. If the validator expects a
              path to the library under test, point it at /home/yans/safelibs/pipeline/ports/port-libzstd/safe.
            - Run the full validator suite and capture the results.

            Treat any validator failures as compatibility or safety regressions of libzstd-safe:

            - For each failure, add a minimal regression test to /home/yans/safelibs/pipeline/ports/port-libzstd/safe that reproduces the issue,
              fix the underlying problem in libzstd-safe, and confirm both the regression test and the
              validator pass.
            - Do not modify the validator suite to make failing checks pass; the fix belongs in
              libzstd-safe unless a validator bug is clearly identified, in which case document the
              finding in /home/yans/safelibs/pipeline/ports/port-libzstd/validator-report.md and skip just that check with justification.
            - Summarize the run (validator commit, checks executed, failures found, fixes applied) in
              /home/yans/safelibs/pipeline/ports/port-libzstd/validator-report.md.

            Each implementation phase should commit to git so that the succeeding checkers can reason
            about what was changed. Keep the workflow linear: checkers must only bounce back to the
            previous implementor. Each class of validator failure will likely warrant its own
            implementation phase followed by a checking phase, with a catch-all "fix everything
            remaining" implementation phase before final review. Review these fixes through agentic
            phases of software testers and senior testers, and ensure a clean validator run at the end.
phases:
- id: impl_validator_setup_initial_run
  type: implement
  bounce_target: null
  prompt: |
    Role: Software engineer implementing phase `impl_validator_setup_initial_run` (Validator Setup And Initial Evidence).

    Workflow goal: {{GOAL}}

    Work from `/home/yans/safelibs/pipeline/ports/port-libzstd`.
    This prompt is self-contained; the agent sees only this prompt.
    Use `.plan/workflow-structure.yaml` as the authoritative source for phase order, phase IDs, phase types, and this phase's fixed `bounce_target` value. Do not invent, rename, or reorder phases.
    Consume or update existing artifacts listed under `Preexisting Inputs` in place. Do not refetch, recollect, rediscover, or regenerate checked-in or already prepared artifacts from scratch when they already satisfy the required contract.
    Preserve unrelated user work.
    Commit all scoped work to git before yielding.

    Topology Contract
    - Phase order: `1`
    - Phase ID: `impl_validator_setup_initial_run`
    - Phase type: `implement`
    - Fixed top-level `bounce_target`: `null`

    Preexisting Inputs
    - `.plan/goal.md`
    - `safe/`
    - `original/libzstd-1.5.5+dfsg2/`
    - `safe/abi/original.exports.txt`
    - `safe/abi/original.soname.txt`
    - `safe/abi/export_map.toml`
    - `safe/tests/upstream_test_matrix.toml`
    - `safe/tests/dependents/dependent_matrix.toml`
    - `safe/tests/fixtures/versions/`
    - `safe/tests/fixtures/regression/`
    - `safe/tests/fixtures/fuzz-corpora/`
    - `dependents.json`
    - `all_cves.json`
    - `relevant_cves.json`
    - `test-original.sh`
    - `safe/docker/dependents/`
    - `safe/scripts/build-artifacts.sh`
    - `safe/scripts/build-original-cli-against-safe.sh`
    - `safe/scripts/build-deb.sh`
    - `safe/debian/control`
    - `safe/debian/rules`
    - `safe/debian/*.install`
    - `safe/scripts/run-full-suite.sh`
    - `safe/scripts/verify-export-parity.sh`
    - `safe/scripts/verify-install-layout.sh`
    - `.gitignore`

    New Outputs
    - `validator/` checkout at the latest fast-forwarded `main` commit, or the existing checkout updated in place
    - generated `safe/out/validator/venv/` with PyYAML if host Python lacks `yaml`
    - generated `safe/out/validator/override-debs/libzstd/*.deb`
    - generated `safe/out/validator/artifacts/proof/port-04-test-debs-lock.json`
    - generated `safe/out/validator/artifacts/port-04-test/results/libzstd/*.json`
    - generated `safe/out/validator/artifacts/port-04-test/logs/libzstd/*.log`
    - generated `safe/out/validator/artifacts/port-04-test/casts/libzstd/*.cast`
    - generated `safe/out/validator/artifacts/proof/port-04-test-validation-proof.json` only if the initial run passes
    - tracked `validator-report.md`
    - tracked `.gitignore` update for `validator/`
    - tracked `safe/scripts/run-validator-libzstd.sh` helper
    - tracked `safe/scripts/check-validator-phase-results.py` helper
    - tracked `safe/scripts/run-validator-regressions.sh` helper
    - a git commit containing `.gitignore`, `validator-report.md`, and the tracked helper scripts created in this phase

    Phase Source Content
    {% raw %}
    # Phase 1: Validator Checkout, Override Packaging, and Initial Run

    ## Phase Name

    Validator Setup And Initial Evidence

    ## Implement Phase ID

    `impl_validator_setup_initial_run`

    ## Preexisting Inputs

    - Goal text in `.plan/goal.md`
    - Existing safe implementation under `safe/`
    - Existing upstream source snapshot under `original/libzstd-1.5.5+dfsg2/`
    - Existing package scripts:
      - `safe/scripts/build-artifacts.sh`
      - `safe/scripts/build-original-cli-against-safe.sh`
      - `safe/scripts/build-deb.sh`
    - Existing package metadata:
      - `safe/debian/control`
      - `safe/debian/rules`
      - `safe/debian/*.install`
    - Existing release gates:
      - `safe/scripts/run-full-suite.sh`
      - `safe/scripts/verify-export-parity.sh`
      - `safe/scripts/verify-install-layout.sh`
    - Existing `.gitignore`
    - No required preexisting `validator/` checkout

    ## New Outputs

    - `validator/` checkout at the latest fast-forwarded `main` commit, or the existing checkout updated in place
    - generated `safe/out/validator/venv/` with PyYAML if host Python lacks `yaml`
    - generated `safe/out/validator/override-debs/libzstd/*.deb`
    - generated `safe/out/validator/artifacts/proof/port-04-test-debs-lock.json`
    - generated `safe/out/validator/artifacts/port-04-test/results/libzstd/*.json`
    - generated `safe/out/validator/artifacts/port-04-test/logs/libzstd/*.log`
    - generated `safe/out/validator/artifacts/port-04-test/casts/libzstd/*.cast`
    - generated `safe/out/validator/artifacts/proof/port-04-test-validation-proof.json` only if the initial run passes
    - tracked `validator-report.md`
    - tracked `.gitignore` update for `validator/`
    - tracked `safe/scripts/run-validator-libzstd.sh` helper
    - tracked `safe/scripts/check-validator-phase-results.py` helper
    - tracked `safe/scripts/run-validator-regressions.sh` helper
    - a git commit containing `.gitignore`, `validator-report.md`, and the tracked helper scripts created in this phase

    ## File Changes

    - Add `validator/` to `.gitignore`.
    - Create `validator-report.md` with sections:
      - `Phase 1 Base Commit: <40-hex commit captured before edits>`
      - validator URL and commit
      - local safe commit
      - Python setup path
      - package filenames and SHA256 hashes
      - generated port lock path, repository string, tag ref, commit, release tag, package architectures, sizes, and SHA256 hashes
      - exact commands run
      - matrix inventory counts: 5 source cases, 80 usage cases, 85 total cases
      - initial summary counts from `safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json`
      - failing testcase table with the exact schema `testcase_id`, `kind`, `client_application`, `exit_code`, `error`, `result_path`, `log_path`, `assigned_remediation_phase`, `remediation_status`, `regression_test`, `fix_commit`, and `notes`
      - skip list, initially empty
    - Create `safe/scripts/run-validator-libzstd.sh` as the canonical rerun command. The script must:
      - use `#!/usr/bin/env bash` and `set -euo pipefail`
      - locate `REPO_ROOT`, `SAFE_ROOT`, and `VALIDATOR_ROOT`
      - create `safe/out/validator/venv` only when needed for `import yaml`
      - export `PYTHON`
      - build safe artifacts and `.deb` packages
      - run the Debian package build as `env -u DEB_BUILD_PROFILES bash safe/scripts/build-deb.sh`, so a caller's `DEB_BUILD_PROFILES` cannot redirect the build to a non-default package root
      - source and validate `safe/out/deb/default/metadata.env` before staging packages, requiring `BUILD_TAG=default`, empty `PROFILES`, `PACKAGE_DIR=$SAFE_ROOT/out/deb/default/packages`, and `INSTALL_ROOT=$SAFE_ROOT/out/deb/default/stage-root`
      - stage exactly `libzstd1_*.deb`, `libzstd-dev_*.deb`, and `zstd_*.deb`
      - clear stale generated files under `safe/out/validator/artifacts/port-04-test/` and stale generated proof/lock files under `safe/out/validator/artifacts/proof/` before each run
      - generate `safe/out/validator/artifacts/proof/port-04-test-debs-lock.json` from the staged `.deb` files before running the matrix
      - run `make unit` and `make check-testcases` in `validator`
      - run the full libzstd validator matrix with `--mode port-04-test`, `--port-deb-lock`, and `--record-casts`; do not implement partial `--source-only` or `--usage-only` modes
      - capture the validator matrix exit status so failed-result JSON remains available for report classification
      - read `safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json` after every matrix run and treat any nonzero `failed` count as a helper failure even when `validator/test.sh` exits zero
      - run proof generation only when the matrix command exits zero and the summary reports zero failed cases
      - default to `validator/tests` and proof thresholds 5 source, 80 usage, and 85 total cases
      - if generated `safe/out/validator/skip.env` exists, source it only after confirming `validator-report.md` mentions every `VALIDATOR_SKIPPED_CASES` entry, then require `VALIDATOR_TESTS_ROOT/libzstd/` and `VALIDATOR_TESTS_ROOT/tests/libzstd/` to exist, run `"$PYTHON" validator/tools/testcases.py --config "$PWD/validator/repositories.yml" --tests-root "$VALIDATOR_TESTS_ROOT" --library libzstd --check --min-source-cases "$VALIDATOR_MIN_SOURCE_CASES" --min-usage-cases "$VALIDATOR_MIN_USAGE_CASES" --min-cases "$VALIDATOR_MIN_CASES"`, and use `VALIDATOR_TESTS_ROOT`, `VALIDATOR_MIN_SOURCE_CASES`, `VALIDATOR_MIN_USAGE_CASES`, and `VALIDATOR_MIN_CASES` for the filtered final matrix and proof run
      - never delete `safe/out/validator/skip.env` or `safe/out/validator/tests-filtered/` during normal invocation
    - Create `safe/scripts/run-validator-regressions.sh` as the stable gate for local reproductions derived from validator failures. The script must:
      - use `#!/usr/bin/env bash` and `set -euo pipefail`
      - locate and export `REPO_ROOT`, `SAFE_ROOT`, and `VALIDATOR_ARTIFACT_ROOT="$REPO_ROOT/safe/out/validator/artifacts"`
      - exit zero with a clear message when `safe/tests/validator/` does not exist
      - discover executable `*.sh` files directly under `safe/tests/validator/` in lexical order
      - fail if `safe/tests/validator/` exists but no executable `*.sh` tests are present
      - run each discovered script from `REPO_ROOT`
      - pass through any extra command-line arguments to each regression script
    - Create `safe/scripts/check-validator-phase-results.py` for Phase 1-5 and final checkers. The script must verify initial classification, completed work, and final table closure without blocking on failures assigned to current or later phases. The script must:
      - use `#!/usr/bin/env python3`
      - accept `--results-root`, `--report`, repeated `--completed-phase`, and repeated `--allow-remaining-phase`
      - accept zero `--completed-phase` arguments for Phase 1 initial-classification verification; in that mode, the set of table rows with `remediation_status` set to `open` must exactly equal the set of currently failing testcase IDs, and every open row must be assigned to one of the provided `--allow-remaining-phase` values
      - read `VALIDATOR_RUNNER_STATUS` from the environment, defaulting to `0` when absent
      - parse result JSON files under `safe/out/validator/artifacts/port-04-test/results/libzstd/`, excluding `summary.json`
      - parse the strict `validator-report.md` failure table and map each `testcase_id` to exactly one `assigned_remediation_phase`, `remediation_status`, `regression_test`, `fix_commit`, and `notes`
      - fail if the failure table header or separator row differs from the schema required by this plan
      - fail if any table row has an invalid `assigned_remediation_phase`, invalid `remediation_status`, or duplicate `testcase_id`
      - fail if any currently failing testcase ID was not present in the Phase 1 failure table
      - fail when any failed testcase has no assignment
      - fail when any failed testcase is assigned to a completed phase
      - fail when any failed testcase is assigned to a phase outside `--allow-remaining-phase`
      - fail when any row assigned to a completed phase still has `remediation_status` set to `open`
      - allow an `open` row that was originally assigned to a completed phase only after the report reassigns it to an `--allow-remaining-phase` value; when the reassignment is for suspected validator-bug triage, require `notes` to contain `suspected_validator_bug_deferred_to_phase5:<source_phase_id>`
      - fail when a `fixed` row lacks `regression_test` or `fix_commit`
      - fail when a `skipped_validator_bug` row lacks `notes` or mentions no generated skip artifact
      - fail when failed testcases remain but `VALIDATOR_RUNNER_STATUS` is zero, because the strict helper must exit nonzero while validator findings remain
      - fail when no failed testcase remains but `VALIDATOR_RUNNER_STATUS` is nonzero, because proof and helper infrastructure must pass on a clean matrix
      - accept zero `--allow-remaining-phase` arguments for Phase 5 and Phase 6 all-completed verification; in that mode, fail if any current result JSON is failing, if any table row still has `remediation_status` set to `open`, or if any completed row is missing its required regression, fix commit, or generated skip-artifact evidence
      - print passed completed phases and any allowed remaining failed testcase IDs

    ## Implementation Details

    ### Workflow Artifact Contract

    - This plan remains the authoritative validator plan. Existing `.plan/workflow-structure.yaml`, `.plan/phases/*.md`, and `workflow.yaml` may describe prior safe-port phases and must be replaced in place from this validator plan when the final workflow is materialized. Do not load prompts or checks from stale phase files.
    - The validator repository reference discovered during planning is `1319bb0374ef66428a42dd71e49553c6d057feaf` on 2026-04-28; record the actual checked-out commit after clone or fast-forward in `validator-report.md`.

    - Clone or update validator:
    ```bash
    if [ -d validator/.git ]; then
      test -z "$(git -C validator status --porcelain --untracked-files=no)"
      test "$(git -C validator remote get-url origin)" = "https://github.com/safelibs/validator"
      git -C validator fetch origin main
      current_branch=$(git -C validator branch --show-current)
      if [ "$current_branch" != "main" ]; then
        git -C validator switch main || git -C validator switch --track origin/main
      fi
      git -C validator merge --ff-only origin/main
    else
      git clone https://github.com/safelibs/validator validator
    fi
    ```

    - Set up Python:

    ```bash
    if python3 -c 'import yaml' >/dev/null 2>&1; then
      PYTHON=python3
    else
      mkdir -p safe/out/validator
      python3 -m venv safe/out/validator/venv
      safe/out/validator/venv/bin/python -m pip install --upgrade pip PyYAML
      PYTHON="$PWD/safe/out/validator/venv/bin/python"
    fi
    export PYTHON

    # Initial Phase 1 setup only. Do not embed this cleanup in
    # safe/scripts/run-validator-libzstd.sh; later final runs must preserve a
    # Phase 5-generated skip.env and filtered test root.
    rm -f safe/out/validator/skip.env
    rm -rf safe/out/validator/tests-filtered
    rm -rf safe/out/validator/artifacts
    ```

    - Build and stage override packages:

    ```bash
    bash safe/scripts/build-artifacts.sh --release
    bash safe/scripts/build-original-cli-against-safe.sh
    env -u DEB_BUILD_PROFILES bash safe/scripts/build-deb.sh
    metadata="$PWD/safe/out/deb/default/metadata.env"
    package_dir="$PWD/safe/out/deb/default/packages"
    test -f "$metadata"
    (
      set -euo pipefail
      source "$metadata"
      test "${BUILD_TAG:-}" = default
      test -z "${PROFILES:-}"
      test "${PACKAGE_DIR:-}" = "$package_dir"
      test "${INSTALL_ROOT:-}" = "$PWD/safe/out/deb/default/stage-root"
    )
    rm -rf safe/out/validator/override-debs/libzstd
    mkdir -p safe/out/validator/override-debs/libzstd
    shopt -s nullglob
    for pkg in libzstd1 libzstd-dev zstd; do
      matches=("$package_dir"/${pkg}_*.deb)
      if [ "${#matches[@]}" -ne 1 ]; then
        printf 'expected exactly one %s .deb, found %d\n' "$pkg" "${#matches[@]}" >&2
        exit 1
      fi
      cp -a "${matches[0]}" safe/out/validator/override-debs/libzstd/
    done
    shopt -u nullglob
    if find safe/out/validator/override-debs/libzstd -maxdepth 1 -type f -name 'libzstd1-udeb_*' | grep -q .; then
      printf 'validator override leaf must not include libzstd1-udeb\n' >&2
      exit 1
    fi
    ```

    - Generate the local port lock from the staged override packages:

    ```bash
    mkdir -p safe/out/validator/artifacts/proof
    "$PYTHON" - <<'PY'
    from __future__ import annotations

    import hashlib
    import json
    import pathlib
    import subprocess
    from datetime import datetime, timezone

    repo_root = pathlib.Path.cwd()
    override_leaf = repo_root / "safe/out/validator/override-debs/libzstd"
    lock_path = repo_root / "safe/out/validator/artifacts/proof/port-04-test-debs-lock.json"
    packages = ["libzstd1", "libzstd-dev", "zstd"]
    commit = subprocess.check_output(["git", "rev-parse", "HEAD"], text=True).strip()
    if len(commit) != 40 or any(ch not in "0123456789abcdef" for ch in commit):
        raise SystemExit(f"git HEAD must be a 40-character lowercase hex commit, got {commit!r}")

    debs = []
    for package in packages:
        matches = sorted(override_leaf.glob(f"{package}_*.deb"))
        if len(matches) != 1:
            raise SystemExit(f"expected exactly one staged {package} .deb, found {len(matches)}")
        path = matches[0]
        architecture = subprocess.check_output(["dpkg-deb", "-f", str(path), "Architecture"], text=True).strip()
        digest = hashlib.sha256(path.read_bytes()).hexdigest()
        debs.append(
            {
                "package": package,
                "filename": path.name,
                "architecture": architecture,
                "sha256": digest,
                "size": path.stat().st_size,
            }
        )

    lock = {
        "schema_version": 1,
        "mode": "port-04-test",
        "generated_at": datetime.now(timezone.utc).replace(microsecond=0).isoformat().replace("+00:00", "Z"),
        "source_config": "validator/repositories.yml",
        "source_inventory": "local override packages from safe/out/validator/override-debs/libzstd",
        "libraries": [
            {
                "library": "libzstd",
                "repository": "local/port-libzstd",
                "tag_ref": "refs/tags/libzstd/04-test-local",
                "commit": commit,
                "release_tag": f"build-{commit[:12]}",
                "debs": debs,
                "unported_original_packages": [],
            }
        ],
    }
    lock_path.parent.mkdir(parents=True, exist_ok=True)
    lock_path.write_text(json.dumps(lock, indent=2, sort_keys=True) + "\n", encoding="utf-8")
    PY
    ```

    - Run validator tooling:

    ```bash
    PYTHON="$PYTHON" make -C validator unit
    PYTHON="$PYTHON" make -C validator check-testcases
    validator_status=0
    PYTHON="$PYTHON" bash validator/test.sh \
      --config "$PWD/validator/repositories.yml" \
      --tests-root "$PWD/validator/tests" \
      --artifact-root "$PWD/safe/out/validator/artifacts" \
      --mode port-04-test \
      --library libzstd \
      --override-deb-root "$PWD/safe/out/validator/override-debs" \
      --port-deb-lock "$PWD/safe/out/validator/artifacts/proof/port-04-test-debs-lock.json" \
      --record-casts || validator_status=$?
    ```

    - Read the mode-specific summary and convert validator findings into a strict helper failure:

    ```bash
    summary_path="$PWD/safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json"
    summary_failed=1
    if [ -f "$summary_path" ]; then
      summary_failed=$("$PYTHON" -c 'import json,sys; print(int(json.load(open(sys.argv[1]))["failed"]))' "$summary_path")
    fi
    strict_status="$validator_status"
    if [ "$summary_failed" -ne 0 ]; then
      strict_status=1
    fi
    ```

    - If the matrix command succeeds and the summary has zero failures, generate proof:

    ```bash
    if [ "$validator_status" -eq 0 ] && [ "$summary_failed" -eq 0 ]; then
      "$PYTHON" validator/tools/verify_proof_artifacts.py \
        --config "$PWD/validator/repositories.yml" \
        --tests-root "$PWD/validator/tests" \
        --artifact-root "$PWD/safe/out/validator/artifacts" \
        --proof-output "$PWD/safe/out/validator/artifacts/proof/port-04-test-validation-proof.json" \
        --mode port-04-test \
        --library libzstd \
        --min-source-cases 5 \
        --min-usage-cases 80 \
        --min-cases 85 \
        --require-casts
    fi
    ```

    - `safe/scripts/run-validator-libzstd.sh` must exit nonzero if `strict_status` is nonzero or if proof generation fails. Phase 1 evidence collection may invoke the helper under `set +e` so `validator-report.md` can still be written and committed when the initial validator run exposes failures.

    - If the matrix command fails or the summary reports failed cases, parse result JSON files and classify failures into:
      - source CLI/dictionary/multiframe/corruption failures -> Phase 2
      - streaming C API failures -> Phase 3
      - libarchive usage failures -> Phase 4
      - packaging, validator infrastructure, or mixed residual failures -> Phase 5
    - End Phase 1 by committing `.gitignore`, `validator-report.md`, `safe/scripts/run-validator-libzstd.sh`, `safe/scripts/check-validator-phase-results.py`, and `safe/scripts/run-validator-regressions.sh`; the commit must exist even when the initial validator matrix fails.

    ## Verification Phases

    - `check_validator_setup_software_tester` - type: `check`; fixed `bounce_target: impl_validator_setup_initial_run`; purpose: verify that validator setup, package override staging, initial matrix execution, and failure classification are reproducible. Commands to run:
    - `git -C validator rev-parse HEAD`
      - `test -x safe/scripts/run-validator-libzstd.sh`
      - `test -x safe/scripts/check-validator-phase-results.py`
      - `test -x safe/scripts/run-validator-regressions.sh`
      - `rg -n 'env -u DEB_BUILD_PROFILES[[:space:]]+bash[[:space:]]+.*build-deb.sh' safe/scripts/run-validator-libzstd.sh`
      - `bash -lc 'set +e; DEB_BUILD_PROFILES=noudeb bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --allow-remaining-phase impl_validator_source_cli_regressions --allow-remaining-phase impl_validator_streaming_capi_regressions --allow-remaining-phase impl_validator_libarchive_usage_regressions --allow-remaining-phase impl_validator_remaining_burn_down'`
      - `bash -lc 'source safe/out/deb/default/metadata.env; test "${BUILD_TAG:-}" = default; test -z "${PROFILES:-}"; test "${PACKAGE_DIR:-}" = "$PWD/safe/out/deb/default/packages"; test "${INSTALL_ROOT:-}" = "$PWD/safe/out/deb/default/stage-root"'`
      - `test -f validator-report.md`
      - `find safe/out/validator/override-debs/libzstd -maxdepth 1 -type f -name '*.deb' -printf '%f\n' | sort`
      - `test -f safe/out/validator/artifacts/proof/port-04-test-debs-lock.json`
      - `python3 -m json.tool safe/out/validator/artifacts/proof/port-04-test-debs-lock.json >/dev/null`
      - `test -f safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json`
      - `python3 -m json.tool safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json >/dev/null`
      - `rg -n 'Validator Commit|Initial Run|Failure Classification|assigned_remediation_phase|libzstd' validator-report.md`
    - `check_validator_setup_senior_tester` - type: `check`; fixed `bounce_target: impl_validator_setup_initial_run`; purpose: review artifact flow and ensure the validator checkout was not modified or committed. Commands to run:
      - `bash -lc 'base=$(awk "/Phase 1 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'`
      - `git status --short`
      - `git check-ignore -v validator`
      - `git -C validator status --short --branch`
      - `rg -n 'validator/|safe/out/validator|override-debs|proof' .gitignore validator-report.md`
      - `test ! -e .gitmodules || ! rg -n 'validator' .gitmodules`

    ## Verification

    - Run the two check phases above.
    - Confirm `validator-report.md` includes the validator commit and initial run summary.
    - Confirm `validator/` is ignored and not staged for commit.
    - Confirm the phase commit exists with `git log --oneline -1`.

    ## Success Criteria

    - `validator/` exists, is on the fast-forwarded validator `main`, and is ignored by the parent repository.
    - The known validator reference commit `1319bb0374ef66428a42dd71e49553c6d057feaf` is preserved as context and the actual checked-out commit is recorded in `validator-report.md`.
    - The helper scripts are tracked, executable, rerunnable, and use `port-04-test` with the generated local port lock.
    - The override package leaf contains exactly `libzstd1`, `libzstd-dev`, and `zstd` `.deb` files from the verified default package root, never `libzstd1-udeb`.
    - The local port lock and summary JSON are valid and match the staged package artifacts.
    - `validator-report.md` contains the validator commit, initial summary, exact failure table schema, skip list, and assigned remediation phases for every current failure.
    - The generated workflow artifacts replace stale prior safe-port workflow descriptions in place, including `workflow.yaml` when the final workflow is materialized from these phase files.

    ## Git Commit Requirement

    The implementer must commit all Phase 1 work to git before yielding to the verifier phases.
    {% endraw %}
- id: check_validator_setup_software_tester
  type: check
  bounce_target: impl_validator_setup_initial_run
  prompt: |
    Reviewer Role: Software tester reviewer for Phase 1 validator setup and initial evidence.

    Workflow goal: {{GOAL}}

    Work from `/home/yans/safelibs/pipeline/ports/port-libzstd`.
    This prompt is self-contained; the agent sees only this prompt.
    Use `.plan/workflow-structure.yaml` as the authoritative source for phase order, phase IDs, phase types, and this phase's fixed `bounce_target` value. Do not invent, rename, or reorder phases.
    Review the committed work from the implement phase `impl_validator_setup_initial_run`. Do not edit implementation files unless the workflow explicitly bounces and you become the implementer for the bounced phase.

    Topology Contract
    - Phase order: `2`
    - Phase ID: `check_validator_setup_software_tester`
    - Phase type: `check`
    - Fixed top-level `bounce_target`: `impl_validator_setup_initial_run`

    Preexisting Inputs
    - `validator/`
    - `validator-report.md`
    - `.gitignore`
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/check-validator-phase-results.py`
    - `safe/scripts/run-validator-regressions.sh`
    - `safe/out/validator/override-debs/libzstd/`
    - `safe/out/validator/artifacts/proof/port-04-test-debs-lock.json`
    - `safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json`

    Specific Things To Verify
    - validator/ is present, on the expected upstream main checkout, and ignored by the parent repository.
    - The validator rerun helper, phase-result checker, and regression helper are tracked, executable, and enforce the default Debian package root.
    - Override .deb staging contains the expected libzstd packages and produces valid lock and summary JSON artifacts.
    - validator-report.md records the validator commit, initial run summary, failure table schema, and assigned remediation phases.

    Commands To Run
    Run these commands exactly, unless an earlier command failure makes a later command impossible. Record any failure details in the verdict reason.

    {% raw %}
    ```bash
    git -C validator rev-parse HEAD
    test -x safe/scripts/run-validator-libzstd.sh
    test -x safe/scripts/check-validator-phase-results.py
    test -x safe/scripts/run-validator-regressions.sh
    rg -n 'env -u DEB_BUILD_PROFILES[[:space:]]+bash[[:space:]]+.*build-deb.sh' safe/scripts/run-validator-libzstd.sh
    bash -lc 'set +e; DEB_BUILD_PROFILES=noudeb bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --allow-remaining-phase impl_validator_source_cli_regressions --allow-remaining-phase impl_validator_streaming_capi_regressions --allow-remaining-phase impl_validator_libarchive_usage_regressions --allow-remaining-phase impl_validator_remaining_burn_down'
    bash -lc 'source safe/out/deb/default/metadata.env; test "${BUILD_TAG:-}" = default; test -z "${PROFILES:-}"; test "${PACKAGE_DIR:-}" = "$PWD/safe/out/deb/default/packages"; test "${INSTALL_ROOT:-}" = "$PWD/safe/out/deb/default/stage-root"'
    test -f validator-report.md
    find safe/out/validator/override-debs/libzstd -maxdepth 1 -type f -name '*.deb' -printf '%f\n' | sort
    test -f safe/out/validator/artifacts/proof/port-04-test-debs-lock.json
    python3 -m json.tool safe/out/validator/artifacts/proof/port-04-test-debs-lock.json >/dev/null
    test -f safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json
    python3 -m json.tool safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json >/dev/null
    rg -n 'Validator Commit|Initial Run|Failure Classification|assigned_remediation_phase|libzstd' validator-report.md
    ```
    {% endraw %}

    Phase Source Content For The Implement Block Being Verified
    {% raw %}
    # Phase 1: Validator Checkout, Override Packaging, and Initial Run

    ## Phase Name

    Validator Setup And Initial Evidence

    ## Implement Phase ID

    `impl_validator_setup_initial_run`

    ## Preexisting Inputs

    - Goal text in `.plan/goal.md`
    - Existing safe implementation under `safe/`
    - Existing upstream source snapshot under `original/libzstd-1.5.5+dfsg2/`
    - Existing package scripts:
      - `safe/scripts/build-artifacts.sh`
      - `safe/scripts/build-original-cli-against-safe.sh`
      - `safe/scripts/build-deb.sh`
    - Existing package metadata:
      - `safe/debian/control`
      - `safe/debian/rules`
      - `safe/debian/*.install`
    - Existing release gates:
      - `safe/scripts/run-full-suite.sh`
      - `safe/scripts/verify-export-parity.sh`
      - `safe/scripts/verify-install-layout.sh`
    - Existing `.gitignore`
    - No required preexisting `validator/` checkout

    ## New Outputs

    - `validator/` checkout at the latest fast-forwarded `main` commit, or the existing checkout updated in place
    - generated `safe/out/validator/venv/` with PyYAML if host Python lacks `yaml`
    - generated `safe/out/validator/override-debs/libzstd/*.deb`
    - generated `safe/out/validator/artifacts/proof/port-04-test-debs-lock.json`
    - generated `safe/out/validator/artifacts/port-04-test/results/libzstd/*.json`
    - generated `safe/out/validator/artifacts/port-04-test/logs/libzstd/*.log`
    - generated `safe/out/validator/artifacts/port-04-test/casts/libzstd/*.cast`
    - generated `safe/out/validator/artifacts/proof/port-04-test-validation-proof.json` only if the initial run passes
    - tracked `validator-report.md`
    - tracked `.gitignore` update for `validator/`
    - tracked `safe/scripts/run-validator-libzstd.sh` helper
    - tracked `safe/scripts/check-validator-phase-results.py` helper
    - tracked `safe/scripts/run-validator-regressions.sh` helper
    - a git commit containing `.gitignore`, `validator-report.md`, and the tracked helper scripts created in this phase

    ## File Changes

    - Add `validator/` to `.gitignore`.
    - Create `validator-report.md` with sections:
      - `Phase 1 Base Commit: <40-hex commit captured before edits>`
      - validator URL and commit
      - local safe commit
      - Python setup path
      - package filenames and SHA256 hashes
      - generated port lock path, repository string, tag ref, commit, release tag, package architectures, sizes, and SHA256 hashes
      - exact commands run
      - matrix inventory counts: 5 source cases, 80 usage cases, 85 total cases
      - initial summary counts from `safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json`
      - failing testcase table with the exact schema `testcase_id`, `kind`, `client_application`, `exit_code`, `error`, `result_path`, `log_path`, `assigned_remediation_phase`, `remediation_status`, `regression_test`, `fix_commit`, and `notes`
      - skip list, initially empty
    - Create `safe/scripts/run-validator-libzstd.sh` as the canonical rerun command. The script must:
      - use `#!/usr/bin/env bash` and `set -euo pipefail`
      - locate `REPO_ROOT`, `SAFE_ROOT`, and `VALIDATOR_ROOT`
      - create `safe/out/validator/venv` only when needed for `import yaml`
      - export `PYTHON`
      - build safe artifacts and `.deb` packages
      - run the Debian package build as `env -u DEB_BUILD_PROFILES bash safe/scripts/build-deb.sh`, so a caller's `DEB_BUILD_PROFILES` cannot redirect the build to a non-default package root
      - source and validate `safe/out/deb/default/metadata.env` before staging packages, requiring `BUILD_TAG=default`, empty `PROFILES`, `PACKAGE_DIR=$SAFE_ROOT/out/deb/default/packages`, and `INSTALL_ROOT=$SAFE_ROOT/out/deb/default/stage-root`
      - stage exactly `libzstd1_*.deb`, `libzstd-dev_*.deb`, and `zstd_*.deb`
      - clear stale generated files under `safe/out/validator/artifacts/port-04-test/` and stale generated proof/lock files under `safe/out/validator/artifacts/proof/` before each run
      - generate `safe/out/validator/artifacts/proof/port-04-test-debs-lock.json` from the staged `.deb` files before running the matrix
      - run `make unit` and `make check-testcases` in `validator`
      - run the full libzstd validator matrix with `--mode port-04-test`, `--port-deb-lock`, and `--record-casts`; do not implement partial `--source-only` or `--usage-only` modes
      - capture the validator matrix exit status so failed-result JSON remains available for report classification
      - read `safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json` after every matrix run and treat any nonzero `failed` count as a helper failure even when `validator/test.sh` exits zero
      - run proof generation only when the matrix command exits zero and the summary reports zero failed cases
      - default to `validator/tests` and proof thresholds 5 source, 80 usage, and 85 total cases
      - if generated `safe/out/validator/skip.env` exists, source it only after confirming `validator-report.md` mentions every `VALIDATOR_SKIPPED_CASES` entry, then require `VALIDATOR_TESTS_ROOT/libzstd/` and `VALIDATOR_TESTS_ROOT/tests/libzstd/` to exist, run `"$PYTHON" validator/tools/testcases.py --config "$PWD/validator/repositories.yml" --tests-root "$VALIDATOR_TESTS_ROOT" --library libzstd --check --min-source-cases "$VALIDATOR_MIN_SOURCE_CASES" --min-usage-cases "$VALIDATOR_MIN_USAGE_CASES" --min-cases "$VALIDATOR_MIN_CASES"`, and use `VALIDATOR_TESTS_ROOT`, `VALIDATOR_MIN_SOURCE_CASES`, `VALIDATOR_MIN_USAGE_CASES`, and `VALIDATOR_MIN_CASES` for the filtered final matrix and proof run
      - never delete `safe/out/validator/skip.env` or `safe/out/validator/tests-filtered/` during normal invocation
    - Create `safe/scripts/run-validator-regressions.sh` as the stable gate for local reproductions derived from validator failures. The script must:
      - use `#!/usr/bin/env bash` and `set -euo pipefail`
      - locate and export `REPO_ROOT`, `SAFE_ROOT`, and `VALIDATOR_ARTIFACT_ROOT="$REPO_ROOT/safe/out/validator/artifacts"`
      - exit zero with a clear message when `safe/tests/validator/` does not exist
      - discover executable `*.sh` files directly under `safe/tests/validator/` in lexical order
      - fail if `safe/tests/validator/` exists but no executable `*.sh` tests are present
      - run each discovered script from `REPO_ROOT`
      - pass through any extra command-line arguments to each regression script
    - Create `safe/scripts/check-validator-phase-results.py` for Phase 1-5 and final checkers. The script must verify initial classification, completed work, and final table closure without blocking on failures assigned to current or later phases. The script must:
      - use `#!/usr/bin/env python3`
      - accept `--results-root`, `--report`, repeated `--completed-phase`, and repeated `--allow-remaining-phase`
      - accept zero `--completed-phase` arguments for Phase 1 initial-classification verification; in that mode, the set of table rows with `remediation_status` set to `open` must exactly equal the set of currently failing testcase IDs, and every open row must be assigned to one of the provided `--allow-remaining-phase` values
      - read `VALIDATOR_RUNNER_STATUS` from the environment, defaulting to `0` when absent
      - parse result JSON files under `safe/out/validator/artifacts/port-04-test/results/libzstd/`, excluding `summary.json`
      - parse the strict `validator-report.md` failure table and map each `testcase_id` to exactly one `assigned_remediation_phase`, `remediation_status`, `regression_test`, `fix_commit`, and `notes`
      - fail if the failure table header or separator row differs from the schema required by this plan
      - fail if any table row has an invalid `assigned_remediation_phase`, invalid `remediation_status`, or duplicate `testcase_id`
      - fail if any currently failing testcase ID was not present in the Phase 1 failure table
      - fail when any failed testcase has no assignment
      - fail when any failed testcase is assigned to a completed phase
      - fail when any failed testcase is assigned to a phase outside `--allow-remaining-phase`
      - fail when any row assigned to a completed phase still has `remediation_status` set to `open`
      - allow an `open` row that was originally assigned to a completed phase only after the report reassigns it to an `--allow-remaining-phase` value; when the reassignment is for suspected validator-bug triage, require `notes` to contain `suspected_validator_bug_deferred_to_phase5:<source_phase_id>`
      - fail when a `fixed` row lacks `regression_test` or `fix_commit`
      - fail when a `skipped_validator_bug` row lacks `notes` or mentions no generated skip artifact
      - fail when failed testcases remain but `VALIDATOR_RUNNER_STATUS` is zero, because the strict helper must exit nonzero while validator findings remain
      - fail when no failed testcase remains but `VALIDATOR_RUNNER_STATUS` is nonzero, because proof and helper infrastructure must pass on a clean matrix
      - accept zero `--allow-remaining-phase` arguments for Phase 5 and Phase 6 all-completed verification; in that mode, fail if any current result JSON is failing, if any table row still has `remediation_status` set to `open`, or if any completed row is missing its required regression, fix commit, or generated skip-artifact evidence
      - print passed completed phases and any allowed remaining failed testcase IDs

    ## Implementation Details

    ### Workflow Artifact Contract

    - This plan remains the authoritative validator plan. Existing `.plan/workflow-structure.yaml`, `.plan/phases/*.md`, and `workflow.yaml` may describe prior safe-port phases and must be replaced in place from this validator plan when the final workflow is materialized. Do not load prompts or checks from stale phase files.
    - The validator repository reference discovered during planning is `1319bb0374ef66428a42dd71e49553c6d057feaf` on 2026-04-28; record the actual checked-out commit after clone or fast-forward in `validator-report.md`.

    - Clone or update validator:
    ```bash
    if [ -d validator/.git ]; then
      test -z "$(git -C validator status --porcelain --untracked-files=no)"
      test "$(git -C validator remote get-url origin)" = "https://github.com/safelibs/validator"
      git -C validator fetch origin main
      current_branch=$(git -C validator branch --show-current)
      if [ "$current_branch" != "main" ]; then
        git -C validator switch main || git -C validator switch --track origin/main
      fi
      git -C validator merge --ff-only origin/main
    else
      git clone https://github.com/safelibs/validator validator
    fi
    ```

    - Set up Python:

    ```bash
    if python3 -c 'import yaml' >/dev/null 2>&1; then
      PYTHON=python3
    else
      mkdir -p safe/out/validator
      python3 -m venv safe/out/validator/venv
      safe/out/validator/venv/bin/python -m pip install --upgrade pip PyYAML
      PYTHON="$PWD/safe/out/validator/venv/bin/python"
    fi
    export PYTHON

    # Initial Phase 1 setup only. Do not embed this cleanup in
    # safe/scripts/run-validator-libzstd.sh; later final runs must preserve a
    # Phase 5-generated skip.env and filtered test root.
    rm -f safe/out/validator/skip.env
    rm -rf safe/out/validator/tests-filtered
    rm -rf safe/out/validator/artifacts
    ```

    - Build and stage override packages:

    ```bash
    bash safe/scripts/build-artifacts.sh --release
    bash safe/scripts/build-original-cli-against-safe.sh
    env -u DEB_BUILD_PROFILES bash safe/scripts/build-deb.sh
    metadata="$PWD/safe/out/deb/default/metadata.env"
    package_dir="$PWD/safe/out/deb/default/packages"
    test -f "$metadata"
    (
      set -euo pipefail
      source "$metadata"
      test "${BUILD_TAG:-}" = default
      test -z "${PROFILES:-}"
      test "${PACKAGE_DIR:-}" = "$package_dir"
      test "${INSTALL_ROOT:-}" = "$PWD/safe/out/deb/default/stage-root"
    )
    rm -rf safe/out/validator/override-debs/libzstd
    mkdir -p safe/out/validator/override-debs/libzstd
    shopt -s nullglob
    for pkg in libzstd1 libzstd-dev zstd; do
      matches=("$package_dir"/${pkg}_*.deb)
      if [ "${#matches[@]}" -ne 1 ]; then
        printf 'expected exactly one %s .deb, found %d\n' "$pkg" "${#matches[@]}" >&2
        exit 1
      fi
      cp -a "${matches[0]}" safe/out/validator/override-debs/libzstd/
    done
    shopt -u nullglob
    if find safe/out/validator/override-debs/libzstd -maxdepth 1 -type f -name 'libzstd1-udeb_*' | grep -q .; then
      printf 'validator override leaf must not include libzstd1-udeb\n' >&2
      exit 1
    fi
    ```

    - Generate the local port lock from the staged override packages:

    ```bash
    mkdir -p safe/out/validator/artifacts/proof
    "$PYTHON" - <<'PY'
    from __future__ import annotations

    import hashlib
    import json
    import pathlib
    import subprocess
    from datetime import datetime, timezone

    repo_root = pathlib.Path.cwd()
    override_leaf = repo_root / "safe/out/validator/override-debs/libzstd"
    lock_path = repo_root / "safe/out/validator/artifacts/proof/port-04-test-debs-lock.json"
    packages = ["libzstd1", "libzstd-dev", "zstd"]
    commit = subprocess.check_output(["git", "rev-parse", "HEAD"], text=True).strip()
    if len(commit) != 40 or any(ch not in "0123456789abcdef" for ch in commit):
        raise SystemExit(f"git HEAD must be a 40-character lowercase hex commit, got {commit!r}")

    debs = []
    for package in packages:
        matches = sorted(override_leaf.glob(f"{package}_*.deb"))
        if len(matches) != 1:
            raise SystemExit(f"expected exactly one staged {package} .deb, found {len(matches)}")
        path = matches[0]
        architecture = subprocess.check_output(["dpkg-deb", "-f", str(path), "Architecture"], text=True).strip()
        digest = hashlib.sha256(path.read_bytes()).hexdigest()
        debs.append(
            {
                "package": package,
                "filename": path.name,
                "architecture": architecture,
                "sha256": digest,
                "size": path.stat().st_size,
            }
        )

    lock = {
        "schema_version": 1,
        "mode": "port-04-test",
        "generated_at": datetime.now(timezone.utc).replace(microsecond=0).isoformat().replace("+00:00", "Z"),
        "source_config": "validator/repositories.yml",
        "source_inventory": "local override packages from safe/out/validator/override-debs/libzstd",
        "libraries": [
            {
                "library": "libzstd",
                "repository": "local/port-libzstd",
                "tag_ref": "refs/tags/libzstd/04-test-local",
                "commit": commit,
                "release_tag": f"build-{commit[:12]}",
                "debs": debs,
                "unported_original_packages": [],
            }
        ],
    }
    lock_path.parent.mkdir(parents=True, exist_ok=True)
    lock_path.write_text(json.dumps(lock, indent=2, sort_keys=True) + "\n", encoding="utf-8")
    PY
    ```

    - Run validator tooling:

    ```bash
    PYTHON="$PYTHON" make -C validator unit
    PYTHON="$PYTHON" make -C validator check-testcases
    validator_status=0
    PYTHON="$PYTHON" bash validator/test.sh \
      --config "$PWD/validator/repositories.yml" \
      --tests-root "$PWD/validator/tests" \
      --artifact-root "$PWD/safe/out/validator/artifacts" \
      --mode port-04-test \
      --library libzstd \
      --override-deb-root "$PWD/safe/out/validator/override-debs" \
      --port-deb-lock "$PWD/safe/out/validator/artifacts/proof/port-04-test-debs-lock.json" \
      --record-casts || validator_status=$?
    ```

    - Read the mode-specific summary and convert validator findings into a strict helper failure:

    ```bash
    summary_path="$PWD/safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json"
    summary_failed=1
    if [ -f "$summary_path" ]; then
      summary_failed=$("$PYTHON" -c 'import json,sys; print(int(json.load(open(sys.argv[1]))["failed"]))' "$summary_path")
    fi
    strict_status="$validator_status"
    if [ "$summary_failed" -ne 0 ]; then
      strict_status=1
    fi
    ```

    - If the matrix command succeeds and the summary has zero failures, generate proof:

    ```bash
    if [ "$validator_status" -eq 0 ] && [ "$summary_failed" -eq 0 ]; then
      "$PYTHON" validator/tools/verify_proof_artifacts.py \
        --config "$PWD/validator/repositories.yml" \
        --tests-root "$PWD/validator/tests" \
        --artifact-root "$PWD/safe/out/validator/artifacts" \
        --proof-output "$PWD/safe/out/validator/artifacts/proof/port-04-test-validation-proof.json" \
        --mode port-04-test \
        --library libzstd \
        --min-source-cases 5 \
        --min-usage-cases 80 \
        --min-cases 85 \
        --require-casts
    fi
    ```

    - `safe/scripts/run-validator-libzstd.sh` must exit nonzero if `strict_status` is nonzero or if proof generation fails. Phase 1 evidence collection may invoke the helper under `set +e` so `validator-report.md` can still be written and committed when the initial validator run exposes failures.

    - If the matrix command fails or the summary reports failed cases, parse result JSON files and classify failures into:
      - source CLI/dictionary/multiframe/corruption failures -> Phase 2
      - streaming C API failures -> Phase 3
      - libarchive usage failures -> Phase 4
      - packaging, validator infrastructure, or mixed residual failures -> Phase 5
    - End Phase 1 by committing `.gitignore`, `validator-report.md`, `safe/scripts/run-validator-libzstd.sh`, `safe/scripts/check-validator-phase-results.py`, and `safe/scripts/run-validator-regressions.sh`; the commit must exist even when the initial validator matrix fails.

    ## Verification Phases

    - `check_validator_setup_software_tester` - type: `check`; fixed `bounce_target: impl_validator_setup_initial_run`; purpose: verify that validator setup, package override staging, initial matrix execution, and failure classification are reproducible. Commands to run:
    - `git -C validator rev-parse HEAD`
      - `test -x safe/scripts/run-validator-libzstd.sh`
      - `test -x safe/scripts/check-validator-phase-results.py`
      - `test -x safe/scripts/run-validator-regressions.sh`
      - `rg -n 'env -u DEB_BUILD_PROFILES[[:space:]]+bash[[:space:]]+.*build-deb.sh' safe/scripts/run-validator-libzstd.sh`
      - `bash -lc 'set +e; DEB_BUILD_PROFILES=noudeb bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --allow-remaining-phase impl_validator_source_cli_regressions --allow-remaining-phase impl_validator_streaming_capi_regressions --allow-remaining-phase impl_validator_libarchive_usage_regressions --allow-remaining-phase impl_validator_remaining_burn_down'`
      - `bash -lc 'source safe/out/deb/default/metadata.env; test "${BUILD_TAG:-}" = default; test -z "${PROFILES:-}"; test "${PACKAGE_DIR:-}" = "$PWD/safe/out/deb/default/packages"; test "${INSTALL_ROOT:-}" = "$PWD/safe/out/deb/default/stage-root"'`
      - `test -f validator-report.md`
      - `find safe/out/validator/override-debs/libzstd -maxdepth 1 -type f -name '*.deb' -printf '%f\n' | sort`
      - `test -f safe/out/validator/artifacts/proof/port-04-test-debs-lock.json`
      - `python3 -m json.tool safe/out/validator/artifacts/proof/port-04-test-debs-lock.json >/dev/null`
      - `test -f safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json`
      - `python3 -m json.tool safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json >/dev/null`
      - `rg -n 'Validator Commit|Initial Run|Failure Classification|assigned_remediation_phase|libzstd' validator-report.md`
    - `check_validator_setup_senior_tester` - type: `check`; fixed `bounce_target: impl_validator_setup_initial_run`; purpose: review artifact flow and ensure the validator checkout was not modified or committed. Commands to run:
      - `bash -lc 'base=$(awk "/Phase 1 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'`
      - `git status --short`
      - `git check-ignore -v validator`
      - `git -C validator status --short --branch`
      - `rg -n 'validator/|safe/out/validator|override-debs|proof' .gitignore validator-report.md`
      - `test ! -e .gitmodules || ! rg -n 'validator' .gitmodules`

    ## Verification

    - Run the two check phases above.
    - Confirm `validator-report.md` includes the validator commit and initial run summary.
    - Confirm `validator/` is ignored and not staged for commit.
    - Confirm the phase commit exists with `git log --oneline -1`.

    ## Success Criteria

    - `validator/` exists, is on the fast-forwarded validator `main`, and is ignored by the parent repository.
    - The known validator reference commit `1319bb0374ef66428a42dd71e49553c6d057feaf` is preserved as context and the actual checked-out commit is recorded in `validator-report.md`.
    - The helper scripts are tracked, executable, rerunnable, and use `port-04-test` with the generated local port lock.
    - The override package leaf contains exactly `libzstd1`, `libzstd-dev`, and `zstd` `.deb` files from the verified default package root, never `libzstd1-udeb`.
    - The local port lock and summary JSON are valid and match the staged package artifacts.
    - `validator-report.md` contains the validator commit, initial summary, exact failure table schema, skip list, and assigned remediation phases for every current failure.
    - The generated workflow artifacts replace stale prior safe-port workflow descriptions in place, including `workflow.yaml` when the final workflow is materialized from these phase files.

    ## Git Commit Requirement

    The implementer must commit all Phase 1 work to git before yielding to the verifier phases.
    {% endraw %}

    Verdict Instruction
    End with exactly one verdict line:
    VERDICT: PASS
    VERDICT: FAIL: <reason>
- id: check_validator_setup_senior_tester
  type: check
  bounce_target: impl_validator_setup_initial_run
  prompt: |
    Reviewer Role: Senior tester reviewer for Phase 1 artifact flow, repository hygiene, and validator isolation.

    Workflow goal: {{GOAL}}

    Work from `/home/yans/safelibs/pipeline/ports/port-libzstd`.
    This prompt is self-contained; the agent sees only this prompt.
    Use `.plan/workflow-structure.yaml` as the authoritative source for phase order, phase IDs, phase types, and this phase's fixed `bounce_target` value. Do not invent, rename, or reorder phases.
    Review the committed work from the implement phase `impl_validator_setup_initial_run`. Do not edit implementation files unless the workflow explicitly bounces and you become the implementer for the bounced phase.

    Topology Contract
    - Phase order: `3`
    - Phase ID: `check_validator_setup_senior_tester`
    - Phase type: `check`
    - Fixed top-level `bounce_target`: `impl_validator_setup_initial_run`

    Preexisting Inputs
    - `validator/`
    - `validator-report.md`
    - `.gitignore`
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/check-validator-phase-results.py`
    - `safe/scripts/run-validator-regressions.sh`
    - `safe/out/validator/override-debs/libzstd/`
    - `safe/out/validator/artifacts/proof/port-04-test-debs-lock.json`
    - `safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json`

    Specific Things To Verify
    - The Phase 1 diff is whitespace-clean from the recorded base commit.
    - The parent repository did not commit the validator checkout or stale generated artifacts.
    - validator/ is ignored and the nested validator checkout is clean apart from normal branch status.
    - The report and .gitignore describe the validator, override-deb, and proof artifact flow.

    Commands To Run
    Run these commands exactly, unless an earlier command failure makes a later command impossible. Record any failure details in the verdict reason.

    {% raw %}
    ```bash
    bash -lc 'base=$(awk "/Phase 1 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'
    git status --short
    git check-ignore -v validator
    git -C validator status --short --branch
    rg -n 'validator/|safe/out/validator|override-debs|proof' .gitignore validator-report.md
    test ! -e .gitmodules || ! rg -n 'validator' .gitmodules
    ```
    {% endraw %}

    Phase Source Content For The Implement Block Being Verified
    {% raw %}
    # Phase 1: Validator Checkout, Override Packaging, and Initial Run

    ## Phase Name

    Validator Setup And Initial Evidence

    ## Implement Phase ID

    `impl_validator_setup_initial_run`

    ## Preexisting Inputs

    - Goal text in `.plan/goal.md`
    - Existing safe implementation under `safe/`
    - Existing upstream source snapshot under `original/libzstd-1.5.5+dfsg2/`
    - Existing package scripts:
      - `safe/scripts/build-artifacts.sh`
      - `safe/scripts/build-original-cli-against-safe.sh`
      - `safe/scripts/build-deb.sh`
    - Existing package metadata:
      - `safe/debian/control`
      - `safe/debian/rules`
      - `safe/debian/*.install`
    - Existing release gates:
      - `safe/scripts/run-full-suite.sh`
      - `safe/scripts/verify-export-parity.sh`
      - `safe/scripts/verify-install-layout.sh`
    - Existing `.gitignore`
    - No required preexisting `validator/` checkout

    ## New Outputs

    - `validator/` checkout at the latest fast-forwarded `main` commit, or the existing checkout updated in place
    - generated `safe/out/validator/venv/` with PyYAML if host Python lacks `yaml`
    - generated `safe/out/validator/override-debs/libzstd/*.deb`
    - generated `safe/out/validator/artifacts/proof/port-04-test-debs-lock.json`
    - generated `safe/out/validator/artifacts/port-04-test/results/libzstd/*.json`
    - generated `safe/out/validator/artifacts/port-04-test/logs/libzstd/*.log`
    - generated `safe/out/validator/artifacts/port-04-test/casts/libzstd/*.cast`
    - generated `safe/out/validator/artifacts/proof/port-04-test-validation-proof.json` only if the initial run passes
    - tracked `validator-report.md`
    - tracked `.gitignore` update for `validator/`
    - tracked `safe/scripts/run-validator-libzstd.sh` helper
    - tracked `safe/scripts/check-validator-phase-results.py` helper
    - tracked `safe/scripts/run-validator-regressions.sh` helper
    - a git commit containing `.gitignore`, `validator-report.md`, and the tracked helper scripts created in this phase

    ## File Changes

    - Add `validator/` to `.gitignore`.
    - Create `validator-report.md` with sections:
      - `Phase 1 Base Commit: <40-hex commit captured before edits>`
      - validator URL and commit
      - local safe commit
      - Python setup path
      - package filenames and SHA256 hashes
      - generated port lock path, repository string, tag ref, commit, release tag, package architectures, sizes, and SHA256 hashes
      - exact commands run
      - matrix inventory counts: 5 source cases, 80 usage cases, 85 total cases
      - initial summary counts from `safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json`
      - failing testcase table with the exact schema `testcase_id`, `kind`, `client_application`, `exit_code`, `error`, `result_path`, `log_path`, `assigned_remediation_phase`, `remediation_status`, `regression_test`, `fix_commit`, and `notes`
      - skip list, initially empty
    - Create `safe/scripts/run-validator-libzstd.sh` as the canonical rerun command. The script must:
      - use `#!/usr/bin/env bash` and `set -euo pipefail`
      - locate `REPO_ROOT`, `SAFE_ROOT`, and `VALIDATOR_ROOT`
      - create `safe/out/validator/venv` only when needed for `import yaml`
      - export `PYTHON`
      - build safe artifacts and `.deb` packages
      - run the Debian package build as `env -u DEB_BUILD_PROFILES bash safe/scripts/build-deb.sh`, so a caller's `DEB_BUILD_PROFILES` cannot redirect the build to a non-default package root
      - source and validate `safe/out/deb/default/metadata.env` before staging packages, requiring `BUILD_TAG=default`, empty `PROFILES`, `PACKAGE_DIR=$SAFE_ROOT/out/deb/default/packages`, and `INSTALL_ROOT=$SAFE_ROOT/out/deb/default/stage-root`
      - stage exactly `libzstd1_*.deb`, `libzstd-dev_*.deb`, and `zstd_*.deb`
      - clear stale generated files under `safe/out/validator/artifacts/port-04-test/` and stale generated proof/lock files under `safe/out/validator/artifacts/proof/` before each run
      - generate `safe/out/validator/artifacts/proof/port-04-test-debs-lock.json` from the staged `.deb` files before running the matrix
      - run `make unit` and `make check-testcases` in `validator`
      - run the full libzstd validator matrix with `--mode port-04-test`, `--port-deb-lock`, and `--record-casts`; do not implement partial `--source-only` or `--usage-only` modes
      - capture the validator matrix exit status so failed-result JSON remains available for report classification
      - read `safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json` after every matrix run and treat any nonzero `failed` count as a helper failure even when `validator/test.sh` exits zero
      - run proof generation only when the matrix command exits zero and the summary reports zero failed cases
      - default to `validator/tests` and proof thresholds 5 source, 80 usage, and 85 total cases
      - if generated `safe/out/validator/skip.env` exists, source it only after confirming `validator-report.md` mentions every `VALIDATOR_SKIPPED_CASES` entry, then require `VALIDATOR_TESTS_ROOT/libzstd/` and `VALIDATOR_TESTS_ROOT/tests/libzstd/` to exist, run `"$PYTHON" validator/tools/testcases.py --config "$PWD/validator/repositories.yml" --tests-root "$VALIDATOR_TESTS_ROOT" --library libzstd --check --min-source-cases "$VALIDATOR_MIN_SOURCE_CASES" --min-usage-cases "$VALIDATOR_MIN_USAGE_CASES" --min-cases "$VALIDATOR_MIN_CASES"`, and use `VALIDATOR_TESTS_ROOT`, `VALIDATOR_MIN_SOURCE_CASES`, `VALIDATOR_MIN_USAGE_CASES`, and `VALIDATOR_MIN_CASES` for the filtered final matrix and proof run
      - never delete `safe/out/validator/skip.env` or `safe/out/validator/tests-filtered/` during normal invocation
    - Create `safe/scripts/run-validator-regressions.sh` as the stable gate for local reproductions derived from validator failures. The script must:
      - use `#!/usr/bin/env bash` and `set -euo pipefail`
      - locate and export `REPO_ROOT`, `SAFE_ROOT`, and `VALIDATOR_ARTIFACT_ROOT="$REPO_ROOT/safe/out/validator/artifacts"`
      - exit zero with a clear message when `safe/tests/validator/` does not exist
      - discover executable `*.sh` files directly under `safe/tests/validator/` in lexical order
      - fail if `safe/tests/validator/` exists but no executable `*.sh` tests are present
      - run each discovered script from `REPO_ROOT`
      - pass through any extra command-line arguments to each regression script
    - Create `safe/scripts/check-validator-phase-results.py` for Phase 1-5 and final checkers. The script must verify initial classification, completed work, and final table closure without blocking on failures assigned to current or later phases. The script must:
      - use `#!/usr/bin/env python3`
      - accept `--results-root`, `--report`, repeated `--completed-phase`, and repeated `--allow-remaining-phase`
      - accept zero `--completed-phase` arguments for Phase 1 initial-classification verification; in that mode, the set of table rows with `remediation_status` set to `open` must exactly equal the set of currently failing testcase IDs, and every open row must be assigned to one of the provided `--allow-remaining-phase` values
      - read `VALIDATOR_RUNNER_STATUS` from the environment, defaulting to `0` when absent
      - parse result JSON files under `safe/out/validator/artifacts/port-04-test/results/libzstd/`, excluding `summary.json`
      - parse the strict `validator-report.md` failure table and map each `testcase_id` to exactly one `assigned_remediation_phase`, `remediation_status`, `regression_test`, `fix_commit`, and `notes`
      - fail if the failure table header or separator row differs from the schema required by this plan
      - fail if any table row has an invalid `assigned_remediation_phase`, invalid `remediation_status`, or duplicate `testcase_id`
      - fail if any currently failing testcase ID was not present in the Phase 1 failure table
      - fail when any failed testcase has no assignment
      - fail when any failed testcase is assigned to a completed phase
      - fail when any failed testcase is assigned to a phase outside `--allow-remaining-phase`
      - fail when any row assigned to a completed phase still has `remediation_status` set to `open`
      - allow an `open` row that was originally assigned to a completed phase only after the report reassigns it to an `--allow-remaining-phase` value; when the reassignment is for suspected validator-bug triage, require `notes` to contain `suspected_validator_bug_deferred_to_phase5:<source_phase_id>`
      - fail when a `fixed` row lacks `regression_test` or `fix_commit`
      - fail when a `skipped_validator_bug` row lacks `notes` or mentions no generated skip artifact
      - fail when failed testcases remain but `VALIDATOR_RUNNER_STATUS` is zero, because the strict helper must exit nonzero while validator findings remain
      - fail when no failed testcase remains but `VALIDATOR_RUNNER_STATUS` is nonzero, because proof and helper infrastructure must pass on a clean matrix
      - accept zero `--allow-remaining-phase` arguments for Phase 5 and Phase 6 all-completed verification; in that mode, fail if any current result JSON is failing, if any table row still has `remediation_status` set to `open`, or if any completed row is missing its required regression, fix commit, or generated skip-artifact evidence
      - print passed completed phases and any allowed remaining failed testcase IDs

    ## Implementation Details

    ### Workflow Artifact Contract

    - This plan remains the authoritative validator plan. Existing `.plan/workflow-structure.yaml`, `.plan/phases/*.md`, and `workflow.yaml` may describe prior safe-port phases and must be replaced in place from this validator plan when the final workflow is materialized. Do not load prompts or checks from stale phase files.
    - The validator repository reference discovered during planning is `1319bb0374ef66428a42dd71e49553c6d057feaf` on 2026-04-28; record the actual checked-out commit after clone or fast-forward in `validator-report.md`.

    - Clone or update validator:
    ```bash
    if [ -d validator/.git ]; then
      test -z "$(git -C validator status --porcelain --untracked-files=no)"
      test "$(git -C validator remote get-url origin)" = "https://github.com/safelibs/validator"
      git -C validator fetch origin main
      current_branch=$(git -C validator branch --show-current)
      if [ "$current_branch" != "main" ]; then
        git -C validator switch main || git -C validator switch --track origin/main
      fi
      git -C validator merge --ff-only origin/main
    else
      git clone https://github.com/safelibs/validator validator
    fi
    ```

    - Set up Python:

    ```bash
    if python3 -c 'import yaml' >/dev/null 2>&1; then
      PYTHON=python3
    else
      mkdir -p safe/out/validator
      python3 -m venv safe/out/validator/venv
      safe/out/validator/venv/bin/python -m pip install --upgrade pip PyYAML
      PYTHON="$PWD/safe/out/validator/venv/bin/python"
    fi
    export PYTHON

    # Initial Phase 1 setup only. Do not embed this cleanup in
    # safe/scripts/run-validator-libzstd.sh; later final runs must preserve a
    # Phase 5-generated skip.env and filtered test root.
    rm -f safe/out/validator/skip.env
    rm -rf safe/out/validator/tests-filtered
    rm -rf safe/out/validator/artifacts
    ```

    - Build and stage override packages:

    ```bash
    bash safe/scripts/build-artifacts.sh --release
    bash safe/scripts/build-original-cli-against-safe.sh
    env -u DEB_BUILD_PROFILES bash safe/scripts/build-deb.sh
    metadata="$PWD/safe/out/deb/default/metadata.env"
    package_dir="$PWD/safe/out/deb/default/packages"
    test -f "$metadata"
    (
      set -euo pipefail
      source "$metadata"
      test "${BUILD_TAG:-}" = default
      test -z "${PROFILES:-}"
      test "${PACKAGE_DIR:-}" = "$package_dir"
      test "${INSTALL_ROOT:-}" = "$PWD/safe/out/deb/default/stage-root"
    )
    rm -rf safe/out/validator/override-debs/libzstd
    mkdir -p safe/out/validator/override-debs/libzstd
    shopt -s nullglob
    for pkg in libzstd1 libzstd-dev zstd; do
      matches=("$package_dir"/${pkg}_*.deb)
      if [ "${#matches[@]}" -ne 1 ]; then
        printf 'expected exactly one %s .deb, found %d\n' "$pkg" "${#matches[@]}" >&2
        exit 1
      fi
      cp -a "${matches[0]}" safe/out/validator/override-debs/libzstd/
    done
    shopt -u nullglob
    if find safe/out/validator/override-debs/libzstd -maxdepth 1 -type f -name 'libzstd1-udeb_*' | grep -q .; then
      printf 'validator override leaf must not include libzstd1-udeb\n' >&2
      exit 1
    fi
    ```

    - Generate the local port lock from the staged override packages:

    ```bash
    mkdir -p safe/out/validator/artifacts/proof
    "$PYTHON" - <<'PY'
    from __future__ import annotations

    import hashlib
    import json
    import pathlib
    import subprocess
    from datetime import datetime, timezone

    repo_root = pathlib.Path.cwd()
    override_leaf = repo_root / "safe/out/validator/override-debs/libzstd"
    lock_path = repo_root / "safe/out/validator/artifacts/proof/port-04-test-debs-lock.json"
    packages = ["libzstd1", "libzstd-dev", "zstd"]
    commit = subprocess.check_output(["git", "rev-parse", "HEAD"], text=True).strip()
    if len(commit) != 40 or any(ch not in "0123456789abcdef" for ch in commit):
        raise SystemExit(f"git HEAD must be a 40-character lowercase hex commit, got {commit!r}")

    debs = []
    for package in packages:
        matches = sorted(override_leaf.glob(f"{package}_*.deb"))
        if len(matches) != 1:
            raise SystemExit(f"expected exactly one staged {package} .deb, found {len(matches)}")
        path = matches[0]
        architecture = subprocess.check_output(["dpkg-deb", "-f", str(path), "Architecture"], text=True).strip()
        digest = hashlib.sha256(path.read_bytes()).hexdigest()
        debs.append(
            {
                "package": package,
                "filename": path.name,
                "architecture": architecture,
                "sha256": digest,
                "size": path.stat().st_size,
            }
        )

    lock = {
        "schema_version": 1,
        "mode": "port-04-test",
        "generated_at": datetime.now(timezone.utc).replace(microsecond=0).isoformat().replace("+00:00", "Z"),
        "source_config": "validator/repositories.yml",
        "source_inventory": "local override packages from safe/out/validator/override-debs/libzstd",
        "libraries": [
            {
                "library": "libzstd",
                "repository": "local/port-libzstd",
                "tag_ref": "refs/tags/libzstd/04-test-local",
                "commit": commit,
                "release_tag": f"build-{commit[:12]}",
                "debs": debs,
                "unported_original_packages": [],
            }
        ],
    }
    lock_path.parent.mkdir(parents=True, exist_ok=True)
    lock_path.write_text(json.dumps(lock, indent=2, sort_keys=True) + "\n", encoding="utf-8")
    PY
    ```

    - Run validator tooling:

    ```bash
    PYTHON="$PYTHON" make -C validator unit
    PYTHON="$PYTHON" make -C validator check-testcases
    validator_status=0
    PYTHON="$PYTHON" bash validator/test.sh \
      --config "$PWD/validator/repositories.yml" \
      --tests-root "$PWD/validator/tests" \
      --artifact-root "$PWD/safe/out/validator/artifacts" \
      --mode port-04-test \
      --library libzstd \
      --override-deb-root "$PWD/safe/out/validator/override-debs" \
      --port-deb-lock "$PWD/safe/out/validator/artifacts/proof/port-04-test-debs-lock.json" \
      --record-casts || validator_status=$?
    ```

    - Read the mode-specific summary and convert validator findings into a strict helper failure:

    ```bash
    summary_path="$PWD/safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json"
    summary_failed=1
    if [ -f "$summary_path" ]; then
      summary_failed=$("$PYTHON" -c 'import json,sys; print(int(json.load(open(sys.argv[1]))["failed"]))' "$summary_path")
    fi
    strict_status="$validator_status"
    if [ "$summary_failed" -ne 0 ]; then
      strict_status=1
    fi
    ```

    - If the matrix command succeeds and the summary has zero failures, generate proof:

    ```bash
    if [ "$validator_status" -eq 0 ] && [ "$summary_failed" -eq 0 ]; then
      "$PYTHON" validator/tools/verify_proof_artifacts.py \
        --config "$PWD/validator/repositories.yml" \
        --tests-root "$PWD/validator/tests" \
        --artifact-root "$PWD/safe/out/validator/artifacts" \
        --proof-output "$PWD/safe/out/validator/artifacts/proof/port-04-test-validation-proof.json" \
        --mode port-04-test \
        --library libzstd \
        --min-source-cases 5 \
        --min-usage-cases 80 \
        --min-cases 85 \
        --require-casts
    fi
    ```

    - `safe/scripts/run-validator-libzstd.sh` must exit nonzero if `strict_status` is nonzero or if proof generation fails. Phase 1 evidence collection may invoke the helper under `set +e` so `validator-report.md` can still be written and committed when the initial validator run exposes failures.

    - If the matrix command fails or the summary reports failed cases, parse result JSON files and classify failures into:
      - source CLI/dictionary/multiframe/corruption failures -> Phase 2
      - streaming C API failures -> Phase 3
      - libarchive usage failures -> Phase 4
      - packaging, validator infrastructure, or mixed residual failures -> Phase 5
    - End Phase 1 by committing `.gitignore`, `validator-report.md`, `safe/scripts/run-validator-libzstd.sh`, `safe/scripts/check-validator-phase-results.py`, and `safe/scripts/run-validator-regressions.sh`; the commit must exist even when the initial validator matrix fails.

    ## Verification Phases

    - `check_validator_setup_software_tester` - type: `check`; fixed `bounce_target: impl_validator_setup_initial_run`; purpose: verify that validator setup, package override staging, initial matrix execution, and failure classification are reproducible. Commands to run:
    - `git -C validator rev-parse HEAD`
      - `test -x safe/scripts/run-validator-libzstd.sh`
      - `test -x safe/scripts/check-validator-phase-results.py`
      - `test -x safe/scripts/run-validator-regressions.sh`
      - `rg -n 'env -u DEB_BUILD_PROFILES[[:space:]]+bash[[:space:]]+.*build-deb.sh' safe/scripts/run-validator-libzstd.sh`
      - `bash -lc 'set +e; DEB_BUILD_PROFILES=noudeb bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --allow-remaining-phase impl_validator_source_cli_regressions --allow-remaining-phase impl_validator_streaming_capi_regressions --allow-remaining-phase impl_validator_libarchive_usage_regressions --allow-remaining-phase impl_validator_remaining_burn_down'`
      - `bash -lc 'source safe/out/deb/default/metadata.env; test "${BUILD_TAG:-}" = default; test -z "${PROFILES:-}"; test "${PACKAGE_DIR:-}" = "$PWD/safe/out/deb/default/packages"; test "${INSTALL_ROOT:-}" = "$PWD/safe/out/deb/default/stage-root"'`
      - `test -f validator-report.md`
      - `find safe/out/validator/override-debs/libzstd -maxdepth 1 -type f -name '*.deb' -printf '%f\n' | sort`
      - `test -f safe/out/validator/artifacts/proof/port-04-test-debs-lock.json`
      - `python3 -m json.tool safe/out/validator/artifacts/proof/port-04-test-debs-lock.json >/dev/null`
      - `test -f safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json`
      - `python3 -m json.tool safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json >/dev/null`
      - `rg -n 'Validator Commit|Initial Run|Failure Classification|assigned_remediation_phase|libzstd' validator-report.md`
    - `check_validator_setup_senior_tester` - type: `check`; fixed `bounce_target: impl_validator_setup_initial_run`; purpose: review artifact flow and ensure the validator checkout was not modified or committed. Commands to run:
      - `bash -lc 'base=$(awk "/Phase 1 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'`
      - `git status --short`
      - `git check-ignore -v validator`
      - `git -C validator status --short --branch`
      - `rg -n 'validator/|safe/out/validator|override-debs|proof' .gitignore validator-report.md`
      - `test ! -e .gitmodules || ! rg -n 'validator' .gitmodules`

    ## Verification

    - Run the two check phases above.
    - Confirm `validator-report.md` includes the validator commit and initial run summary.
    - Confirm `validator/` is ignored and not staged for commit.
    - Confirm the phase commit exists with `git log --oneline -1`.

    ## Success Criteria

    - `validator/` exists, is on the fast-forwarded validator `main`, and is ignored by the parent repository.
    - The known validator reference commit `1319bb0374ef66428a42dd71e49553c6d057feaf` is preserved as context and the actual checked-out commit is recorded in `validator-report.md`.
    - The helper scripts are tracked, executable, rerunnable, and use `port-04-test` with the generated local port lock.
    - The override package leaf contains exactly `libzstd1`, `libzstd-dev`, and `zstd` `.deb` files from the verified default package root, never `libzstd1-udeb`.
    - The local port lock and summary JSON are valid and match the staged package artifacts.
    - `validator-report.md` contains the validator commit, initial summary, exact failure table schema, skip list, and assigned remediation phases for every current failure.
    - The generated workflow artifacts replace stale prior safe-port workflow descriptions in place, including `workflow.yaml` when the final workflow is materialized from these phase files.

    ## Git Commit Requirement

    The implementer must commit all Phase 1 work to git before yielding to the verifier phases.
    {% endraw %}

    Verdict Instruction
    End with exactly one verdict line:
    VERDICT: PASS
    VERDICT: FAIL: <reason>
- id: impl_validator_source_cli_regressions
  type: implement
  bounce_target: null
  prompt: |
    Role: Software engineer implementing phase `impl_validator_source_cli_regressions` (Validator Source Case Remediation).

    Workflow goal: {{GOAL}}

    Work from `/home/yans/safelibs/pipeline/ports/port-libzstd`.
    This prompt is self-contained; the agent sees only this prompt.
    Use `.plan/workflow-structure.yaml` as the authoritative source for phase order, phase IDs, phase types, and this phase's fixed `bounce_target` value. Do not invent, rename, or reorder phases.
    Consume or update existing artifacts listed under `Preexisting Inputs` in place. Do not refetch, recollect, rediscover, or regenerate checked-in or already prepared artifacts from scratch when they already satisfy the required contract.
    Preserve unrelated user work.
    Commit all scoped work to git before yielding.

    Topology Contract
    - Phase order: `4`
    - Phase ID: `impl_validator_source_cli_regressions`
    - Phase type: `implement`
    - Fixed top-level `bounce_target`: `null`

    Preexisting Inputs
    - `validator/`
    - `validator-report.md`
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/check-validator-phase-results.py`
    - `safe/scripts/run-validator-regressions.sh`
    - `safe/tests/rust/compress.rs`
    - `safe/tests/rust/decompress.rs`
    - `safe/tests/capi/roundtrip_smoke.c`
    - `safe/tests/capi/decompress_smoke.c`
    - `safe/tests/capi/frame_probe.c`
    - `safe/tests/capi/invalid_dictionaries_driver.c`
    - `safe/tests/capi/bigdict_driver.c`
    - `safe/scripts/run-capi-roundtrip.sh`
    - `safe/scripts/run-capi-decompression.sh`
    - `safe/src/compress/`
    - `safe/src/decompress/`
    - `safe/src/dict_builder/`
    - `safe/src/common/error.rs`
    - `safe/src/common/frame.rs`
    - `safe/src/ffi/compress.rs`
    - `safe/src/ffi/decompress.rs`
    - `safe/out/validator/artifacts/port-04-test/results/libzstd/`
    - `safe/out/validator/artifacts/port-04-test/logs/libzstd/`

    New Outputs
    - Minimal regression tests for each failing source case in existing Rust/C test files or in `safe/tests/validator/`
    - Optional executable `safe/tests/validator/source_cases.sh` that mirrors the failing validator source command with local paths and is run by `safe/scripts/run-validator-regressions.sh`
    - Updated `safe/scripts/run-validator-regressions.sh` only if the Phase 1 helper needs a narrow extension to execute the new validator-local scripts
    - Fixed safe implementation files
    - Refreshed `validator-report.md` Phase 2 section
    - A pre-validator code/test commit when safe code, packaging, or regression tests changed, plus a final Phase 2 report commit

    Phase Source Content
    {% raw %}
    # Phase 2: Source CLI, Dictionary, Multiframe, and Corruption Failures

    ## Phase Name

    Validator Source Case Remediation

    ## Implement Phase ID

    `impl_validator_source_cli_regressions`

    ## Preexisting Inputs

    - Outputs from `impl_validator_setup_initial_run`
    - `safe/tests/rust/compress.rs`
    - `safe/tests/rust/decompress.rs`
    - `safe/tests/capi/roundtrip_smoke.c`
    - `safe/tests/capi/decompress_smoke.c`
    - `safe/tests/capi/frame_probe.c`
    - `safe/tests/capi/invalid_dictionaries_driver.c`
    - `safe/tests/capi/bigdict_driver.c`
    - `safe/scripts/run-capi-roundtrip.sh`
    - `safe/scripts/run-capi-decompression.sh`
    - `safe/src/compress/`
    - `safe/src/decompress/`
    - `safe/src/dict_builder/`
    - `safe/src/common/error.rs`
    - `safe/src/common/frame.rs`
    - `safe/src/ffi/compress.rs`
    - `safe/src/ffi/decompress.rs`
    - `validator-report.md` failure table from Phase 1

    ## New Outputs

    - Minimal regression tests for each failing source case in existing Rust/C test files or in `safe/tests/validator/`
    - Optional executable `safe/tests/validator/source_cases.sh` that mirrors the failing validator source command with local paths and is run by `safe/scripts/run-validator-regressions.sh`
    - Updated `safe/scripts/run-validator-regressions.sh` only if the Phase 1 helper needs a narrow extension to execute the new validator-local scripts
    - Fixed safe implementation files
    - Refreshed `validator-report.md` Phase 2 section
    - A pre-validator code/test commit when safe code, packaging, or regression tests changed, plus a final Phase 2 report commit

    ## File Changes

    - Add tests before fixing each issue:
    - `zstd-compress-decompress`: add a roundtrip/listing regression covering `zstd -q -c`, `zstd -q -dc`, and `zstd -lv` behavior against safe-built packages or local artifacts.
      - `dictionary-train-use`: add a dictionary training and dictionary compression/decompression regression covering `ZDICT_trainFromBuffer`, `ZSTD_compress_usingDict`, and `ZSTD_decompress_usingDict` or a CLI-level mirror if the failure is CLI-only.
      - `multi-frame-behavior`: add a regression for concatenated independent frames and continuous decode through one-shot and/or streaming decode.
      - `corrupted-frame-rejection`: add a malformed-frame regression that asserts failure status and stable `ZSTD_error_prefix_unknown` or the upstream-compatible error class.
    - Fix narrow implementation files based on failure root cause:
      - compression frame output: `safe/src/compress/cctx.rs`, `safe/src/compress/cstream.rs`, `safe/src/compress/frame.rs`
      - dictionary training/use: `safe/src/dict_builder/zdict.rs`, `safe/src/dict_builder/cover.rs`, `safe/src/dict_builder/fastcover.rs`, `safe/src/compress/cdict.rs`, `safe/src/decompress/ddict.rs`
      - multiframe decode and frame metadata: `safe/src/decompress/frame.rs`, `safe/src/common/frame.rs`, `safe/src/decompress/dstream.rs`
      - error code/name parity: `safe/src/common/error.rs`, `safe/src/ffi/types.rs`
    - Update `validator-report.md` with each failing source case, reproducer path, fixed files, and post-fix command results.

    ## Implementation Details

    - Capture `Phase 2 Base Commit: $(git rev-parse HEAD)` before making tracked changes and record that value in the Phase 2 report section.
    - If no Phase 1 failure row is assigned to `impl_validator_source_cli_regressions`, update `validator-report.md` with the exact note `No source-case failures assigned to impl_validator_source_cli_regressions`, record the phase base commit and commands inspected, commit that report-only update, and yield without changing safe code.
    - For true libzstd-safe source-case rows assigned to this phase, add regressions and fixes first, commit the code/test/package changes before running `safe/scripts/run-validator-libzstd.sh`, then update the failure table rows from `open` to `fixed` with `regression_test`, `fix_commit`, and notes before making the final report commit.
    - If an assigned source-case row appears to be a validator bug rather than a libzstd-safe bug, do not create `skip.env` in this phase and do not mark the row `skipped_validator_bug`. Reassign that row to `impl_validator_remaining_burn_down`, keep it `open`, add `suspected_validator_bug_deferred_to_phase5:impl_validator_source_cli_regressions: <short reason>` to `notes`, include the supporting result/log paths in the Phase 2 report section, and make the final report commit so Phase 5 owns the proof or reclassification.
    - Preserve validator commands exactly; do not weaken scripts or edit validator.
    - Do not add broad rewrites. Scope each fix to the function that mismatches upstream behavior.
    - For dictionary failures, preserve raw dictionary and formatted dictionary behavior. Validate edge cases:
      - empty dictionary pointer with zero size
      - non-null pointer with zero size
      - malformed formatted dictionary header
      - trained dictionary ID lookup
      - dictionary-compressed payload decode with matching and missing dictionaries
    - For multiframe failures, preserve concatenated frame semantics in both content-size discovery and actual decode. Avoid treating successful first-frame decode as archive completion when more valid frame bytes remain.
    - For corruption failures, make the C API return an error code that `ZSTD_isError` recognizes and make the CLI return nonzero through the upstream CLI linked against safe `libzstd`.

    ## Verification Phases

    - `check_validator_source_software_tester` - type: `check`; fixed `bounce_target: impl_validator_source_cli_regressions`; purpose: run local regressions and validator source cases after fixes. Commands to run:
    - `cargo test --manifest-path safe/Cargo.toml --release --all-targets`
      - `bash safe/scripts/verify-export-parity.sh`
      - `cargo test --manifest-path safe/Cargo.toml --release --test compress`
      - `cargo test --manifest-path safe/Cargo.toml --release --test decompress`
      - `bash safe/scripts/run-capi-roundtrip.sh`
      - `bash safe/scripts/run-capi-decompression.sh`
      - `bash -lc 'if [ -d safe/tests/validator ]; then test -x safe/scripts/run-validator-regressions.sh; bash safe/scripts/run-validator-regressions.sh; fi'`
      - `test ! -f safe/out/validator/skip.env`
      - `bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --allow-remaining-phase impl_validator_streaming_capi_regressions --allow-remaining-phase impl_validator_libarchive_usage_regressions --allow-remaining-phase impl_validator_remaining_burn_down'`
      - `rg -n 'zstd-compress-decompress|dictionary-train-use|multi-frame-behavior|corrupted-frame-rejection|No source-case failures assigned to impl_validator_source_cli_regressions' validator-report.md`
    - `check_validator_source_senior_tester` - type: `check`; fixed `bounce_target: impl_validator_source_cli_regressions`; purpose: review each source-case failure-to-regression mapping and ensure no validator files were patched. Commands to run:
      - `bash -lc 'base=$(awk "/Phase 2 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'`
      - `git -C validator status --short --branch`
      - `rg -n 'Phase 2|Regression|Fix|No source-case failures' validator-report.md`
      - `rg -n 'SAFE_UPSTREAM_LIB|dlopen|dlsym|load_upstream' safe/src safe/scripts || true`

    ## Verification

    - Run the two check phases above.
    - Rebuild validator override packages before rerunning validator.
    - Confirm all Phase 1 source-case failures now pass, or that no source-case failures existed and the report says so.

    ## Success Criteria

    - Every Phase 1 row assigned to `impl_validator_source_cli_regressions` is fixed with a checked-in regression and `fix_commit`, or explicitly deferred to Phase 5 as a suspected validator bug.
    - If no source-case rows were assigned, `validator-report.md` contains `No source-case failures assigned to impl_validator_source_cli_regressions` and a report-only commit.
    - Source CLI, dictionary, multiframe, and corruption validator cases pass when this phase is marked complete, while only later-phase rows may remain open.
    - Required Rust, C API, validator-regression, package rebuild, and phase-result checker commands have been run and recorded.

    ## Git Commit Requirement

    The implementer must commit all Phase 2 work to git before yielding to the verifier phases.
    {% endraw %}
- id: check_validator_source_software_tester
  type: check
  bounce_target: impl_validator_source_cli_regressions
  prompt: |
    Reviewer Role: Software tester reviewer for Phase 2 source CLI, dictionary, multiframe, and corruption remediations.

    Workflow goal: {{GOAL}}

    Work from `/home/yans/safelibs/pipeline/ports/port-libzstd`.
    This prompt is self-contained; the agent sees only this prompt.
    Use `.plan/workflow-structure.yaml` as the authoritative source for phase order, phase IDs, phase types, and this phase's fixed `bounce_target` value. Do not invent, rename, or reorder phases.
    Review the committed work from the implement phase `impl_validator_source_cli_regressions`. Do not edit implementation files unless the workflow explicitly bounces and you become the implementer for the bounced phase.

    Topology Contract
    - Phase order: `5`
    - Phase ID: `check_validator_source_software_tester`
    - Phase type: `check`
    - Fixed top-level `bounce_target`: `impl_validator_source_cli_regressions`

    Preexisting Inputs
    - `validator/`
    - `validator-report.md`
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/check-validator-phase-results.py`
    - `safe/scripts/run-validator-regressions.sh`
    - `safe/out/validator/artifacts/port-04-test/results/libzstd/`
    - `safe/out/validator/artifacts/proof/port-04-test-debs-lock.json`

    Specific Things To Verify
    - Each Phase 1 source-case row assigned to Phase 2 is fixed or explicitly deferred to Phase 5 as a suspected validator bug.
    - Local Rust, C API, validator-regression, export-parity, and package-backed validator gates run with no Phase 2 failures remaining.
    - No validator skip file exists during this phase.
    - validator-report.md records regressions, fix commits, or the no-assigned-failures note.

    Commands To Run
    Run these commands exactly, unless an earlier command failure makes a later command impossible. Record any failure details in the verdict reason.

    {% raw %}
    ```bash
    cargo test --manifest-path safe/Cargo.toml --release --all-targets
    bash safe/scripts/verify-export-parity.sh
    cargo test --manifest-path safe/Cargo.toml --release --test compress
    cargo test --manifest-path safe/Cargo.toml --release --test decompress
    bash safe/scripts/run-capi-roundtrip.sh
    bash safe/scripts/run-capi-decompression.sh
    bash -lc 'if [ -d safe/tests/validator ]; then test -x safe/scripts/run-validator-regressions.sh; bash safe/scripts/run-validator-regressions.sh; fi'
    test ! -f safe/out/validator/skip.env
    bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --allow-remaining-phase impl_validator_streaming_capi_regressions --allow-remaining-phase impl_validator_libarchive_usage_regressions --allow-remaining-phase impl_validator_remaining_burn_down'
    rg -n 'zstd-compress-decompress|dictionary-train-use|multi-frame-behavior|corrupted-frame-rejection|No source-case failures assigned to impl_validator_source_cli_regressions' validator-report.md
    ```
    {% endraw %}

    Phase Source Content For The Implement Block Being Verified
    {% raw %}
    # Phase 2: Source CLI, Dictionary, Multiframe, and Corruption Failures

    ## Phase Name

    Validator Source Case Remediation

    ## Implement Phase ID

    `impl_validator_source_cli_regressions`

    ## Preexisting Inputs

    - Outputs from `impl_validator_setup_initial_run`
    - `safe/tests/rust/compress.rs`
    - `safe/tests/rust/decompress.rs`
    - `safe/tests/capi/roundtrip_smoke.c`
    - `safe/tests/capi/decompress_smoke.c`
    - `safe/tests/capi/frame_probe.c`
    - `safe/tests/capi/invalid_dictionaries_driver.c`
    - `safe/tests/capi/bigdict_driver.c`
    - `safe/scripts/run-capi-roundtrip.sh`
    - `safe/scripts/run-capi-decompression.sh`
    - `safe/src/compress/`
    - `safe/src/decompress/`
    - `safe/src/dict_builder/`
    - `safe/src/common/error.rs`
    - `safe/src/common/frame.rs`
    - `safe/src/ffi/compress.rs`
    - `safe/src/ffi/decompress.rs`
    - `validator-report.md` failure table from Phase 1

    ## New Outputs

    - Minimal regression tests for each failing source case in existing Rust/C test files or in `safe/tests/validator/`
    - Optional executable `safe/tests/validator/source_cases.sh` that mirrors the failing validator source command with local paths and is run by `safe/scripts/run-validator-regressions.sh`
    - Updated `safe/scripts/run-validator-regressions.sh` only if the Phase 1 helper needs a narrow extension to execute the new validator-local scripts
    - Fixed safe implementation files
    - Refreshed `validator-report.md` Phase 2 section
    - A pre-validator code/test commit when safe code, packaging, or regression tests changed, plus a final Phase 2 report commit

    ## File Changes

    - Add tests before fixing each issue:
    - `zstd-compress-decompress`: add a roundtrip/listing regression covering `zstd -q -c`, `zstd -q -dc`, and `zstd -lv` behavior against safe-built packages or local artifacts.
      - `dictionary-train-use`: add a dictionary training and dictionary compression/decompression regression covering `ZDICT_trainFromBuffer`, `ZSTD_compress_usingDict`, and `ZSTD_decompress_usingDict` or a CLI-level mirror if the failure is CLI-only.
      - `multi-frame-behavior`: add a regression for concatenated independent frames and continuous decode through one-shot and/or streaming decode.
      - `corrupted-frame-rejection`: add a malformed-frame regression that asserts failure status and stable `ZSTD_error_prefix_unknown` or the upstream-compatible error class.
    - Fix narrow implementation files based on failure root cause:
      - compression frame output: `safe/src/compress/cctx.rs`, `safe/src/compress/cstream.rs`, `safe/src/compress/frame.rs`
      - dictionary training/use: `safe/src/dict_builder/zdict.rs`, `safe/src/dict_builder/cover.rs`, `safe/src/dict_builder/fastcover.rs`, `safe/src/compress/cdict.rs`, `safe/src/decompress/ddict.rs`
      - multiframe decode and frame metadata: `safe/src/decompress/frame.rs`, `safe/src/common/frame.rs`, `safe/src/decompress/dstream.rs`
      - error code/name parity: `safe/src/common/error.rs`, `safe/src/ffi/types.rs`
    - Update `validator-report.md` with each failing source case, reproducer path, fixed files, and post-fix command results.

    ## Implementation Details

    - Capture `Phase 2 Base Commit: $(git rev-parse HEAD)` before making tracked changes and record that value in the Phase 2 report section.
    - If no Phase 1 failure row is assigned to `impl_validator_source_cli_regressions`, update `validator-report.md` with the exact note `No source-case failures assigned to impl_validator_source_cli_regressions`, record the phase base commit and commands inspected, commit that report-only update, and yield without changing safe code.
    - For true libzstd-safe source-case rows assigned to this phase, add regressions and fixes first, commit the code/test/package changes before running `safe/scripts/run-validator-libzstd.sh`, then update the failure table rows from `open` to `fixed` with `regression_test`, `fix_commit`, and notes before making the final report commit.
    - If an assigned source-case row appears to be a validator bug rather than a libzstd-safe bug, do not create `skip.env` in this phase and do not mark the row `skipped_validator_bug`. Reassign that row to `impl_validator_remaining_burn_down`, keep it `open`, add `suspected_validator_bug_deferred_to_phase5:impl_validator_source_cli_regressions: <short reason>` to `notes`, include the supporting result/log paths in the Phase 2 report section, and make the final report commit so Phase 5 owns the proof or reclassification.
    - Preserve validator commands exactly; do not weaken scripts or edit validator.
    - Do not add broad rewrites. Scope each fix to the function that mismatches upstream behavior.
    - For dictionary failures, preserve raw dictionary and formatted dictionary behavior. Validate edge cases:
      - empty dictionary pointer with zero size
      - non-null pointer with zero size
      - malformed formatted dictionary header
      - trained dictionary ID lookup
      - dictionary-compressed payload decode with matching and missing dictionaries
    - For multiframe failures, preserve concatenated frame semantics in both content-size discovery and actual decode. Avoid treating successful first-frame decode as archive completion when more valid frame bytes remain.
    - For corruption failures, make the C API return an error code that `ZSTD_isError` recognizes and make the CLI return nonzero through the upstream CLI linked against safe `libzstd`.

    ## Verification Phases

    - `check_validator_source_software_tester` - type: `check`; fixed `bounce_target: impl_validator_source_cli_regressions`; purpose: run local regressions and validator source cases after fixes. Commands to run:
    - `cargo test --manifest-path safe/Cargo.toml --release --all-targets`
      - `bash safe/scripts/verify-export-parity.sh`
      - `cargo test --manifest-path safe/Cargo.toml --release --test compress`
      - `cargo test --manifest-path safe/Cargo.toml --release --test decompress`
      - `bash safe/scripts/run-capi-roundtrip.sh`
      - `bash safe/scripts/run-capi-decompression.sh`
      - `bash -lc 'if [ -d safe/tests/validator ]; then test -x safe/scripts/run-validator-regressions.sh; bash safe/scripts/run-validator-regressions.sh; fi'`
      - `test ! -f safe/out/validator/skip.env`
      - `bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --allow-remaining-phase impl_validator_streaming_capi_regressions --allow-remaining-phase impl_validator_libarchive_usage_regressions --allow-remaining-phase impl_validator_remaining_burn_down'`
      - `rg -n 'zstd-compress-decompress|dictionary-train-use|multi-frame-behavior|corrupted-frame-rejection|No source-case failures assigned to impl_validator_source_cli_regressions' validator-report.md`
    - `check_validator_source_senior_tester` - type: `check`; fixed `bounce_target: impl_validator_source_cli_regressions`; purpose: review each source-case failure-to-regression mapping and ensure no validator files were patched. Commands to run:
      - `bash -lc 'base=$(awk "/Phase 2 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'`
      - `git -C validator status --short --branch`
      - `rg -n 'Phase 2|Regression|Fix|No source-case failures' validator-report.md`
      - `rg -n 'SAFE_UPSTREAM_LIB|dlopen|dlsym|load_upstream' safe/src safe/scripts || true`

    ## Verification

    - Run the two check phases above.
    - Rebuild validator override packages before rerunning validator.
    - Confirm all Phase 1 source-case failures now pass, or that no source-case failures existed and the report says so.

    ## Success Criteria

    - Every Phase 1 row assigned to `impl_validator_source_cli_regressions` is fixed with a checked-in regression and `fix_commit`, or explicitly deferred to Phase 5 as a suspected validator bug.
    - If no source-case rows were assigned, `validator-report.md` contains `No source-case failures assigned to impl_validator_source_cli_regressions` and a report-only commit.
    - Source CLI, dictionary, multiframe, and corruption validator cases pass when this phase is marked complete, while only later-phase rows may remain open.
    - Required Rust, C API, validator-regression, package rebuild, and phase-result checker commands have been run and recorded.

    ## Git Commit Requirement

    The implementer must commit all Phase 2 work to git before yielding to the verifier phases.
    {% endraw %}

    Verdict Instruction
    End with exactly one verdict line:
    VERDICT: PASS
    VERDICT: FAIL: <reason>
- id: check_validator_source_senior_tester
  type: check
  bounce_target: impl_validator_source_cli_regressions
  prompt: |
    Reviewer Role: Senior tester reviewer for Phase 2 source-case failure-to-regression mapping.

    Workflow goal: {{GOAL}}

    Work from `/home/yans/safelibs/pipeline/ports/port-libzstd`.
    This prompt is self-contained; the agent sees only this prompt.
    Use `.plan/workflow-structure.yaml` as the authoritative source for phase order, phase IDs, phase types, and this phase's fixed `bounce_target` value. Do not invent, rename, or reorder phases.
    Review the committed work from the implement phase `impl_validator_source_cli_regressions`. Do not edit implementation files unless the workflow explicitly bounces and you become the implementer for the bounced phase.

    Topology Contract
    - Phase order: `6`
    - Phase ID: `check_validator_source_senior_tester`
    - Phase type: `check`
    - Fixed top-level `bounce_target`: `impl_validator_source_cli_regressions`

    Preexisting Inputs
    - `validator/`
    - `validator-report.md`
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/check-validator-phase-results.py`
    - `safe/out/validator/artifacts/port-04-test/results/libzstd/`

    Specific Things To Verify
    - The Phase 2 diff is whitespace-clean from the recorded base commit.
    - Each source-case failure has a clear regression, scoped fix, report update, and fix commit unless deferred to Phase 5 with required suspected-validator-bug notes.
    - The validator checkout was not patched.
    - The safe implementation does not evade failures by loading upstream libzstd dynamically.

    Commands To Run
    Run these commands exactly, unless an earlier command failure makes a later command impossible. Record any failure details in the verdict reason.

    {% raw %}
    ```bash
    bash -lc 'base=$(awk "/Phase 2 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'
    git -C validator status --short --branch
    rg -n 'Phase 2|Regression|Fix|No source-case failures' validator-report.md
    rg -n 'SAFE_UPSTREAM_LIB|dlopen|dlsym|load_upstream' safe/src safe/scripts || true
    ```
    {% endraw %}

    Phase Source Content For The Implement Block Being Verified
    {% raw %}
    # Phase 2: Source CLI, Dictionary, Multiframe, and Corruption Failures

    ## Phase Name

    Validator Source Case Remediation

    ## Implement Phase ID

    `impl_validator_source_cli_regressions`

    ## Preexisting Inputs

    - Outputs from `impl_validator_setup_initial_run`
    - `safe/tests/rust/compress.rs`
    - `safe/tests/rust/decompress.rs`
    - `safe/tests/capi/roundtrip_smoke.c`
    - `safe/tests/capi/decompress_smoke.c`
    - `safe/tests/capi/frame_probe.c`
    - `safe/tests/capi/invalid_dictionaries_driver.c`
    - `safe/tests/capi/bigdict_driver.c`
    - `safe/scripts/run-capi-roundtrip.sh`
    - `safe/scripts/run-capi-decompression.sh`
    - `safe/src/compress/`
    - `safe/src/decompress/`
    - `safe/src/dict_builder/`
    - `safe/src/common/error.rs`
    - `safe/src/common/frame.rs`
    - `safe/src/ffi/compress.rs`
    - `safe/src/ffi/decompress.rs`
    - `validator-report.md` failure table from Phase 1

    ## New Outputs

    - Minimal regression tests for each failing source case in existing Rust/C test files or in `safe/tests/validator/`
    - Optional executable `safe/tests/validator/source_cases.sh` that mirrors the failing validator source command with local paths and is run by `safe/scripts/run-validator-regressions.sh`
    - Updated `safe/scripts/run-validator-regressions.sh` only if the Phase 1 helper needs a narrow extension to execute the new validator-local scripts
    - Fixed safe implementation files
    - Refreshed `validator-report.md` Phase 2 section
    - A pre-validator code/test commit when safe code, packaging, or regression tests changed, plus a final Phase 2 report commit

    ## File Changes

    - Add tests before fixing each issue:
    - `zstd-compress-decompress`: add a roundtrip/listing regression covering `zstd -q -c`, `zstd -q -dc`, and `zstd -lv` behavior against safe-built packages or local artifacts.
      - `dictionary-train-use`: add a dictionary training and dictionary compression/decompression regression covering `ZDICT_trainFromBuffer`, `ZSTD_compress_usingDict`, and `ZSTD_decompress_usingDict` or a CLI-level mirror if the failure is CLI-only.
      - `multi-frame-behavior`: add a regression for concatenated independent frames and continuous decode through one-shot and/or streaming decode.
      - `corrupted-frame-rejection`: add a malformed-frame regression that asserts failure status and stable `ZSTD_error_prefix_unknown` or the upstream-compatible error class.
    - Fix narrow implementation files based on failure root cause:
      - compression frame output: `safe/src/compress/cctx.rs`, `safe/src/compress/cstream.rs`, `safe/src/compress/frame.rs`
      - dictionary training/use: `safe/src/dict_builder/zdict.rs`, `safe/src/dict_builder/cover.rs`, `safe/src/dict_builder/fastcover.rs`, `safe/src/compress/cdict.rs`, `safe/src/decompress/ddict.rs`
      - multiframe decode and frame metadata: `safe/src/decompress/frame.rs`, `safe/src/common/frame.rs`, `safe/src/decompress/dstream.rs`
      - error code/name parity: `safe/src/common/error.rs`, `safe/src/ffi/types.rs`
    - Update `validator-report.md` with each failing source case, reproducer path, fixed files, and post-fix command results.

    ## Implementation Details

    - Capture `Phase 2 Base Commit: $(git rev-parse HEAD)` before making tracked changes and record that value in the Phase 2 report section.
    - If no Phase 1 failure row is assigned to `impl_validator_source_cli_regressions`, update `validator-report.md` with the exact note `No source-case failures assigned to impl_validator_source_cli_regressions`, record the phase base commit and commands inspected, commit that report-only update, and yield without changing safe code.
    - For true libzstd-safe source-case rows assigned to this phase, add regressions and fixes first, commit the code/test/package changes before running `safe/scripts/run-validator-libzstd.sh`, then update the failure table rows from `open` to `fixed` with `regression_test`, `fix_commit`, and notes before making the final report commit.
    - If an assigned source-case row appears to be a validator bug rather than a libzstd-safe bug, do not create `skip.env` in this phase and do not mark the row `skipped_validator_bug`. Reassign that row to `impl_validator_remaining_burn_down`, keep it `open`, add `suspected_validator_bug_deferred_to_phase5:impl_validator_source_cli_regressions: <short reason>` to `notes`, include the supporting result/log paths in the Phase 2 report section, and make the final report commit so Phase 5 owns the proof or reclassification.
    - Preserve validator commands exactly; do not weaken scripts or edit validator.
    - Do not add broad rewrites. Scope each fix to the function that mismatches upstream behavior.
    - For dictionary failures, preserve raw dictionary and formatted dictionary behavior. Validate edge cases:
      - empty dictionary pointer with zero size
      - non-null pointer with zero size
      - malformed formatted dictionary header
      - trained dictionary ID lookup
      - dictionary-compressed payload decode with matching and missing dictionaries
    - For multiframe failures, preserve concatenated frame semantics in both content-size discovery and actual decode. Avoid treating successful first-frame decode as archive completion when more valid frame bytes remain.
    - For corruption failures, make the C API return an error code that `ZSTD_isError` recognizes and make the CLI return nonzero through the upstream CLI linked against safe `libzstd`.

    ## Verification Phases

    - `check_validator_source_software_tester` - type: `check`; fixed `bounce_target: impl_validator_source_cli_regressions`; purpose: run local regressions and validator source cases after fixes. Commands to run:
    - `cargo test --manifest-path safe/Cargo.toml --release --all-targets`
      - `bash safe/scripts/verify-export-parity.sh`
      - `cargo test --manifest-path safe/Cargo.toml --release --test compress`
      - `cargo test --manifest-path safe/Cargo.toml --release --test decompress`
      - `bash safe/scripts/run-capi-roundtrip.sh`
      - `bash safe/scripts/run-capi-decompression.sh`
      - `bash -lc 'if [ -d safe/tests/validator ]; then test -x safe/scripts/run-validator-regressions.sh; bash safe/scripts/run-validator-regressions.sh; fi'`
      - `test ! -f safe/out/validator/skip.env`
      - `bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --allow-remaining-phase impl_validator_streaming_capi_regressions --allow-remaining-phase impl_validator_libarchive_usage_regressions --allow-remaining-phase impl_validator_remaining_burn_down'`
      - `rg -n 'zstd-compress-decompress|dictionary-train-use|multi-frame-behavior|corrupted-frame-rejection|No source-case failures assigned to impl_validator_source_cli_regressions' validator-report.md`
    - `check_validator_source_senior_tester` - type: `check`; fixed `bounce_target: impl_validator_source_cli_regressions`; purpose: review each source-case failure-to-regression mapping and ensure no validator files were patched. Commands to run:
      - `bash -lc 'base=$(awk "/Phase 2 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'`
      - `git -C validator status --short --branch`
      - `rg -n 'Phase 2|Regression|Fix|No source-case failures' validator-report.md`
      - `rg -n 'SAFE_UPSTREAM_LIB|dlopen|dlsym|load_upstream' safe/src safe/scripts || true`

    ## Verification

    - Run the two check phases above.
    - Rebuild validator override packages before rerunning validator.
    - Confirm all Phase 1 source-case failures now pass, or that no source-case failures existed and the report says so.

    ## Success Criteria

    - Every Phase 1 row assigned to `impl_validator_source_cli_regressions` is fixed with a checked-in regression and `fix_commit`, or explicitly deferred to Phase 5 as a suspected validator bug.
    - If no source-case rows were assigned, `validator-report.md` contains `No source-case failures assigned to impl_validator_source_cli_regressions` and a report-only commit.
    - Source CLI, dictionary, multiframe, and corruption validator cases pass when this phase is marked complete, while only later-phase rows may remain open.
    - Required Rust, C API, validator-regression, package rebuild, and phase-result checker commands have been run and recorded.

    ## Git Commit Requirement

    The implementer must commit all Phase 2 work to git before yielding to the verifier phases.
    {% endraw %}

    Verdict Instruction
    End with exactly one verdict line:
    VERDICT: PASS
    VERDICT: FAIL: <reason>
- id: impl_validator_streaming_capi_regressions
  type: implement
  bounce_target: null
  prompt: |
    Role: Software engineer implementing phase `impl_validator_streaming_capi_regressions` (Validator Streaming C API Remediation).

    Workflow goal: {{GOAL}}

    Work from `/home/yans/safelibs/pipeline/ports/port-libzstd`.
    This prompt is self-contained; the agent sees only this prompt.
    Use `.plan/workflow-structure.yaml` as the authoritative source for phase order, phase IDs, phase types, and this phase's fixed `bounce_target` value. Do not invent, rename, or reorder phases.
    Consume or update existing artifacts listed under `Preexisting Inputs` in place. Do not refetch, recollect, rediscover, or regenerate checked-in or already prepared artifacts from scratch when they already satisfy the required contract.
    Preserve unrelated user work.
    Commit all scoped work to git before yielding.

    Topology Contract
    - Phase order: `7`
    - Phase ID: `impl_validator_streaming_capi_regressions`
    - Phase type: `implement`
    - Fixed top-level `bounce_target`: `null`

    Preexisting Inputs
    - `validator/`
    - `validator-report.md`
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/check-validator-phase-results.py`
    - `safe/scripts/run-validator-regressions.sh`
    - `safe/src/compress/cstream.rs`
    - `safe/src/compress/cctx.rs`
    - `safe/src/compress/cctx_params.rs`
    - `safe/src/compress/params.rs`
    - `safe/src/ffi/compress.rs`
    - `safe/src/decompress/dstream.rs`
    - `safe/src/decompress/dctx.rs`
    - `safe/src/ffi/decompress.rs`
    - `safe/src/ffi/types.rs`
    - `safe/tests/capi/zstream_driver.c`
    - `safe/tests/capi/roundtrip_smoke.c`
    - `safe/tests/capi/paramgrill_driver.c`
    - `safe/scripts/run-capi-roundtrip.sh`
    - `safe/out/validator/artifacts/port-04-test/results/libzstd/`
    - `safe/out/validator/artifacts/port-04-test/logs/libzstd/`

    New Outputs
    - Minimal C regression reproducer for the validator smoke program in `safe/tests/capi/zstream_driver.c` or a new `safe/tests/capi/validator_streaming_smoke.c`
    - Updated C API runner script if a new C file is added
    - Fixed C streaming implementation
    - Refreshed `validator-report.md` Phase 3 section
    - A pre-validator code/test commit when safe code, packaging, or regression tests changed, plus a final Phase 3 report commit

    Phase Source Content
    {% raw %}
    # Phase 3: Streaming C API Validator Failures

    ## Phase Name

    Validator Streaming C API Remediation

    ## Implement Phase ID

    `impl_validator_streaming_capi_regressions`

    ## Preexisting Inputs

    - Outputs from Phases 1 and 2
    - `safe/src/compress/cstream.rs`
    - `safe/src/compress/cctx.rs`
    - `safe/src/compress/cctx_params.rs`
    - `safe/src/compress/params.rs`
    - `safe/src/ffi/compress.rs`
    - `safe/src/decompress/dstream.rs`
    - `safe/src/decompress/dctx.rs`
    - `safe/src/ffi/decompress.rs`
    - `safe/src/ffi/types.rs`
    - `safe/tests/capi/zstream_driver.c`
    - `safe/tests/capi/roundtrip_smoke.c`
    - `safe/tests/capi/paramgrill_driver.c`
    - `safe/scripts/run-capi-roundtrip.sh`
    - Validator failing result/log for `streaming-c-api-smoke`, if any

    ## New Outputs

    - Minimal C regression reproducer for the validator smoke program in `safe/tests/capi/zstream_driver.c` or a new `safe/tests/capi/validator_streaming_smoke.c`
    - Updated C API runner script if a new C file is added
    - Fixed C streaming implementation
    - Refreshed `validator-report.md` Phase 3 section
    - A pre-validator code/test commit when safe code, packaging, or regression tests changed, plus a final Phase 3 report commit

    ## File Changes

    - Add a C regression that matches the validator smoke shape:
    - create `ZSTD_CCtx`
      - initialize `ZSTD_inBuffer` and `ZSTD_outBuffer`
      - call `ZSTD_compressStream2(..., ZSTD_e_end)`
      - free the compression context
      - create `ZSTD_DCtx`
      - call `ZSTD_decompressStream`
      - assert decoded bytes and buffer positions
    - Fix `safe/src/ffi/compress.rs` and `safe/src/compress/cstream.rs` for:
      - final-frame production on `ZSTD_e_end`
      - correct `input.pos` and `output.pos` updates
      - return value semantics for complete vs pending output
      - destination-too-small progress handling
    - Fix `safe/src/ffi/decompress.rs` and `safe/src/decompress/dstream.rs` for:
      - one-call full-frame decode
      - empty-output and partial-output behavior
      - `ZSTD_inBuffer` / `ZSTD_outBuffer` pointer validation
      - no-forward-progress error semantics

    ## Implementation Details

    - Capture `Phase 3 Base Commit: $(git rev-parse HEAD)` before making tracked changes and record that value in the Phase 3 report section.
    - If no Phase 1 failure row is assigned to `impl_validator_streaming_capi_regressions`, update `validator-report.md` with the exact note `No streaming C API failures assigned to impl_validator_streaming_capi_regressions`, record the phase base commit and commands inspected, commit that report-only update, and yield without changing safe code.
    - For true libzstd-safe streaming C API rows assigned to this phase, add regressions and fixes first, commit the code/test/package changes before running `safe/scripts/run-validator-libzstd.sh`, then update the failure table rows from `open` to `fixed` with `regression_test`, `fix_commit`, and notes before making the final report commit.
    - If an assigned streaming C API row appears to be a validator bug rather than a libzstd-safe bug, do not create `skip.env` in this phase and do not mark the row `skipped_validator_bug`. Reassign that row to `impl_validator_remaining_burn_down`, keep it `open`, add `suspected_validator_bug_deferred_to_phase5:impl_validator_streaming_capi_regressions: <short reason>` to `notes`, include the supporting result/log paths in the Phase 3 report section, and make the final report commit so Phase 5 owns the proof or reclassification.
    - Maintain exact C layout for `ZSTD_inBuffer`, `ZSTD_outBuffer`, and `ZSTD_EndDirective` in `safe/src/ffi/types.rs`.
    - Preserve allocation/free behavior for null contexts and null buffers.
    - For partial buffers, loop only while forward progress is possible; return the expected remaining hint rather than spinning.
    - If the validator failure comes from compile/link behavior rather than runtime behavior, inspect installed headers and pkg-config metadata:
      - `safe/include/zstd.h`
      - `safe/pkgconfig/libzstd.pc.in`
      - `safe/cmake/`
      - `safe/scripts/build-artifacts.sh`

    ## Verification Phases

    - `check_validator_capi_software_tester` - type: `check`; fixed `bounce_target: impl_validator_streaming_capi_regressions`; purpose: verify the exact streaming C API smoke behavior and local C ABI regressions. Commands to run:
    - `cargo test --manifest-path safe/Cargo.toml --release --all-targets`
      - `bash safe/scripts/run-capi-roundtrip.sh`
      - `bash safe/scripts/run-capi-decompression.sh`
      - `cargo test --manifest-path safe/Cargo.toml --release --test compress`
      - `cargo test --manifest-path safe/Cargo.toml --release --test decompress`
      - `bash -lc 'if [ -d safe/tests/validator ]; then test -x safe/scripts/run-validator-regressions.sh; bash safe/scripts/run-validator-regressions.sh; fi'`
      - `test ! -f safe/out/validator/skip.env`
      - `bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --allow-remaining-phase impl_validator_libarchive_usage_regressions --allow-remaining-phase impl_validator_remaining_burn_down'`
      - `rg -n 'streaming-c-api-smoke|No streaming C API failures assigned to impl_validator_streaming_capi_regressions' validator-report.md`
    - `check_validator_capi_senior_tester` - type: `check`; fixed `bounce_target: impl_validator_streaming_capi_regressions`; purpose: review buffer position semantics, ownership/lifetime handling, and C ABI compatibility. Commands to run:
      - `bash -lc 'base=$(awk "/Phase 3 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'`
      - `bash safe/scripts/verify-export-parity.sh`
      - `bash safe/scripts/verify-link-compat.sh`
      - `rg -n 'streaming-c-api-smoke|No streaming C API failures assigned|ZSTD_compressStream2|ZSTD_decompressStream' validator-report.md safe/tests safe/src`

    ## Verification

    - Run the two check phases above.
    - Rebuild `.deb` packages and rerun the validator matrix.
    - Confirm `streaming-c-api-smoke` passes or the report states there was no Phase 3 failure.

    ## Success Criteria

    - Every Phase 1 row assigned to `impl_validator_streaming_capi_regressions` is fixed with a checked-in C/Rust regression and `fix_commit`, or explicitly deferred to Phase 5 as a suspected validator bug.
    - If no streaming C API rows were assigned, `validator-report.md` contains `No streaming C API failures assigned to impl_validator_streaming_capi_regressions` and a report-only commit.
    - `streaming-c-api-smoke` passes when this phase is marked complete, while only later-phase rows may remain open.
    - C buffer position, return-hint, pointer validation, and ABI layout behavior remain compatible with upstream and are covered by the listed commands.

    ## Git Commit Requirement

    The implementer must commit all Phase 3 work to git before yielding to the verifier phases.
    {% endraw %}
- id: check_validator_capi_software_tester
  type: check
  bounce_target: impl_validator_streaming_capi_regressions
  prompt: |
    Reviewer Role: Software tester reviewer for Phase 3 streaming C API remediation.

    Workflow goal: {{GOAL}}

    Work from `/home/yans/safelibs/pipeline/ports/port-libzstd`.
    This prompt is self-contained; the agent sees only this prompt.
    Use `.plan/workflow-structure.yaml` as the authoritative source for phase order, phase IDs, phase types, and this phase's fixed `bounce_target` value. Do not invent, rename, or reorder phases.
    Review the committed work from the implement phase `impl_validator_streaming_capi_regressions`. Do not edit implementation files unless the workflow explicitly bounces and you become the implementer for the bounced phase.

    Topology Contract
    - Phase order: `8`
    - Phase ID: `check_validator_capi_software_tester`
    - Phase type: `check`
    - Fixed top-level `bounce_target`: `impl_validator_streaming_capi_regressions`

    Preexisting Inputs
    - `validator/`
    - `validator-report.md`
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/check-validator-phase-results.py`
    - `safe/scripts/run-validator-regressions.sh`
    - `safe/out/validator/artifacts/port-04-test/results/libzstd/`
    - `safe/out/validator/artifacts/proof/port-04-test-debs-lock.json`

    Specific Things To Verify
    - The streaming C API smoke behavior is reproduced locally and fixed or explicitly absent.
    - Rust, C API, validator-regression, and package-backed validator gates run with Phase 2 and Phase 3 marked completed.
    - No validator skip file exists during this phase.
    - validator-report.md records the streaming C API result, regression, and fix commit or the no-assigned-failures note.

    Commands To Run
    Run these commands exactly, unless an earlier command failure makes a later command impossible. Record any failure details in the verdict reason.

    {% raw %}
    ```bash
    cargo test --manifest-path safe/Cargo.toml --release --all-targets
    bash safe/scripts/run-capi-roundtrip.sh
    bash safe/scripts/run-capi-decompression.sh
    cargo test --manifest-path safe/Cargo.toml --release --test compress
    cargo test --manifest-path safe/Cargo.toml --release --test decompress
    bash -lc 'if [ -d safe/tests/validator ]; then test -x safe/scripts/run-validator-regressions.sh; bash safe/scripts/run-validator-regressions.sh; fi'
    test ! -f safe/out/validator/skip.env
    bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --allow-remaining-phase impl_validator_libarchive_usage_regressions --allow-remaining-phase impl_validator_remaining_burn_down'
    rg -n 'streaming-c-api-smoke|No streaming C API failures assigned to impl_validator_streaming_capi_regressions' validator-report.md
    ```
    {% endraw %}

    Phase Source Content For The Implement Block Being Verified
    {% raw %}
    # Phase 3: Streaming C API Validator Failures

    ## Phase Name

    Validator Streaming C API Remediation

    ## Implement Phase ID

    `impl_validator_streaming_capi_regressions`

    ## Preexisting Inputs

    - Outputs from Phases 1 and 2
    - `safe/src/compress/cstream.rs`
    - `safe/src/compress/cctx.rs`
    - `safe/src/compress/cctx_params.rs`
    - `safe/src/compress/params.rs`
    - `safe/src/ffi/compress.rs`
    - `safe/src/decompress/dstream.rs`
    - `safe/src/decompress/dctx.rs`
    - `safe/src/ffi/decompress.rs`
    - `safe/src/ffi/types.rs`
    - `safe/tests/capi/zstream_driver.c`
    - `safe/tests/capi/roundtrip_smoke.c`
    - `safe/tests/capi/paramgrill_driver.c`
    - `safe/scripts/run-capi-roundtrip.sh`
    - Validator failing result/log for `streaming-c-api-smoke`, if any

    ## New Outputs

    - Minimal C regression reproducer for the validator smoke program in `safe/tests/capi/zstream_driver.c` or a new `safe/tests/capi/validator_streaming_smoke.c`
    - Updated C API runner script if a new C file is added
    - Fixed C streaming implementation
    - Refreshed `validator-report.md` Phase 3 section
    - A pre-validator code/test commit when safe code, packaging, or regression tests changed, plus a final Phase 3 report commit

    ## File Changes

    - Add a C regression that matches the validator smoke shape:
    - create `ZSTD_CCtx`
      - initialize `ZSTD_inBuffer` and `ZSTD_outBuffer`
      - call `ZSTD_compressStream2(..., ZSTD_e_end)`
      - free the compression context
      - create `ZSTD_DCtx`
      - call `ZSTD_decompressStream`
      - assert decoded bytes and buffer positions
    - Fix `safe/src/ffi/compress.rs` and `safe/src/compress/cstream.rs` for:
      - final-frame production on `ZSTD_e_end`
      - correct `input.pos` and `output.pos` updates
      - return value semantics for complete vs pending output
      - destination-too-small progress handling
    - Fix `safe/src/ffi/decompress.rs` and `safe/src/decompress/dstream.rs` for:
      - one-call full-frame decode
      - empty-output and partial-output behavior
      - `ZSTD_inBuffer` / `ZSTD_outBuffer` pointer validation
      - no-forward-progress error semantics

    ## Implementation Details

    - Capture `Phase 3 Base Commit: $(git rev-parse HEAD)` before making tracked changes and record that value in the Phase 3 report section.
    - If no Phase 1 failure row is assigned to `impl_validator_streaming_capi_regressions`, update `validator-report.md` with the exact note `No streaming C API failures assigned to impl_validator_streaming_capi_regressions`, record the phase base commit and commands inspected, commit that report-only update, and yield without changing safe code.
    - For true libzstd-safe streaming C API rows assigned to this phase, add regressions and fixes first, commit the code/test/package changes before running `safe/scripts/run-validator-libzstd.sh`, then update the failure table rows from `open` to `fixed` with `regression_test`, `fix_commit`, and notes before making the final report commit.
    - If an assigned streaming C API row appears to be a validator bug rather than a libzstd-safe bug, do not create `skip.env` in this phase and do not mark the row `skipped_validator_bug`. Reassign that row to `impl_validator_remaining_burn_down`, keep it `open`, add `suspected_validator_bug_deferred_to_phase5:impl_validator_streaming_capi_regressions: <short reason>` to `notes`, include the supporting result/log paths in the Phase 3 report section, and make the final report commit so Phase 5 owns the proof or reclassification.
    - Maintain exact C layout for `ZSTD_inBuffer`, `ZSTD_outBuffer`, and `ZSTD_EndDirective` in `safe/src/ffi/types.rs`.
    - Preserve allocation/free behavior for null contexts and null buffers.
    - For partial buffers, loop only while forward progress is possible; return the expected remaining hint rather than spinning.
    - If the validator failure comes from compile/link behavior rather than runtime behavior, inspect installed headers and pkg-config metadata:
      - `safe/include/zstd.h`
      - `safe/pkgconfig/libzstd.pc.in`
      - `safe/cmake/`
      - `safe/scripts/build-artifacts.sh`

    ## Verification Phases

    - `check_validator_capi_software_tester` - type: `check`; fixed `bounce_target: impl_validator_streaming_capi_regressions`; purpose: verify the exact streaming C API smoke behavior and local C ABI regressions. Commands to run:
    - `cargo test --manifest-path safe/Cargo.toml --release --all-targets`
      - `bash safe/scripts/run-capi-roundtrip.sh`
      - `bash safe/scripts/run-capi-decompression.sh`
      - `cargo test --manifest-path safe/Cargo.toml --release --test compress`
      - `cargo test --manifest-path safe/Cargo.toml --release --test decompress`
      - `bash -lc 'if [ -d safe/tests/validator ]; then test -x safe/scripts/run-validator-regressions.sh; bash safe/scripts/run-validator-regressions.sh; fi'`
      - `test ! -f safe/out/validator/skip.env`
      - `bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --allow-remaining-phase impl_validator_libarchive_usage_regressions --allow-remaining-phase impl_validator_remaining_burn_down'`
      - `rg -n 'streaming-c-api-smoke|No streaming C API failures assigned to impl_validator_streaming_capi_regressions' validator-report.md`
    - `check_validator_capi_senior_tester` - type: `check`; fixed `bounce_target: impl_validator_streaming_capi_regressions`; purpose: review buffer position semantics, ownership/lifetime handling, and C ABI compatibility. Commands to run:
      - `bash -lc 'base=$(awk "/Phase 3 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'`
      - `bash safe/scripts/verify-export-parity.sh`
      - `bash safe/scripts/verify-link-compat.sh`
      - `rg -n 'streaming-c-api-smoke|No streaming C API failures assigned|ZSTD_compressStream2|ZSTD_decompressStream' validator-report.md safe/tests safe/src`

    ## Verification

    - Run the two check phases above.
    - Rebuild `.deb` packages and rerun the validator matrix.
    - Confirm `streaming-c-api-smoke` passes or the report states there was no Phase 3 failure.

    ## Success Criteria

    - Every Phase 1 row assigned to `impl_validator_streaming_capi_regressions` is fixed with a checked-in C/Rust regression and `fix_commit`, or explicitly deferred to Phase 5 as a suspected validator bug.
    - If no streaming C API rows were assigned, `validator-report.md` contains `No streaming C API failures assigned to impl_validator_streaming_capi_regressions` and a report-only commit.
    - `streaming-c-api-smoke` passes when this phase is marked complete, while only later-phase rows may remain open.
    - C buffer position, return-hint, pointer validation, and ABI layout behavior remain compatible with upstream and are covered by the listed commands.

    ## Git Commit Requirement

    The implementer must commit all Phase 3 work to git before yielding to the verifier phases.
    {% endraw %}

    Verdict Instruction
    End with exactly one verdict line:
    VERDICT: PASS
    VERDICT: FAIL: <reason>
- id: check_validator_capi_senior_tester
  type: check
  bounce_target: impl_validator_streaming_capi_regressions
  prompt: |
    Reviewer Role: Senior tester reviewer for Phase 3 buffer semantics, ownership, lifetime, and ABI compatibility.

    Workflow goal: {{GOAL}}

    Work from `/home/yans/safelibs/pipeline/ports/port-libzstd`.
    This prompt is self-contained; the agent sees only this prompt.
    Use `.plan/workflow-structure.yaml` as the authoritative source for phase order, phase IDs, phase types, and this phase's fixed `bounce_target` value. Do not invent, rename, or reorder phases.
    Review the committed work from the implement phase `impl_validator_streaming_capi_regressions`. Do not edit implementation files unless the workflow explicitly bounces and you become the implementer for the bounced phase.

    Topology Contract
    - Phase order: `9`
    - Phase ID: `check_validator_capi_senior_tester`
    - Phase type: `check`
    - Fixed top-level `bounce_target`: `impl_validator_streaming_capi_regressions`

    Preexisting Inputs
    - `validator/`
    - `validator-report.md`
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/check-validator-phase-results.py`
    - `safe/out/validator/artifacts/port-04-test/results/libzstd/`

    Specific Things To Verify
    - The Phase 3 diff is whitespace-clean from the recorded base commit.
    - Streaming compression and decompression update input/output positions and return hints compatibly with upstream.
    - Pointer validation, no-forward-progress behavior, allocation/free behavior, export parity, and link compatibility hold.
    - validator-report.md names the streaming C API evidence or the no-assigned-failures note.

    Commands To Run
    Run these commands exactly, unless an earlier command failure makes a later command impossible. Record any failure details in the verdict reason.

    {% raw %}
    ```bash
    bash -lc 'base=$(awk "/Phase 3 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'
    bash safe/scripts/verify-export-parity.sh
    bash safe/scripts/verify-link-compat.sh
    rg -n 'streaming-c-api-smoke|No streaming C API failures assigned|ZSTD_compressStream2|ZSTD_decompressStream' validator-report.md safe/tests safe/src
    ```
    {% endraw %}

    Phase Source Content For The Implement Block Being Verified
    {% raw %}
    # Phase 3: Streaming C API Validator Failures

    ## Phase Name

    Validator Streaming C API Remediation

    ## Implement Phase ID

    `impl_validator_streaming_capi_regressions`

    ## Preexisting Inputs

    - Outputs from Phases 1 and 2
    - `safe/src/compress/cstream.rs`
    - `safe/src/compress/cctx.rs`
    - `safe/src/compress/cctx_params.rs`
    - `safe/src/compress/params.rs`
    - `safe/src/ffi/compress.rs`
    - `safe/src/decompress/dstream.rs`
    - `safe/src/decompress/dctx.rs`
    - `safe/src/ffi/decompress.rs`
    - `safe/src/ffi/types.rs`
    - `safe/tests/capi/zstream_driver.c`
    - `safe/tests/capi/roundtrip_smoke.c`
    - `safe/tests/capi/paramgrill_driver.c`
    - `safe/scripts/run-capi-roundtrip.sh`
    - Validator failing result/log for `streaming-c-api-smoke`, if any

    ## New Outputs

    - Minimal C regression reproducer for the validator smoke program in `safe/tests/capi/zstream_driver.c` or a new `safe/tests/capi/validator_streaming_smoke.c`
    - Updated C API runner script if a new C file is added
    - Fixed C streaming implementation
    - Refreshed `validator-report.md` Phase 3 section
    - A pre-validator code/test commit when safe code, packaging, or regression tests changed, plus a final Phase 3 report commit

    ## File Changes

    - Add a C regression that matches the validator smoke shape:
    - create `ZSTD_CCtx`
      - initialize `ZSTD_inBuffer` and `ZSTD_outBuffer`
      - call `ZSTD_compressStream2(..., ZSTD_e_end)`
      - free the compression context
      - create `ZSTD_DCtx`
      - call `ZSTD_decompressStream`
      - assert decoded bytes and buffer positions
    - Fix `safe/src/ffi/compress.rs` and `safe/src/compress/cstream.rs` for:
      - final-frame production on `ZSTD_e_end`
      - correct `input.pos` and `output.pos` updates
      - return value semantics for complete vs pending output
      - destination-too-small progress handling
    - Fix `safe/src/ffi/decompress.rs` and `safe/src/decompress/dstream.rs` for:
      - one-call full-frame decode
      - empty-output and partial-output behavior
      - `ZSTD_inBuffer` / `ZSTD_outBuffer` pointer validation
      - no-forward-progress error semantics

    ## Implementation Details

    - Capture `Phase 3 Base Commit: $(git rev-parse HEAD)` before making tracked changes and record that value in the Phase 3 report section.
    - If no Phase 1 failure row is assigned to `impl_validator_streaming_capi_regressions`, update `validator-report.md` with the exact note `No streaming C API failures assigned to impl_validator_streaming_capi_regressions`, record the phase base commit and commands inspected, commit that report-only update, and yield without changing safe code.
    - For true libzstd-safe streaming C API rows assigned to this phase, add regressions and fixes first, commit the code/test/package changes before running `safe/scripts/run-validator-libzstd.sh`, then update the failure table rows from `open` to `fixed` with `regression_test`, `fix_commit`, and notes before making the final report commit.
    - If an assigned streaming C API row appears to be a validator bug rather than a libzstd-safe bug, do not create `skip.env` in this phase and do not mark the row `skipped_validator_bug`. Reassign that row to `impl_validator_remaining_burn_down`, keep it `open`, add `suspected_validator_bug_deferred_to_phase5:impl_validator_streaming_capi_regressions: <short reason>` to `notes`, include the supporting result/log paths in the Phase 3 report section, and make the final report commit so Phase 5 owns the proof or reclassification.
    - Maintain exact C layout for `ZSTD_inBuffer`, `ZSTD_outBuffer`, and `ZSTD_EndDirective` in `safe/src/ffi/types.rs`.
    - Preserve allocation/free behavior for null contexts and null buffers.
    - For partial buffers, loop only while forward progress is possible; return the expected remaining hint rather than spinning.
    - If the validator failure comes from compile/link behavior rather than runtime behavior, inspect installed headers and pkg-config metadata:
      - `safe/include/zstd.h`
      - `safe/pkgconfig/libzstd.pc.in`
      - `safe/cmake/`
      - `safe/scripts/build-artifacts.sh`

    ## Verification Phases

    - `check_validator_capi_software_tester` - type: `check`; fixed `bounce_target: impl_validator_streaming_capi_regressions`; purpose: verify the exact streaming C API smoke behavior and local C ABI regressions. Commands to run:
    - `cargo test --manifest-path safe/Cargo.toml --release --all-targets`
      - `bash safe/scripts/run-capi-roundtrip.sh`
      - `bash safe/scripts/run-capi-decompression.sh`
      - `cargo test --manifest-path safe/Cargo.toml --release --test compress`
      - `cargo test --manifest-path safe/Cargo.toml --release --test decompress`
      - `bash -lc 'if [ -d safe/tests/validator ]; then test -x safe/scripts/run-validator-regressions.sh; bash safe/scripts/run-validator-regressions.sh; fi'`
      - `test ! -f safe/out/validator/skip.env`
      - `bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --allow-remaining-phase impl_validator_libarchive_usage_regressions --allow-remaining-phase impl_validator_remaining_burn_down'`
      - `rg -n 'streaming-c-api-smoke|No streaming C API failures assigned to impl_validator_streaming_capi_regressions' validator-report.md`
    - `check_validator_capi_senior_tester` - type: `check`; fixed `bounce_target: impl_validator_streaming_capi_regressions`; purpose: review buffer position semantics, ownership/lifetime handling, and C ABI compatibility. Commands to run:
      - `bash -lc 'base=$(awk "/Phase 3 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'`
      - `bash safe/scripts/verify-export-parity.sh`
      - `bash safe/scripts/verify-link-compat.sh`
      - `rg -n 'streaming-c-api-smoke|No streaming C API failures assigned|ZSTD_compressStream2|ZSTD_decompressStream' validator-report.md safe/tests safe/src`

    ## Verification

    - Run the two check phases above.
    - Rebuild `.deb` packages and rerun the validator matrix.
    - Confirm `streaming-c-api-smoke` passes or the report states there was no Phase 3 failure.

    ## Success Criteria

    - Every Phase 1 row assigned to `impl_validator_streaming_capi_regressions` is fixed with a checked-in C/Rust regression and `fix_commit`, or explicitly deferred to Phase 5 as a suspected validator bug.
    - If no streaming C API rows were assigned, `validator-report.md` contains `No streaming C API failures assigned to impl_validator_streaming_capi_regressions` and a report-only commit.
    - `streaming-c-api-smoke` passes when this phase is marked complete, while only later-phase rows may remain open.
    - C buffer position, return-hint, pointer validation, and ABI layout behavior remain compatible with upstream and are covered by the listed commands.

    ## Git Commit Requirement

    The implementer must commit all Phase 3 work to git before yielding to the verifier phases.
    {% endraw %}

    Verdict Instruction
    End with exactly one verdict line:
    VERDICT: PASS
    VERDICT: FAIL: <reason>
- id: impl_validator_libarchive_usage_regressions
  type: implement
  bounce_target: null
  prompt: |
    Role: Software engineer implementing phase `impl_validator_libarchive_usage_regressions` (Validator Libarchive Usage Remediation).

    Workflow goal: {{GOAL}}

    Work from `/home/yans/safelibs/pipeline/ports/port-libzstd`.
    This prompt is self-contained; the agent sees only this prompt.
    Use `.plan/workflow-structure.yaml` as the authoritative source for phase order, phase IDs, phase types, and this phase's fixed `bounce_target` value. Do not invent, rename, or reorder phases.
    Consume or update existing artifacts listed under `Preexisting Inputs` in place. Do not refetch, recollect, rediscover, or regenerate checked-in or already prepared artifacts from scratch when they already satisfy the required contract.
    Preserve unrelated user work.
    Commit all scoped work to git before yielding.

    Topology Contract
    - Phase order: `10`
    - Phase ID: `impl_validator_libarchive_usage_regressions`
    - Phase type: `implement`
    - Fixed top-level `bounce_target`: `null`

    Preexisting Inputs
    - `validator/`
    - `validator-report.md`
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/check-validator-phase-results.py`
    - `safe/scripts/run-validator-regressions.sh`
    - `safe/out/validator/artifacts/port-04-test/results/libzstd/`
    - `safe/out/validator/artifacts/port-04-test/logs/libzstd/`
    - `dependents.json`
    - `test-original.sh`
    - `safe/docker/dependents/`
    - `safe/docker/dependents/entrypoint.sh`
    - `safe/tests/dependents/dependent_matrix.toml`
    - `safe/tests/dependents/src/libarchive_probe.c`
    - `safe/scripts/run-dependent-matrix.sh`
    - `safe/scripts/build-dependent-image.sh`
    - `safe/src/compress/`
    - `safe/src/decompress/`
    - `safe/src/common/frame.rs`
    - `safe/src/ffi/compress.rs`
    - `safe/src/ffi/decompress.rs`
    - `safe/scripts/build-artifacts.sh`
    - `safe/scripts/build-original-cli-against-safe.sh`
    - `safe/scripts/build-deb.sh`

    New Outputs
    - Minimal libarchive usage reproducer for each unique usage failure class
    - Optional executable `safe/tests/validator/libarchive_usage_cases.sh` with failing validator case mirrors, run by `safe/scripts/run-validator-regressions.sh`
    - Updated dependent runtime `test_libarchive` if the validator uncovers a durable downstream scenario not already covered locally
    - Fixed safe implementation or package metadata
    - Refreshed `validator-report.md` Phase 4 section
    - A pre-validator code/test/package commit when safe code, packaging, or regression tests changed, plus a final Phase 4 report commit

    Phase Source Content
    {% raw %}
    # Phase 4: Libarchive Usage Validator Failures

    ## Phase Name

    Validator Libarchive Usage Remediation

    ## Implement Phase ID

    `impl_validator_libarchive_usage_regressions`

    ## Preexisting Inputs

    - Outputs from Phases 1-3
    - Validator libarchive usage failure logs under `safe/out/validator/artifacts/port-04-test/logs/libzstd/`
    - `safe/docker/dependents/entrypoint.sh`
    - `safe/tests/dependents/dependent_matrix.toml`
    - `safe/tests/dependents/src/libarchive_probe.c`
    - `safe/scripts/run-dependent-matrix.sh`
    - `safe/scripts/build-dependent-image.sh`
    - `safe/src/compress/`
    - `safe/src/decompress/`
    - `safe/src/common/frame.rs`
    - `safe/src/ffi/compress.rs`
    - `safe/src/ffi/decompress.rs`
    - `safe/scripts/build-artifacts.sh`
    - `safe/scripts/build-original-cli-against-safe.sh`
    - `safe/scripts/build-deb.sh`

    ## New Outputs

    - Minimal libarchive usage reproducer for each unique usage failure class
    - Optional executable `safe/tests/validator/libarchive_usage_cases.sh` with failing validator case mirrors, run by `safe/scripts/run-validator-regressions.sh`
    - Updated dependent runtime `test_libarchive` if the validator uncovers a durable downstream scenario not already covered locally
    - Fixed safe implementation or package metadata
    - Refreshed `validator-report.md` Phase 4 section
    - A pre-validator code/test/package commit when safe code, packaging, or regression tests changed, plus a final Phase 4 report commit

    ## File Changes

    - Add a reproducer before each fix. Prefer an existing dependent runtime test when the issue is truly downstream behavior:
    - `safe/docker/dependents/entrypoint.sh` `test_libarchive`
      - `safe/tests/dependents/src/libarchive_probe.c`
      - fixtures under `safe/tests/dependents/fixtures/` if needed
    - If the failure is specific to validator shell behavior, add a narrower script under `safe/tests/validator/` rather than expanding the dependent matrix unnecessarily.
    - Fix likely implementation areas:
      - zstd frame generation and final block handling in `safe/src/compress/frame.rs`, `safe/src/compress/cstream.rs`, and `safe/src/ffi/compress.rs`
      - continuous stream decode and concatenated frame handling in `safe/src/decompress/frame.rs`, `safe/src/decompress/dstream.rs`, and `safe/src/ffi/decompress.rs`
      - frame size and content size probes in `safe/src/common/frame.rs`
      - package library/header metadata in `safe/pkgconfig/libzstd.pc.in`, `safe/cmake/`, `safe/debian/`, or `safe/scripts/build-artifacts.sh`

    ## Implementation Details

    - Capture `Phase 4 Base Commit: $(git rev-parse HEAD)` before making tracked changes and record that value in the Phase 4 report section.
    - If no Phase 1 failure row is assigned to `impl_validator_libarchive_usage_regressions`, update `validator-report.md` with the exact note `No libarchive usage failures assigned to impl_validator_libarchive_usage_regressions`, record the phase base commit and commands inspected, commit that report-only update, and yield without changing safe code.
    - For true libzstd-safe libarchive usage rows assigned to this phase, add regressions and fixes first, commit the code/test/package changes before running `safe/scripts/run-validator-libzstd.sh`, then update the failure table rows from `open` to `fixed` with `regression_test`, `fix_commit`, and notes before making the final report commit.
    - If an assigned libarchive usage row appears to be a validator bug rather than a libzstd-safe bug, do not create `skip.env` in this phase and do not mark the row `skipped_validator_bug`. Reassign that row to `impl_validator_remaining_burn_down`, keep it `open`, add `suspected_validator_bug_deferred_to_phase5:impl_validator_libarchive_usage_regressions: <short reason>` to `notes`, include the supporting result/log paths in the Phase 4 report section, and make the final report commit so Phase 5 owns the proof or reclassification.
    - Group the 80 usage cases by failing symptom and fix one root cause per change set:
      - archive creation failures
      - archive listing failures
      - extraction data mismatch
      - stdin/stdout streaming failures
      - metadata or mode preservation failures
      - large-file or multi-member failures
      - dynamic linker resolving the wrong `libzstd`
    - Preserve validator's use of installed Debian packages. Do not run libarchive usage tests by pointing `LD_LIBRARY_PATH` at build-tree artifacts unless the local reproducer is explicitly marked as pre-package triage.
    - When a failure is due to `bsdtar --zstd` producing archives that safe cannot read, fix compression output. When a failure is due to safe `zstd` output that `bsdtar` cannot read, fix frame format compatibility. When a failure occurs only after package install, fix packaging or install layout.

    ## Verification Phases

    - `check_validator_usage_software_tester` - type: `check`; fixed `bounce_target: impl_validator_libarchive_usage_regressions`; purpose: run dependent/libarchive-focused regressions and confirm usage-case deltas. Commands to run:
    - `cargo test --manifest-path safe/Cargo.toml --release --all-targets`
      - `bash safe/scripts/verify-export-parity.sh`
      - `bash -lc 'if [ -d safe/tests/validator ]; then test -x safe/scripts/run-validator-regressions.sh; bash safe/scripts/run-validator-regressions.sh; fi'`
      - `test ! -f safe/out/validator/skip.env`
      - `bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --allow-remaining-phase impl_validator_remaining_burn_down'`
      - `bash safe/scripts/build-dependent-image.sh`
      - `bash safe/scripts/run-dependent-matrix.sh --runtime-only --apps libarchive`
      - `bash test-original.sh --runtime-only --apps libarchive`
      - `rg -n 'usage-libarchive-tools|No libarchive usage failures assigned to impl_validator_libarchive_usage_regressions' validator-report.md`
    - `check_validator_usage_senior_tester` - type: `check`; fixed `bounce_target: impl_validator_libarchive_usage_regressions`; purpose: review package install behavior, dynamic linkage, streaming semantics, and metadata handling. Commands to run:
      - `bash -lc 'base=$(awk "/Phase 4 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'`
      - `bash safe/scripts/verify-install-layout.sh`
      - `bash safe/scripts/verify-install-layout.sh --debian`
      - `bash safe/scripts/verify-link-compat.sh`
      - `git -C validator status --short --branch`

    ## Verification

    - Run the two check phases above.
    - Rebuild packages and dependent image after every safe or packaging fix.
    - Rerun the validator matrix and confirm the affected usage cases pass.

    ## Success Criteria

    - Every Phase 1 row assigned to `impl_validator_libarchive_usage_regressions` is fixed with a checked-in validator/dependent regression and `fix_commit`, or explicitly deferred to Phase 5 as a suspected validator bug.
    - If no libarchive usage rows were assigned, `validator-report.md` contains `No libarchive usage failures assigned to impl_validator_libarchive_usage_regressions` and a report-only commit.
    - Affected `libarchive-tools` and `bsdtar --zstd` usage cases pass when this phase is marked complete, while only Phase 5 rows may remain open.
    - Package install layout, dynamic linkage, dependent-image libarchive runtime coverage, and safe frame compatibility are verified with the listed commands.

    ## Git Commit Requirement

    The implementer must commit all Phase 4 work to git before yielding to the verifier phases.
    {% endraw %}
- id: check_validator_usage_software_tester
  type: check
  bounce_target: impl_validator_libarchive_usage_regressions
  prompt: |
    Reviewer Role: Software tester reviewer for Phase 4 libarchive usage remediation.

    Workflow goal: {{GOAL}}

    Work from `/home/yans/safelibs/pipeline/ports/port-libzstd`.
    This prompt is self-contained; the agent sees only this prompt.
    Use `.plan/workflow-structure.yaml` as the authoritative source for phase order, phase IDs, phase types, and this phase's fixed `bounce_target` value. Do not invent, rename, or reorder phases.
    Review the committed work from the implement phase `impl_validator_libarchive_usage_regressions`. Do not edit implementation files unless the workflow explicitly bounces and you become the implementer for the bounced phase.

    Topology Contract
    - Phase order: `11`
    - Phase ID: `check_validator_usage_software_tester`
    - Phase type: `check`
    - Fixed top-level `bounce_target`: `impl_validator_libarchive_usage_regressions`

    Preexisting Inputs
    - `validator/`
    - `validator-report.md`
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/check-validator-phase-results.py`
    - `safe/scripts/run-validator-regressions.sh`
    - `safe/out/validator/artifacts/port-04-test/results/libzstd/`
    - `safe/out/validator/artifacts/proof/port-04-test-debs-lock.json`
    - `safe/scripts/build-dependent-image.sh`
    - `safe/scripts/run-dependent-matrix.sh`
    - `test-original.sh`

    Specific Things To Verify
    - Each libarchive usage failure class has a minimal local or dependent regression and a scoped safe-side fix, or is deferred to Phase 5 with required suspected-validator-bug notes.
    - Package-backed validator results show Phase 2, Phase 3, and Phase 4 completed while only Phase 5 rows may remain open.
    - No validator skip file exists during this phase.
    - Dependent libarchive image/runtime gates and original runtime comparison pass.

    Commands To Run
    Run these commands exactly, unless an earlier command failure makes a later command impossible. Record any failure details in the verdict reason.

    {% raw %}
    ```bash
    cargo test --manifest-path safe/Cargo.toml --release --all-targets
    bash safe/scripts/verify-export-parity.sh
    bash -lc 'if [ -d safe/tests/validator ]; then test -x safe/scripts/run-validator-regressions.sh; bash safe/scripts/run-validator-regressions.sh; fi'
    test ! -f safe/out/validator/skip.env
    bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --allow-remaining-phase impl_validator_remaining_burn_down'
    bash safe/scripts/build-dependent-image.sh
    bash safe/scripts/run-dependent-matrix.sh --runtime-only --apps libarchive
    bash test-original.sh --runtime-only --apps libarchive
    rg -n 'usage-libarchive-tools|No libarchive usage failures assigned to impl_validator_libarchive_usage_regressions' validator-report.md
    ```
    {% endraw %}

    Phase Source Content For The Implement Block Being Verified
    {% raw %}
    # Phase 4: Libarchive Usage Validator Failures

    ## Phase Name

    Validator Libarchive Usage Remediation

    ## Implement Phase ID

    `impl_validator_libarchive_usage_regressions`

    ## Preexisting Inputs

    - Outputs from Phases 1-3
    - Validator libarchive usage failure logs under `safe/out/validator/artifacts/port-04-test/logs/libzstd/`
    - `safe/docker/dependents/entrypoint.sh`
    - `safe/tests/dependents/dependent_matrix.toml`
    - `safe/tests/dependents/src/libarchive_probe.c`
    - `safe/scripts/run-dependent-matrix.sh`
    - `safe/scripts/build-dependent-image.sh`
    - `safe/src/compress/`
    - `safe/src/decompress/`
    - `safe/src/common/frame.rs`
    - `safe/src/ffi/compress.rs`
    - `safe/src/ffi/decompress.rs`
    - `safe/scripts/build-artifacts.sh`
    - `safe/scripts/build-original-cli-against-safe.sh`
    - `safe/scripts/build-deb.sh`

    ## New Outputs

    - Minimal libarchive usage reproducer for each unique usage failure class
    - Optional executable `safe/tests/validator/libarchive_usage_cases.sh` with failing validator case mirrors, run by `safe/scripts/run-validator-regressions.sh`
    - Updated dependent runtime `test_libarchive` if the validator uncovers a durable downstream scenario not already covered locally
    - Fixed safe implementation or package metadata
    - Refreshed `validator-report.md` Phase 4 section
    - A pre-validator code/test/package commit when safe code, packaging, or regression tests changed, plus a final Phase 4 report commit

    ## File Changes

    - Add a reproducer before each fix. Prefer an existing dependent runtime test when the issue is truly downstream behavior:
    - `safe/docker/dependents/entrypoint.sh` `test_libarchive`
      - `safe/tests/dependents/src/libarchive_probe.c`
      - fixtures under `safe/tests/dependents/fixtures/` if needed
    - If the failure is specific to validator shell behavior, add a narrower script under `safe/tests/validator/` rather than expanding the dependent matrix unnecessarily.
    - Fix likely implementation areas:
      - zstd frame generation and final block handling in `safe/src/compress/frame.rs`, `safe/src/compress/cstream.rs`, and `safe/src/ffi/compress.rs`
      - continuous stream decode and concatenated frame handling in `safe/src/decompress/frame.rs`, `safe/src/decompress/dstream.rs`, and `safe/src/ffi/decompress.rs`
      - frame size and content size probes in `safe/src/common/frame.rs`
      - package library/header metadata in `safe/pkgconfig/libzstd.pc.in`, `safe/cmake/`, `safe/debian/`, or `safe/scripts/build-artifacts.sh`

    ## Implementation Details

    - Capture `Phase 4 Base Commit: $(git rev-parse HEAD)` before making tracked changes and record that value in the Phase 4 report section.
    - If no Phase 1 failure row is assigned to `impl_validator_libarchive_usage_regressions`, update `validator-report.md` with the exact note `No libarchive usage failures assigned to impl_validator_libarchive_usage_regressions`, record the phase base commit and commands inspected, commit that report-only update, and yield without changing safe code.
    - For true libzstd-safe libarchive usage rows assigned to this phase, add regressions and fixes first, commit the code/test/package changes before running `safe/scripts/run-validator-libzstd.sh`, then update the failure table rows from `open` to `fixed` with `regression_test`, `fix_commit`, and notes before making the final report commit.
    - If an assigned libarchive usage row appears to be a validator bug rather than a libzstd-safe bug, do not create `skip.env` in this phase and do not mark the row `skipped_validator_bug`. Reassign that row to `impl_validator_remaining_burn_down`, keep it `open`, add `suspected_validator_bug_deferred_to_phase5:impl_validator_libarchive_usage_regressions: <short reason>` to `notes`, include the supporting result/log paths in the Phase 4 report section, and make the final report commit so Phase 5 owns the proof or reclassification.
    - Group the 80 usage cases by failing symptom and fix one root cause per change set:
      - archive creation failures
      - archive listing failures
      - extraction data mismatch
      - stdin/stdout streaming failures
      - metadata or mode preservation failures
      - large-file or multi-member failures
      - dynamic linker resolving the wrong `libzstd`
    - Preserve validator's use of installed Debian packages. Do not run libarchive usage tests by pointing `LD_LIBRARY_PATH` at build-tree artifacts unless the local reproducer is explicitly marked as pre-package triage.
    - When a failure is due to `bsdtar --zstd` producing archives that safe cannot read, fix compression output. When a failure is due to safe `zstd` output that `bsdtar` cannot read, fix frame format compatibility. When a failure occurs only after package install, fix packaging or install layout.

    ## Verification Phases

    - `check_validator_usage_software_tester` - type: `check`; fixed `bounce_target: impl_validator_libarchive_usage_regressions`; purpose: run dependent/libarchive-focused regressions and confirm usage-case deltas. Commands to run:
    - `cargo test --manifest-path safe/Cargo.toml --release --all-targets`
      - `bash safe/scripts/verify-export-parity.sh`
      - `bash -lc 'if [ -d safe/tests/validator ]; then test -x safe/scripts/run-validator-regressions.sh; bash safe/scripts/run-validator-regressions.sh; fi'`
      - `test ! -f safe/out/validator/skip.env`
      - `bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --allow-remaining-phase impl_validator_remaining_burn_down'`
      - `bash safe/scripts/build-dependent-image.sh`
      - `bash safe/scripts/run-dependent-matrix.sh --runtime-only --apps libarchive`
      - `bash test-original.sh --runtime-only --apps libarchive`
      - `rg -n 'usage-libarchive-tools|No libarchive usage failures assigned to impl_validator_libarchive_usage_regressions' validator-report.md`
    - `check_validator_usage_senior_tester` - type: `check`; fixed `bounce_target: impl_validator_libarchive_usage_regressions`; purpose: review package install behavior, dynamic linkage, streaming semantics, and metadata handling. Commands to run:
      - `bash -lc 'base=$(awk "/Phase 4 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'`
      - `bash safe/scripts/verify-install-layout.sh`
      - `bash safe/scripts/verify-install-layout.sh --debian`
      - `bash safe/scripts/verify-link-compat.sh`
      - `git -C validator status --short --branch`

    ## Verification

    - Run the two check phases above.
    - Rebuild packages and dependent image after every safe or packaging fix.
    - Rerun the validator matrix and confirm the affected usage cases pass.

    ## Success Criteria

    - Every Phase 1 row assigned to `impl_validator_libarchive_usage_regressions` is fixed with a checked-in validator/dependent regression and `fix_commit`, or explicitly deferred to Phase 5 as a suspected validator bug.
    - If no libarchive usage rows were assigned, `validator-report.md` contains `No libarchive usage failures assigned to impl_validator_libarchive_usage_regressions` and a report-only commit.
    - Affected `libarchive-tools` and `bsdtar --zstd` usage cases pass when this phase is marked complete, while only Phase 5 rows may remain open.
    - Package install layout, dynamic linkage, dependent-image libarchive runtime coverage, and safe frame compatibility are verified with the listed commands.

    ## Git Commit Requirement

    The implementer must commit all Phase 4 work to git before yielding to the verifier phases.
    {% endraw %}

    Verdict Instruction
    End with exactly one verdict line:
    VERDICT: PASS
    VERDICT: FAIL: <reason>
- id: check_validator_usage_senior_tester
  type: check
  bounce_target: impl_validator_libarchive_usage_regressions
  prompt: |
    Reviewer Role: Senior tester reviewer for Phase 4 package install behavior, dynamic linkage, streaming semantics, and metadata handling.

    Workflow goal: {{GOAL}}

    Work from `/home/yans/safelibs/pipeline/ports/port-libzstd`.
    This prompt is self-contained; the agent sees only this prompt.
    Use `.plan/workflow-structure.yaml` as the authoritative source for phase order, phase IDs, phase types, and this phase's fixed `bounce_target` value. Do not invent, rename, or reorder phases.
    Review the committed work from the implement phase `impl_validator_libarchive_usage_regressions`. Do not edit implementation files unless the workflow explicitly bounces and you become the implementer for the bounced phase.

    Topology Contract
    - Phase order: `12`
    - Phase ID: `check_validator_usage_senior_tester`
    - Phase type: `check`
    - Fixed top-level `bounce_target`: `impl_validator_libarchive_usage_regressions`

    Preexisting Inputs
    - `validator/`
    - `validator-report.md`
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/check-validator-phase-results.py`
    - `safe/out/validator/artifacts/port-04-test/results/libzstd/`
    - `safe/scripts/verify-install-layout.sh`
    - `safe/scripts/verify-link-compat.sh`

    Specific Things To Verify
    - The Phase 4 diff is whitespace-clean from the recorded base commit.
    - Install layout and Debian install layout are valid after package rebuilds.
    - Link compatibility remains intact and validator/ was not modified.
    - Any libarchive usage fix addresses safe implementation or package metadata rather than bypassing the installed package path.

    Commands To Run
    Run these commands exactly, unless an earlier command failure makes a later command impossible. Record any failure details in the verdict reason.

    {% raw %}
    ```bash
    bash -lc 'base=$(awk "/Phase 4 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'
    bash safe/scripts/verify-install-layout.sh
    bash safe/scripts/verify-install-layout.sh --debian
    bash safe/scripts/verify-link-compat.sh
    git -C validator status --short --branch
    ```
    {% endraw %}

    Phase Source Content For The Implement Block Being Verified
    {% raw %}
    # Phase 4: Libarchive Usage Validator Failures

    ## Phase Name

    Validator Libarchive Usage Remediation

    ## Implement Phase ID

    `impl_validator_libarchive_usage_regressions`

    ## Preexisting Inputs

    - Outputs from Phases 1-3
    - Validator libarchive usage failure logs under `safe/out/validator/artifacts/port-04-test/logs/libzstd/`
    - `safe/docker/dependents/entrypoint.sh`
    - `safe/tests/dependents/dependent_matrix.toml`
    - `safe/tests/dependents/src/libarchive_probe.c`
    - `safe/scripts/run-dependent-matrix.sh`
    - `safe/scripts/build-dependent-image.sh`
    - `safe/src/compress/`
    - `safe/src/decompress/`
    - `safe/src/common/frame.rs`
    - `safe/src/ffi/compress.rs`
    - `safe/src/ffi/decompress.rs`
    - `safe/scripts/build-artifacts.sh`
    - `safe/scripts/build-original-cli-against-safe.sh`
    - `safe/scripts/build-deb.sh`

    ## New Outputs

    - Minimal libarchive usage reproducer for each unique usage failure class
    - Optional executable `safe/tests/validator/libarchive_usage_cases.sh` with failing validator case mirrors, run by `safe/scripts/run-validator-regressions.sh`
    - Updated dependent runtime `test_libarchive` if the validator uncovers a durable downstream scenario not already covered locally
    - Fixed safe implementation or package metadata
    - Refreshed `validator-report.md` Phase 4 section
    - A pre-validator code/test/package commit when safe code, packaging, or regression tests changed, plus a final Phase 4 report commit

    ## File Changes

    - Add a reproducer before each fix. Prefer an existing dependent runtime test when the issue is truly downstream behavior:
    - `safe/docker/dependents/entrypoint.sh` `test_libarchive`
      - `safe/tests/dependents/src/libarchive_probe.c`
      - fixtures under `safe/tests/dependents/fixtures/` if needed
    - If the failure is specific to validator shell behavior, add a narrower script under `safe/tests/validator/` rather than expanding the dependent matrix unnecessarily.
    - Fix likely implementation areas:
      - zstd frame generation and final block handling in `safe/src/compress/frame.rs`, `safe/src/compress/cstream.rs`, and `safe/src/ffi/compress.rs`
      - continuous stream decode and concatenated frame handling in `safe/src/decompress/frame.rs`, `safe/src/decompress/dstream.rs`, and `safe/src/ffi/decompress.rs`
      - frame size and content size probes in `safe/src/common/frame.rs`
      - package library/header metadata in `safe/pkgconfig/libzstd.pc.in`, `safe/cmake/`, `safe/debian/`, or `safe/scripts/build-artifacts.sh`

    ## Implementation Details

    - Capture `Phase 4 Base Commit: $(git rev-parse HEAD)` before making tracked changes and record that value in the Phase 4 report section.
    - If no Phase 1 failure row is assigned to `impl_validator_libarchive_usage_regressions`, update `validator-report.md` with the exact note `No libarchive usage failures assigned to impl_validator_libarchive_usage_regressions`, record the phase base commit and commands inspected, commit that report-only update, and yield without changing safe code.
    - For true libzstd-safe libarchive usage rows assigned to this phase, add regressions and fixes first, commit the code/test/package changes before running `safe/scripts/run-validator-libzstd.sh`, then update the failure table rows from `open` to `fixed` with `regression_test`, `fix_commit`, and notes before making the final report commit.
    - If an assigned libarchive usage row appears to be a validator bug rather than a libzstd-safe bug, do not create `skip.env` in this phase and do not mark the row `skipped_validator_bug`. Reassign that row to `impl_validator_remaining_burn_down`, keep it `open`, add `suspected_validator_bug_deferred_to_phase5:impl_validator_libarchive_usage_regressions: <short reason>` to `notes`, include the supporting result/log paths in the Phase 4 report section, and make the final report commit so Phase 5 owns the proof or reclassification.
    - Group the 80 usage cases by failing symptom and fix one root cause per change set:
      - archive creation failures
      - archive listing failures
      - extraction data mismatch
      - stdin/stdout streaming failures
      - metadata or mode preservation failures
      - large-file or multi-member failures
      - dynamic linker resolving the wrong `libzstd`
    - Preserve validator's use of installed Debian packages. Do not run libarchive usage tests by pointing `LD_LIBRARY_PATH` at build-tree artifacts unless the local reproducer is explicitly marked as pre-package triage.
    - When a failure is due to `bsdtar --zstd` producing archives that safe cannot read, fix compression output. When a failure is due to safe `zstd` output that `bsdtar` cannot read, fix frame format compatibility. When a failure occurs only after package install, fix packaging or install layout.

    ## Verification Phases

    - `check_validator_usage_software_tester` - type: `check`; fixed `bounce_target: impl_validator_libarchive_usage_regressions`; purpose: run dependent/libarchive-focused regressions and confirm usage-case deltas. Commands to run:
    - `cargo test --manifest-path safe/Cargo.toml --release --all-targets`
      - `bash safe/scripts/verify-export-parity.sh`
      - `bash -lc 'if [ -d safe/tests/validator ]; then test -x safe/scripts/run-validator-regressions.sh; bash safe/scripts/run-validator-regressions.sh; fi'`
      - `test ! -f safe/out/validator/skip.env`
      - `bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --allow-remaining-phase impl_validator_remaining_burn_down'`
      - `bash safe/scripts/build-dependent-image.sh`
      - `bash safe/scripts/run-dependent-matrix.sh --runtime-only --apps libarchive`
      - `bash test-original.sh --runtime-only --apps libarchive`
      - `rg -n 'usage-libarchive-tools|No libarchive usage failures assigned to impl_validator_libarchive_usage_regressions' validator-report.md`
    - `check_validator_usage_senior_tester` - type: `check`; fixed `bounce_target: impl_validator_libarchive_usage_regressions`; purpose: review package install behavior, dynamic linkage, streaming semantics, and metadata handling. Commands to run:
      - `bash -lc 'base=$(awk "/Phase 4 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'`
      - `bash safe/scripts/verify-install-layout.sh`
      - `bash safe/scripts/verify-install-layout.sh --debian`
      - `bash safe/scripts/verify-link-compat.sh`
      - `git -C validator status --short --branch`

    ## Verification

    - Run the two check phases above.
    - Rebuild packages and dependent image after every safe or packaging fix.
    - Rerun the validator matrix and confirm the affected usage cases pass.

    ## Success Criteria

    - Every Phase 1 row assigned to `impl_validator_libarchive_usage_regressions` is fixed with a checked-in validator/dependent regression and `fix_commit`, or explicitly deferred to Phase 5 as a suspected validator bug.
    - If no libarchive usage rows were assigned, `validator-report.md` contains `No libarchive usage failures assigned to impl_validator_libarchive_usage_regressions` and a report-only commit.
    - Affected `libarchive-tools` and `bsdtar --zstd` usage cases pass when this phase is marked complete, while only Phase 5 rows may remain open.
    - Package install layout, dynamic linkage, dependent-image libarchive runtime coverage, and safe frame compatibility are verified with the listed commands.

    ## Git Commit Requirement

    The implementer must commit all Phase 4 work to git before yielding to the verifier phases.
    {% endraw %}

    Verdict Instruction
    End with exactly one verdict line:
    VERDICT: PASS
    VERDICT: FAIL: <reason>
- id: impl_validator_remaining_burn_down
  type: implement
  bounce_target: null
  prompt: |
    Role: Software engineer implementing phase `impl_validator_remaining_burn_down` (Remaining Validator Burn Down).

    Workflow goal: {{GOAL}}

    Work from `/home/yans/safelibs/pipeline/ports/port-libzstd`.
    This prompt is self-contained; the agent sees only this prompt.
    Use `.plan/workflow-structure.yaml` as the authoritative source for phase order, phase IDs, phase types, and this phase's fixed `bounce_target` value. Do not invent, rename, or reorder phases.
    Consume or update existing artifacts listed under `Preexisting Inputs` in place. Do not refetch, recollect, rediscover, or regenerate checked-in or already prepared artifacts from scratch when they already satisfy the required contract.
    Preserve unrelated user work.
    Commit all scoped work to git before yielding.

    Topology Contract
    - Phase order: `13`
    - Phase ID: `impl_validator_remaining_burn_down`
    - Phase type: `implement`
    - Fixed top-level `bounce_target`: `null`

    Preexisting Inputs
    - `validator/`
    - `validator-report.md`
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/check-validator-phase-results.py`
    - `safe/scripts/run-validator-regressions.sh`
    - `safe/out/validator/artifacts/`

    New Outputs
    - Fixes for any residual validator failures not covered by Phases 2-4
    - Generated filtered validator test root under `safe/out/validator/tests-filtered/` only if a validator bug is clearly proven
    - Generated skip metadata under `safe/out/validator/skip.env` only if a validator bug is clearly proven
    - Finalized failure classification in `validator-report.md`
    - A pre-validator code/test/package commit when safe code, packaging, or regression tests changed, plus a final Phase 5 report commit

    Phase Source Content
    {% raw %}
    # Phase 5: Remaining Failures, Validator-Bug Triage, and Report Consolidation

    ## Phase Name

    Remaining Validator Burn Down

    ## Implement Phase ID

    `impl_validator_remaining_burn_down`

    ## Preexisting Inputs

    - Outputs from Phases 1-4
    - Full validator results under `safe/out/validator/artifacts/`
    - `validator-report.md`
    - All local regression tests added in earlier phases

    ## New Outputs

    - Fixes for any residual validator failures not covered by Phases 2-4
    - Generated filtered validator test root under `safe/out/validator/tests-filtered/` only if a validator bug is clearly proven
    - Generated skip metadata under `safe/out/validator/skip.env` only if a validator bug is clearly proven
    - Finalized failure classification in `validator-report.md`
    - A pre-validator code/test/package commit when safe code, packaging, or regression tests changed, plus a final Phase 5 report commit

    ## File Changes

    - Update only the safe-side files needed for true residual compatibility failures.
    - If a validator bug is proven:
      - leave `validator/` unchanged
      - document the bug in `validator-report.md`
      - generate any filtered test root under `safe/out/validator/tests-filtered/`
      - generate `safe/out/validator/skip.env` with adjusted proof thresholds and skipped testcase IDs
      - ensure only the invalid check is omitted from the filtered run
    - Update `safe/scripts/run-validator-libzstd.sh` only if needed to support generated skip roots or better report generation.

    ## Implementation Details

    - Capture `Phase 5 Base Commit: $(git rev-parse HEAD)` before making tracked changes and record that value in the Phase 5 report section.
    - If no Phase 1 failure row is assigned to `impl_validator_remaining_burn_down`, run the strict full validator helper, then update `validator-report.md` with the exact note `No remaining failures assigned to impl_validator_remaining_burn_down`, the phase base commit, commands run, and result paths before making one report-only commit.
    - If residual rows are assigned to this phase, add regressions and fixes first for true libzstd-safe failures, commit code/test/package changes before running `safe/scripts/run-validator-libzstd.sh`, then update the failure table rows from `open` to `fixed` with `regression_test`, `fix_commit`, and notes before making the final report commit.
    - Re-parse all result JSON files after the Phase 4 run. The burn-down phase owns:
      - validator infrastructure problems
      - package override install problems
      - proof generation problems
      - residual mixed failures that involve more than one prior category
      - suspected validator-bug rows reassigned from Phases 2-4
      - validator bug documentation
    - A validator-bug finding must include:
      - exact testcase ID
      - validator commit
      - log path
      - why Ubuntu original-package behavior or validator script logic is inconsistent with the stated test
      - why modifying libzstd-safe would be incorrect
      - exact generated skip mechanics, if used
    - For each suspected validator-bug row handed off from an earlier phase, either prove it as a validator bug and apply the generated skip path below, or reclassify it as a true libzstd-safe residual failure, add a checked-in regression, fix it in safe code or package metadata, and mark it `fixed` with the Phase 5 fix commit.
    - When generating a skip, remove any stale `safe/out/validator/tests-filtered/`, then create this generated dual layout:
      - copy `validator/tests/libzstd/` to `safe/out/validator/tests-filtered/libzstd/`
      - create `safe/out/validator/tests-filtered/tests/`
      - copy `validator/tests/libzstd/` to `safe/out/validator/tests-filtered/tests/libzstd/`
      - remove only the invalid testcase entries from both generated `testcases.yml` files
      - preserve the copied Dockerfile, entrypoint, fixtures, and case scripts in both generated copies
      - leave `validator/tests/_shared/` consumed from the unmodified validator checkout; do not copy or edit `_shared`
    - Resolve `PYTHON` exactly as in `safe/scripts/run-validator-libzstd.sh`, preferring `safe/out/validator/venv/bin/python` when it exists and otherwise requiring `python3 -c 'import yaml'` to pass. Validate the generated skip root before the filtered run with `"$PYTHON" validator/tools/testcases.py --config validator/repositories.yml --tests-root "$PWD/safe/out/validator/tests-filtered" --library libzstd --check --min-source-cases "$VALIDATOR_MIN_SOURCE_CASES" --min-usage-cases "$VALIDATOR_MIN_USAGE_CASES" --min-cases "$VALIDATOR_MIN_CASES"`. Run the filtered matrix with the unmodified `--config "$PWD/validator/repositories.yml"`, `--tests-root "$PWD/safe/out/validator/tests-filtered"`, `--mode port-04-test`, `--override-deb-root "$PWD/safe/out/validator/override-debs"`, `--port-deb-lock "$PWD/safe/out/validator/artifacts/proof/port-04-test-debs-lock.json"`, and `--library libzstd`.
    - `safe/out/validator/skip.env` must define:
      - `VALIDATOR_TESTS_ROOT` as the absolute path to `safe/out/validator/tests-filtered`
      - `VALIDATOR_SKIPPED_CASES` as a space-separated list of skipped testcase IDs
      - `VALIDATOR_CANONICAL_SOURCE_CASES=5`, `VALIDATOR_CANONICAL_USAGE_CASES=80`, and `VALIDATOR_CANONICAL_CASES=85`
      - `VALIDATOR_SKIPPED_SOURCE_CASES`, `VALIDATOR_SKIPPED_USAGE_CASES`, and `VALIDATOR_SKIPPED_TOTAL_CASES` as the documented skipped counts
      - `VALIDATOR_MIN_SOURCE_CASES`, `VALIDATOR_MIN_USAGE_CASES`, and `VALIDATOR_MIN_CASES` as 5, 80, and 85 minus the documented skipped source, usage, and total counts
    - A true libzstd-safe residual failure must still get a checked-in regression before the fix.

    ## Verification Phases

    - `check_validator_remaining_software_tester` - type: `check`; fixed `bounce_target: impl_validator_remaining_burn_down`; purpose: verify no unclassified failures remain and all fixed failures have regressions. Commands to run:
    - `bash safe/scripts/run-validator-regressions.sh`
      - `bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --completed-phase impl_validator_remaining_burn_down; test "$status" -eq 0'`
      - `python3 -m json.tool safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json >/dev/null`
      - `rg -n 'Unclassified|Remaining|Validator Bug|Skip|No remaining failures assigned to impl_validator_remaining_burn_down' validator-report.md`
      - `cargo test --manifest-path safe/Cargo.toml --release --all-targets`
      - `bash safe/scripts/verify-export-parity.sh`
    - `check_validator_remaining_senior_tester` - type: `check`; fixed `bounce_target: impl_validator_remaining_burn_down`; purpose: review any validator-bug claims and ensure skips are minimal and generated-only. Commands to run:
      - `bash -lc 'base=$(awk "/Phase 5 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'`
      - `test -z "$(git -C validator status --porcelain --untracked-files=no)"`
      - `bash -lc 'if [ -f safe/out/validator/skip.env ]; then py=python3; if [ -x safe/out/validator/venv/bin/python ]; then py="$PWD/safe/out/validator/venv/bin/python"; else python3 -c "import yaml"; fi; set -a; . safe/out/validator/skip.env; set +a; test -n "${VALIDATOR_TESTS_ROOT:-}"; test -d "$VALIDATOR_TESTS_ROOT/libzstd"; test -d "$VALIDATOR_TESTS_ROOT/tests/libzstd"; "$py" validator/tools/testcases.py --config validator/repositories.yml --tests-root "$VALIDATOR_TESTS_ROOT" --library libzstd --check --min-source-cases "$VALIDATOR_MIN_SOURCE_CASES" --min-usage-cases "$VALIDATOR_MIN_USAGE_CASES" --min-cases "$VALIDATOR_MIN_CASES"; fi'`
      - `find safe/out/validator -maxdepth 3 -type d | sort | sed -n '1,120p'`
      - `rg -n 'skip|skipped|validator bug|justification|No remaining failures assigned to impl_validator_remaining_burn_down' validator-report.md`

    ## Verification

    - Run the two check phases above.
    - Confirm `validator-report.md` has no unclassified failures.
    - Confirm any skip is generated under `safe/out/validator/` and justified in the report.

    ## Success Criteria

    - No unclassified or open failures remain after Phase 5 all-completed checking.
    - Every `fixed` row has a non-empty `regression_test` and `fix_commit`.
    - Every `skipped_validator_bug` row has non-empty notes naming generated skip artifacts and a clear justification.
    - Any skip root and threshold reduction are generated only under `safe/out/validator/`, minimal, validated, and recorded with `VALIDATOR_SKIPPED_SOURCE_CASES`, `VALIDATOR_SKIPPED_USAGE_CASES`, and `VALIDATOR_SKIPPED_TOTAL_CASES`.
    - `validator/` remains unmodified except normal clone/fetch/switch/fast-forward operations.

    ## Git Commit Requirement

    The implementer must commit all Phase 5 work to git before yielding to the verifier phases.
    {% endraw %}
- id: check_validator_remaining_software_tester
  type: check
  bounce_target: impl_validator_remaining_burn_down
  prompt: |
    Reviewer Role: Software tester reviewer for Phase 5 remaining failure burn-down and table closure.

    Workflow goal: {{GOAL}}

    Work from `/home/yans/safelibs/pipeline/ports/port-libzstd`.
    This prompt is self-contained; the agent sees only this prompt.
    Use `.plan/workflow-structure.yaml` as the authoritative source for phase order, phase IDs, phase types, and this phase's fixed `bounce_target` value. Do not invent, rename, or reorder phases.
    Review the committed work from the implement phase `impl_validator_remaining_burn_down`. Do not edit implementation files unless the workflow explicitly bounces and you become the implementer for the bounced phase.

    Topology Contract
    - Phase order: `14`
    - Phase ID: `check_validator_remaining_software_tester`
    - Phase type: `check`
    - Fixed top-level `bounce_target`: `impl_validator_remaining_burn_down`

    Preexisting Inputs
    - `validator/`
    - `validator-report.md`
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/check-validator-phase-results.py`
    - `safe/scripts/run-validator-regressions.sh`
    - `safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json`

    Specific Things To Verify
    - No unclassified or open validator failures remain after all remediation phases are marked completed.
    - Every fixed row has a regression test and fix commit; every skipped_validator_bug row has generated skip-artifact evidence.
    - The validator helper exits zero and the strict phase-result checker accepts the all-completed state.
    - Core Rust tests and export parity still pass after any residual fixes or skip mechanics.

    Commands To Run
    Run these commands exactly, unless an earlier command failure makes a later command impossible. Record any failure details in the verdict reason.

    {% raw %}
    ```bash
    bash safe/scripts/run-validator-regressions.sh
    bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --completed-phase impl_validator_remaining_burn_down; test "$status" -eq 0'
    python3 -m json.tool safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json >/dev/null
    rg -n 'Unclassified|Remaining|Validator Bug|Skip|No remaining failures assigned to impl_validator_remaining_burn_down' validator-report.md
    cargo test --manifest-path safe/Cargo.toml --release --all-targets
    bash safe/scripts/verify-export-parity.sh
    ```
    {% endraw %}

    Phase Source Content For The Implement Block Being Verified
    {% raw %}
    # Phase 5: Remaining Failures, Validator-Bug Triage, and Report Consolidation

    ## Phase Name

    Remaining Validator Burn Down

    ## Implement Phase ID

    `impl_validator_remaining_burn_down`

    ## Preexisting Inputs

    - Outputs from Phases 1-4
    - Full validator results under `safe/out/validator/artifacts/`
    - `validator-report.md`
    - All local regression tests added in earlier phases

    ## New Outputs

    - Fixes for any residual validator failures not covered by Phases 2-4
    - Generated filtered validator test root under `safe/out/validator/tests-filtered/` only if a validator bug is clearly proven
    - Generated skip metadata under `safe/out/validator/skip.env` only if a validator bug is clearly proven
    - Finalized failure classification in `validator-report.md`
    - A pre-validator code/test/package commit when safe code, packaging, or regression tests changed, plus a final Phase 5 report commit

    ## File Changes

    - Update only the safe-side files needed for true residual compatibility failures.
    - If a validator bug is proven:
      - leave `validator/` unchanged
      - document the bug in `validator-report.md`
      - generate any filtered test root under `safe/out/validator/tests-filtered/`
      - generate `safe/out/validator/skip.env` with adjusted proof thresholds and skipped testcase IDs
      - ensure only the invalid check is omitted from the filtered run
    - Update `safe/scripts/run-validator-libzstd.sh` only if needed to support generated skip roots or better report generation.

    ## Implementation Details

    - Capture `Phase 5 Base Commit: $(git rev-parse HEAD)` before making tracked changes and record that value in the Phase 5 report section.
    - If no Phase 1 failure row is assigned to `impl_validator_remaining_burn_down`, run the strict full validator helper, then update `validator-report.md` with the exact note `No remaining failures assigned to impl_validator_remaining_burn_down`, the phase base commit, commands run, and result paths before making one report-only commit.
    - If residual rows are assigned to this phase, add regressions and fixes first for true libzstd-safe failures, commit code/test/package changes before running `safe/scripts/run-validator-libzstd.sh`, then update the failure table rows from `open` to `fixed` with `regression_test`, `fix_commit`, and notes before making the final report commit.
    - Re-parse all result JSON files after the Phase 4 run. The burn-down phase owns:
      - validator infrastructure problems
      - package override install problems
      - proof generation problems
      - residual mixed failures that involve more than one prior category
      - suspected validator-bug rows reassigned from Phases 2-4
      - validator bug documentation
    - A validator-bug finding must include:
      - exact testcase ID
      - validator commit
      - log path
      - why Ubuntu original-package behavior or validator script logic is inconsistent with the stated test
      - why modifying libzstd-safe would be incorrect
      - exact generated skip mechanics, if used
    - For each suspected validator-bug row handed off from an earlier phase, either prove it as a validator bug and apply the generated skip path below, or reclassify it as a true libzstd-safe residual failure, add a checked-in regression, fix it in safe code or package metadata, and mark it `fixed` with the Phase 5 fix commit.
    - When generating a skip, remove any stale `safe/out/validator/tests-filtered/`, then create this generated dual layout:
      - copy `validator/tests/libzstd/` to `safe/out/validator/tests-filtered/libzstd/`
      - create `safe/out/validator/tests-filtered/tests/`
      - copy `validator/tests/libzstd/` to `safe/out/validator/tests-filtered/tests/libzstd/`
      - remove only the invalid testcase entries from both generated `testcases.yml` files
      - preserve the copied Dockerfile, entrypoint, fixtures, and case scripts in both generated copies
      - leave `validator/tests/_shared/` consumed from the unmodified validator checkout; do not copy or edit `_shared`
    - Resolve `PYTHON` exactly as in `safe/scripts/run-validator-libzstd.sh`, preferring `safe/out/validator/venv/bin/python` when it exists and otherwise requiring `python3 -c 'import yaml'` to pass. Validate the generated skip root before the filtered run with `"$PYTHON" validator/tools/testcases.py --config validator/repositories.yml --tests-root "$PWD/safe/out/validator/tests-filtered" --library libzstd --check --min-source-cases "$VALIDATOR_MIN_SOURCE_CASES" --min-usage-cases "$VALIDATOR_MIN_USAGE_CASES" --min-cases "$VALIDATOR_MIN_CASES"`. Run the filtered matrix with the unmodified `--config "$PWD/validator/repositories.yml"`, `--tests-root "$PWD/safe/out/validator/tests-filtered"`, `--mode port-04-test`, `--override-deb-root "$PWD/safe/out/validator/override-debs"`, `--port-deb-lock "$PWD/safe/out/validator/artifacts/proof/port-04-test-debs-lock.json"`, and `--library libzstd`.
    - `safe/out/validator/skip.env` must define:
      - `VALIDATOR_TESTS_ROOT` as the absolute path to `safe/out/validator/tests-filtered`
      - `VALIDATOR_SKIPPED_CASES` as a space-separated list of skipped testcase IDs
      - `VALIDATOR_CANONICAL_SOURCE_CASES=5`, `VALIDATOR_CANONICAL_USAGE_CASES=80`, and `VALIDATOR_CANONICAL_CASES=85`
      - `VALIDATOR_SKIPPED_SOURCE_CASES`, `VALIDATOR_SKIPPED_USAGE_CASES`, and `VALIDATOR_SKIPPED_TOTAL_CASES` as the documented skipped counts
      - `VALIDATOR_MIN_SOURCE_CASES`, `VALIDATOR_MIN_USAGE_CASES`, and `VALIDATOR_MIN_CASES` as 5, 80, and 85 minus the documented skipped source, usage, and total counts
    - A true libzstd-safe residual failure must still get a checked-in regression before the fix.

    ## Verification Phases

    - `check_validator_remaining_software_tester` - type: `check`; fixed `bounce_target: impl_validator_remaining_burn_down`; purpose: verify no unclassified failures remain and all fixed failures have regressions. Commands to run:
    - `bash safe/scripts/run-validator-regressions.sh`
      - `bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --completed-phase impl_validator_remaining_burn_down; test "$status" -eq 0'`
      - `python3 -m json.tool safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json >/dev/null`
      - `rg -n 'Unclassified|Remaining|Validator Bug|Skip|No remaining failures assigned to impl_validator_remaining_burn_down' validator-report.md`
      - `cargo test --manifest-path safe/Cargo.toml --release --all-targets`
      - `bash safe/scripts/verify-export-parity.sh`
    - `check_validator_remaining_senior_tester` - type: `check`; fixed `bounce_target: impl_validator_remaining_burn_down`; purpose: review any validator-bug claims and ensure skips are minimal and generated-only. Commands to run:
      - `bash -lc 'base=$(awk "/Phase 5 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'`
      - `test -z "$(git -C validator status --porcelain --untracked-files=no)"`
      - `bash -lc 'if [ -f safe/out/validator/skip.env ]; then py=python3; if [ -x safe/out/validator/venv/bin/python ]; then py="$PWD/safe/out/validator/venv/bin/python"; else python3 -c "import yaml"; fi; set -a; . safe/out/validator/skip.env; set +a; test -n "${VALIDATOR_TESTS_ROOT:-}"; test -d "$VALIDATOR_TESTS_ROOT/libzstd"; test -d "$VALIDATOR_TESTS_ROOT/tests/libzstd"; "$py" validator/tools/testcases.py --config validator/repositories.yml --tests-root "$VALIDATOR_TESTS_ROOT" --library libzstd --check --min-source-cases "$VALIDATOR_MIN_SOURCE_CASES" --min-usage-cases "$VALIDATOR_MIN_USAGE_CASES" --min-cases "$VALIDATOR_MIN_CASES"; fi'`
      - `find safe/out/validator -maxdepth 3 -type d | sort | sed -n '1,120p'`
      - `rg -n 'skip|skipped|validator bug|justification|No remaining failures assigned to impl_validator_remaining_burn_down' validator-report.md`

    ## Verification

    - Run the two check phases above.
    - Confirm `validator-report.md` has no unclassified failures.
    - Confirm any skip is generated under `safe/out/validator/` and justified in the report.

    ## Success Criteria

    - No unclassified or open failures remain after Phase 5 all-completed checking.
    - Every `fixed` row has a non-empty `regression_test` and `fix_commit`.
    - Every `skipped_validator_bug` row has non-empty notes naming generated skip artifacts and a clear justification.
    - Any skip root and threshold reduction are generated only under `safe/out/validator/`, minimal, validated, and recorded with `VALIDATOR_SKIPPED_SOURCE_CASES`, `VALIDATOR_SKIPPED_USAGE_CASES`, and `VALIDATOR_SKIPPED_TOTAL_CASES`.
    - `validator/` remains unmodified except normal clone/fetch/switch/fast-forward operations.

    ## Git Commit Requirement

    The implementer must commit all Phase 5 work to git before yielding to the verifier phases.
    {% endraw %}

    Verdict Instruction
    End with exactly one verdict line:
    VERDICT: PASS
    VERDICT: FAIL: <reason>
- id: check_validator_remaining_senior_tester
  type: check
  bounce_target: impl_validator_remaining_burn_down
  prompt: |
    Reviewer Role: Senior tester reviewer for Phase 5 validator-bug claims, skip minimality, and generated-only skip mechanics.

    Workflow goal: {{GOAL}}

    Work from `/home/yans/safelibs/pipeline/ports/port-libzstd`.
    This prompt is self-contained; the agent sees only this prompt.
    Use `.plan/workflow-structure.yaml` as the authoritative source for phase order, phase IDs, phase types, and this phase's fixed `bounce_target` value. Do not invent, rename, or reorder phases.
    Review the committed work from the implement phase `impl_validator_remaining_burn_down`. Do not edit implementation files unless the workflow explicitly bounces and you become the implementer for the bounced phase.

    Topology Contract
    - Phase order: `15`
    - Phase ID: `check_validator_remaining_senior_tester`
    - Phase type: `check`
    - Fixed top-level `bounce_target`: `impl_validator_remaining_burn_down`

    Preexisting Inputs
    - `validator/`
    - `validator-report.md`
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/check-validator-phase-results.py`
    - `safe/out/validator/`

    Specific Things To Verify
    - The Phase 5 diff is whitespace-clean from the recorded base commit.
    - validator/ remains unmodified except normal clone/fetch/switch/fast-forward state.
    - Any skip.env and filtered test root are generated under safe/out/validator, minimal, threshold-adjusted, and validated against validator/tools/testcases.py.
    - validator-report.md justifies every validator-bug skip and records the generated artifacts.

    Commands To Run
    Run these commands exactly, unless an earlier command failure makes a later command impossible. Record any failure details in the verdict reason.

    {% raw %}
    ```bash
    bash -lc 'base=$(awk "/Phase 5 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'
    test -z "$(git -C validator status --porcelain --untracked-files=no)"
    bash -lc 'if [ -f safe/out/validator/skip.env ]; then py=python3; if [ -x safe/out/validator/venv/bin/python ]; then py="$PWD/safe/out/validator/venv/bin/python"; else python3 -c "import yaml"; fi; set -a; . safe/out/validator/skip.env; set +a; test -n "${VALIDATOR_TESTS_ROOT:-}"; test -d "$VALIDATOR_TESTS_ROOT/libzstd"; test -d "$VALIDATOR_TESTS_ROOT/tests/libzstd"; "$py" validator/tools/testcases.py --config validator/repositories.yml --tests-root "$VALIDATOR_TESTS_ROOT" --library libzstd --check --min-source-cases "$VALIDATOR_MIN_SOURCE_CASES" --min-usage-cases "$VALIDATOR_MIN_USAGE_CASES" --min-cases "$VALIDATOR_MIN_CASES"; fi'
    find safe/out/validator -maxdepth 3 -type d | sort | sed -n '1,120p'
    rg -n 'skip|skipped|validator bug|justification|No remaining failures assigned to impl_validator_remaining_burn_down' validator-report.md
    ```
    {% endraw %}

    Phase Source Content For The Implement Block Being Verified
    {% raw %}
    # Phase 5: Remaining Failures, Validator-Bug Triage, and Report Consolidation

    ## Phase Name

    Remaining Validator Burn Down

    ## Implement Phase ID

    `impl_validator_remaining_burn_down`

    ## Preexisting Inputs

    - Outputs from Phases 1-4
    - Full validator results under `safe/out/validator/artifacts/`
    - `validator-report.md`
    - All local regression tests added in earlier phases

    ## New Outputs

    - Fixes for any residual validator failures not covered by Phases 2-4
    - Generated filtered validator test root under `safe/out/validator/tests-filtered/` only if a validator bug is clearly proven
    - Generated skip metadata under `safe/out/validator/skip.env` only if a validator bug is clearly proven
    - Finalized failure classification in `validator-report.md`
    - A pre-validator code/test/package commit when safe code, packaging, or regression tests changed, plus a final Phase 5 report commit

    ## File Changes

    - Update only the safe-side files needed for true residual compatibility failures.
    - If a validator bug is proven:
      - leave `validator/` unchanged
      - document the bug in `validator-report.md`
      - generate any filtered test root under `safe/out/validator/tests-filtered/`
      - generate `safe/out/validator/skip.env` with adjusted proof thresholds and skipped testcase IDs
      - ensure only the invalid check is omitted from the filtered run
    - Update `safe/scripts/run-validator-libzstd.sh` only if needed to support generated skip roots or better report generation.

    ## Implementation Details

    - Capture `Phase 5 Base Commit: $(git rev-parse HEAD)` before making tracked changes and record that value in the Phase 5 report section.
    - If no Phase 1 failure row is assigned to `impl_validator_remaining_burn_down`, run the strict full validator helper, then update `validator-report.md` with the exact note `No remaining failures assigned to impl_validator_remaining_burn_down`, the phase base commit, commands run, and result paths before making one report-only commit.
    - If residual rows are assigned to this phase, add regressions and fixes first for true libzstd-safe failures, commit code/test/package changes before running `safe/scripts/run-validator-libzstd.sh`, then update the failure table rows from `open` to `fixed` with `regression_test`, `fix_commit`, and notes before making the final report commit.
    - Re-parse all result JSON files after the Phase 4 run. The burn-down phase owns:
      - validator infrastructure problems
      - package override install problems
      - proof generation problems
      - residual mixed failures that involve more than one prior category
      - suspected validator-bug rows reassigned from Phases 2-4
      - validator bug documentation
    - A validator-bug finding must include:
      - exact testcase ID
      - validator commit
      - log path
      - why Ubuntu original-package behavior or validator script logic is inconsistent with the stated test
      - why modifying libzstd-safe would be incorrect
      - exact generated skip mechanics, if used
    - For each suspected validator-bug row handed off from an earlier phase, either prove it as a validator bug and apply the generated skip path below, or reclassify it as a true libzstd-safe residual failure, add a checked-in regression, fix it in safe code or package metadata, and mark it `fixed` with the Phase 5 fix commit.
    - When generating a skip, remove any stale `safe/out/validator/tests-filtered/`, then create this generated dual layout:
      - copy `validator/tests/libzstd/` to `safe/out/validator/tests-filtered/libzstd/`
      - create `safe/out/validator/tests-filtered/tests/`
      - copy `validator/tests/libzstd/` to `safe/out/validator/tests-filtered/tests/libzstd/`
      - remove only the invalid testcase entries from both generated `testcases.yml` files
      - preserve the copied Dockerfile, entrypoint, fixtures, and case scripts in both generated copies
      - leave `validator/tests/_shared/` consumed from the unmodified validator checkout; do not copy or edit `_shared`
    - Resolve `PYTHON` exactly as in `safe/scripts/run-validator-libzstd.sh`, preferring `safe/out/validator/venv/bin/python` when it exists and otherwise requiring `python3 -c 'import yaml'` to pass. Validate the generated skip root before the filtered run with `"$PYTHON" validator/tools/testcases.py --config validator/repositories.yml --tests-root "$PWD/safe/out/validator/tests-filtered" --library libzstd --check --min-source-cases "$VALIDATOR_MIN_SOURCE_CASES" --min-usage-cases "$VALIDATOR_MIN_USAGE_CASES" --min-cases "$VALIDATOR_MIN_CASES"`. Run the filtered matrix with the unmodified `--config "$PWD/validator/repositories.yml"`, `--tests-root "$PWD/safe/out/validator/tests-filtered"`, `--mode port-04-test`, `--override-deb-root "$PWD/safe/out/validator/override-debs"`, `--port-deb-lock "$PWD/safe/out/validator/artifacts/proof/port-04-test-debs-lock.json"`, and `--library libzstd`.
    - `safe/out/validator/skip.env` must define:
      - `VALIDATOR_TESTS_ROOT` as the absolute path to `safe/out/validator/tests-filtered`
      - `VALIDATOR_SKIPPED_CASES` as a space-separated list of skipped testcase IDs
      - `VALIDATOR_CANONICAL_SOURCE_CASES=5`, `VALIDATOR_CANONICAL_USAGE_CASES=80`, and `VALIDATOR_CANONICAL_CASES=85`
      - `VALIDATOR_SKIPPED_SOURCE_CASES`, `VALIDATOR_SKIPPED_USAGE_CASES`, and `VALIDATOR_SKIPPED_TOTAL_CASES` as the documented skipped counts
      - `VALIDATOR_MIN_SOURCE_CASES`, `VALIDATOR_MIN_USAGE_CASES`, and `VALIDATOR_MIN_CASES` as 5, 80, and 85 minus the documented skipped source, usage, and total counts
    - A true libzstd-safe residual failure must still get a checked-in regression before the fix.

    ## Verification Phases

    - `check_validator_remaining_software_tester` - type: `check`; fixed `bounce_target: impl_validator_remaining_burn_down`; purpose: verify no unclassified failures remain and all fixed failures have regressions. Commands to run:
    - `bash safe/scripts/run-validator-regressions.sh`
      - `bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --completed-phase impl_validator_remaining_burn_down; test "$status" -eq 0'`
      - `python3 -m json.tool safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json >/dev/null`
      - `rg -n 'Unclassified|Remaining|Validator Bug|Skip|No remaining failures assigned to impl_validator_remaining_burn_down' validator-report.md`
      - `cargo test --manifest-path safe/Cargo.toml --release --all-targets`
      - `bash safe/scripts/verify-export-parity.sh`
    - `check_validator_remaining_senior_tester` - type: `check`; fixed `bounce_target: impl_validator_remaining_burn_down`; purpose: review any validator-bug claims and ensure skips are minimal and generated-only. Commands to run:
      - `bash -lc 'base=$(awk "/Phase 5 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'`
      - `test -z "$(git -C validator status --porcelain --untracked-files=no)"`
      - `bash -lc 'if [ -f safe/out/validator/skip.env ]; then py=python3; if [ -x safe/out/validator/venv/bin/python ]; then py="$PWD/safe/out/validator/venv/bin/python"; else python3 -c "import yaml"; fi; set -a; . safe/out/validator/skip.env; set +a; test -n "${VALIDATOR_TESTS_ROOT:-}"; test -d "$VALIDATOR_TESTS_ROOT/libzstd"; test -d "$VALIDATOR_TESTS_ROOT/tests/libzstd"; "$py" validator/tools/testcases.py --config validator/repositories.yml --tests-root "$VALIDATOR_TESTS_ROOT" --library libzstd --check --min-source-cases "$VALIDATOR_MIN_SOURCE_CASES" --min-usage-cases "$VALIDATOR_MIN_USAGE_CASES" --min-cases "$VALIDATOR_MIN_CASES"; fi'`
      - `find safe/out/validator -maxdepth 3 -type d | sort | sed -n '1,120p'`
      - `rg -n 'skip|skipped|validator bug|justification|No remaining failures assigned to impl_validator_remaining_burn_down' validator-report.md`

    ## Verification

    - Run the two check phases above.
    - Confirm `validator-report.md` has no unclassified failures.
    - Confirm any skip is generated under `safe/out/validator/` and justified in the report.

    ## Success Criteria

    - No unclassified or open failures remain after Phase 5 all-completed checking.
    - Every `fixed` row has a non-empty `regression_test` and `fix_commit`.
    - Every `skipped_validator_bug` row has non-empty notes naming generated skip artifacts and a clear justification.
    - Any skip root and threshold reduction are generated only under `safe/out/validator/`, minimal, validated, and recorded with `VALIDATOR_SKIPPED_SOURCE_CASES`, `VALIDATOR_SKIPPED_USAGE_CASES`, and `VALIDATOR_SKIPPED_TOTAL_CASES`.
    - `validator/` remains unmodified except normal clone/fetch/switch/fast-forward operations.

    ## Git Commit Requirement

    The implementer must commit all Phase 5 work to git before yielding to the verifier phases.
    {% endraw %}

    Verdict Instruction
    End with exactly one verdict line:
    VERDICT: PASS
    VERDICT: FAIL: <reason>
- id: impl_validator_final_clean_run
  type: implement
  bounce_target: null
  prompt: |
    Role: Software engineer implementing phase `impl_validator_final_clean_run` (Final Validator Evidence And Review).

    Workflow goal: {{GOAL}}

    Work from `/home/yans/safelibs/pipeline/ports/port-libzstd`.
    This prompt is self-contained; the agent sees only this prompt.
    Use `.plan/workflow-structure.yaml` as the authoritative source for phase order, phase IDs, phase types, and this phase's fixed `bounce_target` value. Do not invent, rename, or reorder phases.
    Consume or update existing artifacts listed under `Preexisting Inputs` in place. Do not refetch, recollect, rediscover, or regenerate checked-in or already prepared artifacts from scratch when they already satisfy the required contract.
    Preserve unrelated user work.
    Commit all scoped work to git before yielding.

    Topology Contract
    - Phase order: `16`
    - Phase ID: `impl_validator_final_clean_run`
    - Phase type: `implement`
    - Fixed top-level `bounce_target`: `null`

    Preexisting Inputs
    - `validator/`
    - `validator-report.md`
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/check-validator-phase-results.py`
    - `safe/scripts/run-validator-regressions.sh`
    - `safe/scripts/run-full-suite.sh`
    - `safe/scripts/build-dependent-image.sh`
    - `safe/abi/original.exports.txt`
    - `safe/abi/original.soname.txt`
    - `safe/abi/export_map.toml`
    - `safe/tests/upstream_test_matrix.toml`
    - `safe/tests/dependents/dependent_matrix.toml`
    - `safe/tests/fixtures/versions/`
    - `safe/tests/fixtures/regression/`
    - `safe/tests/fixtures/fuzz-corpora/`
    - `dependents.json`
    - `all_cves.json`
    - `relevant_cves.json`
    - `test-original.sh`
    - `safe/docker/dependents/`
    - `safe/out/validator/artifacts/`

    New Outputs
    - Final clean validator artifacts under `safe/out/validator/artifacts/`
    - Final proof JSON under `safe/out/validator/artifacts/proof/port-04-test-validation-proof.json`
    - Final `validator-report.md` with complete run summary
    - A final git commit for Phase 6

    Phase Source Content
    {% raw %}
    # Phase 6: Final Clean Validator Run and Release Gate

    ## Phase Name

    Final Validator Evidence And Review

    ## Implement Phase ID

    `impl_validator_final_clean_run`

    ## Preexisting Inputs

    - Outputs from Phases 1-5
    - All checked-in regressions and fixes
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/run-full-suite.sh`
    - `safe/scripts/build-dependent-image.sh`
    - `validator-report.md`

    ## New Outputs

    - Final clean validator artifacts under `safe/out/validator/artifacts/`
    - Final proof JSON under `safe/out/validator/artifacts/proof/port-04-test-validation-proof.json`
    - Final `validator-report.md` with complete run summary
    - A final git commit for Phase 6

    ## File Changes

    - Update `validator-report.md` final sections:
    - validator commit
      - safe commits included
      - commands executed
      - package filenames and hashes
      - canonical 85-case summary, plus adjusted executed-case summary if a generated validator-bug skip exists
      - all failures found across the workflow
      - fixes applied
      - regressions added
      - skips, if any, with justification
      - final validator proof path
      - final `safe/scripts/run-full-suite.sh` result
    - Do not make new code changes in this phase unless a final verifier finds a blocking issue. Any final verifier bounce must go only to this phase's fixed `bounce_target`, `impl_validator_final_clean_run`; the final implementor must document the failure, make the smallest necessary correction or report-only update in this phase, commit it, and rerun the final evidence commands.

    ## Implementation Details

    - Capture `Phase 6 Base Commit: $(git rev-parse HEAD)` before the final evidence run and record that value in the final report section.
    - Rerun the full validator matrix from a clean artifact root to avoid stale pass/fail data:

    ```bash
    rm -rf safe/out/validator/artifacts
    set +e
    bash safe/scripts/run-validator-libzstd.sh
    validator_status=$?
    set -e
    VALIDATOR_RUNNER_STATUS=$validator_status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --completed-phase impl_validator_remaining_burn_down
    test "$validator_status" -eq 0
    ```

    If `safe/out/validator/skip.env` exists, `safe/scripts/run-validator-libzstd.sh` must print the skipped testcase IDs, use the filtered tests root recorded in that file, and use the adjusted proof thresholds recorded there. Without `skip.env`, it must use the unmodified `validator/tests` root and the canonical 5/80/85 thresholds.

    - Then run local full release verification:

    ```bash
    bash safe/scripts/run-validator-regressions.sh
    bash safe/scripts/build-dependent-image.sh
    bash safe/scripts/run-full-suite.sh
    ```

    - Without a validator-bug skip, the final validator summary must show 85 executed cases, 5 source cases, 80 usage cases, and 0 failures. With a documented validator-bug skip, the final summary must show the adjusted executed counts, 0 failures, and `validator-report.md` must state the canonical 85-case inventory and the omitted testcase IDs.
    - The final report must be enough for a checker to reproduce the exact run from clone/pull through proof generation.

    ## Verification Phases

    - `check_validator_final_software_tester` - type: `check`; fixed `bounce_target: impl_validator_final_clean_run`; purpose: independently rerun the complete validator command and local release gates. Commands to run:
    - `bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --completed-phase impl_validator_remaining_burn_down; test "$status" -eq 0'`
      - `bash safe/scripts/run-validator-regressions.sh`
      - `bash safe/scripts/build-dependent-image.sh`
      - `bash safe/scripts/run-full-suite.sh`
      - `python3 -m json.tool safe/out/validator/artifacts/proof/port-04-test-debs-lock.json >/dev/null`
      - `python3 -m json.tool safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json >/dev/null`
      - `python3 -m json.tool safe/out/validator/artifacts/proof/port-04-test-validation-proof.json >/dev/null`
    - `check_validator_final_senior_tester` - type: `check`; fixed `bounce_target: impl_validator_final_clean_run`; purpose: final architectural review of evidence, commits, validator isolation, and report completeness. Commands to run:
      - `bash -lc 'base=$(awk "/Phase 6 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'`
      - `git status --short`
      - `git log --oneline --decorate -8`
      - `git -C validator rev-parse HEAD`
      - `git -C validator status --short --branch`
      - `VALIDATOR_RUNNER_STATUS=0 python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --completed-phase impl_validator_remaining_burn_down`
      - `bash -lc 'if [ -f safe/out/validator/skip.env ]; then py=python3; if [ -x safe/out/validator/venv/bin/python ]; then py="$PWD/safe/out/validator/venv/bin/python"; else python3 -c "import yaml"; fi; set -a; . safe/out/validator/skip.env; set +a; test -d "$VALIDATOR_TESTS_ROOT/libzstd"; test -d "$VALIDATOR_TESTS_ROOT/tests/libzstd"; "$py" validator/tools/testcases.py --config validator/repositories.yml --tests-root "$VALIDATOR_TESTS_ROOT" --library libzstd --check --min-source-cases "$VALIDATOR_MIN_SOURCE_CASES" --min-usage-cases "$VALIDATOR_MIN_USAGE_CASES" --min-cases "$VALIDATOR_MIN_CASES"; fi'`
      - `rg -n 'Final Run|Validator Commit|Checks Executed|Failures Found|Fixes Applied|Skips|Proof' validator-report.md`
      - `test ! -e .gitmodules || ! rg -n 'validator' .gitmodules`

    ## Verification

    - Run the two check phases above.
    - Confirm all work is committed and the only untracked files are generated/ignored artifacts or unrelated preexisting bytecode.

    ## Success Criteria

    - The final validator run exits zero and produces valid port lock, summary, and proof JSON artifacts.
    - Without skips, the final summary records 85 total cases, 5 source cases, 80 usage cases, and 0 failures; with documented skips, adjusted counts and skipped IDs are recorded while the canonical 85-case inventory remains in the report.
    - `validator-report.md` summarizes validator commit, safe commits, commands, package hashes, failures, fixes, regressions, skips, proof path, and release-gate result.
    - `safe/scripts/run-validator-regressions.sh`, `safe/scripts/build-dependent-image.sh`, and `safe/scripts/run-full-suite.sh` pass after the final validator run.
    - All work is committed; any remaining untracked files are generated/ignored artifacts or unrelated preexisting bytecode.

    ## Git Commit Requirement

    The implementer must commit all Phase 6 work to git before yielding to the verifier phases.
    {% endraw %}
- id: check_validator_final_software_tester
  type: check
  bounce_target: impl_validator_final_clean_run
  prompt: |
    Reviewer Role: Software tester reviewer for Phase 6 final clean validator run and local release gates.

    Workflow goal: {{GOAL}}

    Work from `/home/yans/safelibs/pipeline/ports/port-libzstd`.
    This prompt is self-contained; the agent sees only this prompt.
    Use `.plan/workflow-structure.yaml` as the authoritative source for phase order, phase IDs, phase types, and this phase's fixed `bounce_target` value. Do not invent, rename, or reorder phases.
    Review the committed work from the implement phase `impl_validator_final_clean_run`. Do not edit implementation files unless the workflow explicitly bounces and you become the implementer for the bounced phase.

    Topology Contract
    - Phase order: `17`
    - Phase ID: `check_validator_final_software_tester`
    - Phase type: `check`
    - Fixed top-level `bounce_target`: `impl_validator_final_clean_run`

    Preexisting Inputs
    - `validator/`
    - `validator-report.md`
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/check-validator-phase-results.py`
    - `safe/scripts/run-validator-regressions.sh`
    - `safe/scripts/run-full-suite.sh`
    - `safe/scripts/build-dependent-image.sh`
    - `safe/out/validator/artifacts/proof/port-04-test-debs-lock.json`
    - `safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json`
    - `safe/out/validator/artifacts/proof/port-04-test-validation-proof.json`

    Specific Things To Verify
    - The full validator command exits zero with the strict phase-result checker in all-completed mode.
    - Local validator regressions, dependent-image build, and full release suite pass after the final validator run.
    - The port lock, final summary, and validation proof JSON artifacts exist and parse as JSON.
    - validator-report.md contains the final clean-run evidence required by the phase source content.

    Commands To Run
    Run these commands exactly, unless an earlier command failure makes a later command impossible. Record any failure details in the verdict reason.

    {% raw %}
    ```bash
    bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --completed-phase impl_validator_remaining_burn_down; test "$status" -eq 0'
    bash safe/scripts/run-validator-regressions.sh
    bash safe/scripts/build-dependent-image.sh
    bash safe/scripts/run-full-suite.sh
    python3 -m json.tool safe/out/validator/artifacts/proof/port-04-test-debs-lock.json >/dev/null
    python3 -m json.tool safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json >/dev/null
    python3 -m json.tool safe/out/validator/artifacts/proof/port-04-test-validation-proof.json >/dev/null
    ```
    {% endraw %}

    Phase Source Content For The Implement Block Being Verified
    {% raw %}
    # Phase 6: Final Clean Validator Run and Release Gate

    ## Phase Name

    Final Validator Evidence And Review

    ## Implement Phase ID

    `impl_validator_final_clean_run`

    ## Preexisting Inputs

    - Outputs from Phases 1-5
    - All checked-in regressions and fixes
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/run-full-suite.sh`
    - `safe/scripts/build-dependent-image.sh`
    - `validator-report.md`

    ## New Outputs

    - Final clean validator artifacts under `safe/out/validator/artifacts/`
    - Final proof JSON under `safe/out/validator/artifacts/proof/port-04-test-validation-proof.json`
    - Final `validator-report.md` with complete run summary
    - A final git commit for Phase 6

    ## File Changes

    - Update `validator-report.md` final sections:
    - validator commit
      - safe commits included
      - commands executed
      - package filenames and hashes
      - canonical 85-case summary, plus adjusted executed-case summary if a generated validator-bug skip exists
      - all failures found across the workflow
      - fixes applied
      - regressions added
      - skips, if any, with justification
      - final validator proof path
      - final `safe/scripts/run-full-suite.sh` result
    - Do not make new code changes in this phase unless a final verifier finds a blocking issue. Any final verifier bounce must go only to this phase's fixed `bounce_target`, `impl_validator_final_clean_run`; the final implementor must document the failure, make the smallest necessary correction or report-only update in this phase, commit it, and rerun the final evidence commands.

    ## Implementation Details

    - Capture `Phase 6 Base Commit: $(git rev-parse HEAD)` before the final evidence run and record that value in the final report section.
    - Rerun the full validator matrix from a clean artifact root to avoid stale pass/fail data:

    ```bash
    rm -rf safe/out/validator/artifacts
    set +e
    bash safe/scripts/run-validator-libzstd.sh
    validator_status=$?
    set -e
    VALIDATOR_RUNNER_STATUS=$validator_status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --completed-phase impl_validator_remaining_burn_down
    test "$validator_status" -eq 0
    ```

    If `safe/out/validator/skip.env` exists, `safe/scripts/run-validator-libzstd.sh` must print the skipped testcase IDs, use the filtered tests root recorded in that file, and use the adjusted proof thresholds recorded there. Without `skip.env`, it must use the unmodified `validator/tests` root and the canonical 5/80/85 thresholds.

    - Then run local full release verification:

    ```bash
    bash safe/scripts/run-validator-regressions.sh
    bash safe/scripts/build-dependent-image.sh
    bash safe/scripts/run-full-suite.sh
    ```

    - Without a validator-bug skip, the final validator summary must show 85 executed cases, 5 source cases, 80 usage cases, and 0 failures. With a documented validator-bug skip, the final summary must show the adjusted executed counts, 0 failures, and `validator-report.md` must state the canonical 85-case inventory and the omitted testcase IDs.
    - The final report must be enough for a checker to reproduce the exact run from clone/pull through proof generation.

    ## Verification Phases

    - `check_validator_final_software_tester` - type: `check`; fixed `bounce_target: impl_validator_final_clean_run`; purpose: independently rerun the complete validator command and local release gates. Commands to run:
    - `bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --completed-phase impl_validator_remaining_burn_down; test "$status" -eq 0'`
      - `bash safe/scripts/run-validator-regressions.sh`
      - `bash safe/scripts/build-dependent-image.sh`
      - `bash safe/scripts/run-full-suite.sh`
      - `python3 -m json.tool safe/out/validator/artifacts/proof/port-04-test-debs-lock.json >/dev/null`
      - `python3 -m json.tool safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json >/dev/null`
      - `python3 -m json.tool safe/out/validator/artifacts/proof/port-04-test-validation-proof.json >/dev/null`
    - `check_validator_final_senior_tester` - type: `check`; fixed `bounce_target: impl_validator_final_clean_run`; purpose: final architectural review of evidence, commits, validator isolation, and report completeness. Commands to run:
      - `bash -lc 'base=$(awk "/Phase 6 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'`
      - `git status --short`
      - `git log --oneline --decorate -8`
      - `git -C validator rev-parse HEAD`
      - `git -C validator status --short --branch`
      - `VALIDATOR_RUNNER_STATUS=0 python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --completed-phase impl_validator_remaining_burn_down`
      - `bash -lc 'if [ -f safe/out/validator/skip.env ]; then py=python3; if [ -x safe/out/validator/venv/bin/python ]; then py="$PWD/safe/out/validator/venv/bin/python"; else python3 -c "import yaml"; fi; set -a; . safe/out/validator/skip.env; set +a; test -d "$VALIDATOR_TESTS_ROOT/libzstd"; test -d "$VALIDATOR_TESTS_ROOT/tests/libzstd"; "$py" validator/tools/testcases.py --config validator/repositories.yml --tests-root "$VALIDATOR_TESTS_ROOT" --library libzstd --check --min-source-cases "$VALIDATOR_MIN_SOURCE_CASES" --min-usage-cases "$VALIDATOR_MIN_USAGE_CASES" --min-cases "$VALIDATOR_MIN_CASES"; fi'`
      - `rg -n 'Final Run|Validator Commit|Checks Executed|Failures Found|Fixes Applied|Skips|Proof' validator-report.md`
      - `test ! -e .gitmodules || ! rg -n 'validator' .gitmodules`

    ## Verification

    - Run the two check phases above.
    - Confirm all work is committed and the only untracked files are generated/ignored artifacts or unrelated preexisting bytecode.

    ## Success Criteria

    - The final validator run exits zero and produces valid port lock, summary, and proof JSON artifacts.
    - Without skips, the final summary records 85 total cases, 5 source cases, 80 usage cases, and 0 failures; with documented skips, adjusted counts and skipped IDs are recorded while the canonical 85-case inventory remains in the report.
    - `validator-report.md` summarizes validator commit, safe commits, commands, package hashes, failures, fixes, regressions, skips, proof path, and release-gate result.
    - `safe/scripts/run-validator-regressions.sh`, `safe/scripts/build-dependent-image.sh`, and `safe/scripts/run-full-suite.sh` pass after the final validator run.
    - All work is committed; any remaining untracked files are generated/ignored artifacts or unrelated preexisting bytecode.

    ## Git Commit Requirement

    The implementer must commit all Phase 6 work to git before yielding to the verifier phases.
    {% endraw %}

    Verdict Instruction
    End with exactly one verdict line:
    VERDICT: PASS
    VERDICT: FAIL: <reason>
- id: check_validator_final_senior_tester
  type: check
  bounce_target: impl_validator_final_clean_run
  prompt: |
    Reviewer Role: Senior tester reviewer for Phase 6 final evidence, commits, validator isolation, and report completeness.

    Workflow goal: {{GOAL}}

    Work from `/home/yans/safelibs/pipeline/ports/port-libzstd`.
    This prompt is self-contained; the agent sees only this prompt.
    Use `.plan/workflow-structure.yaml` as the authoritative source for phase order, phase IDs, phase types, and this phase's fixed `bounce_target` value. Do not invent, rename, or reorder phases.
    Review the committed work from the implement phase `impl_validator_final_clean_run`. Do not edit implementation files unless the workflow explicitly bounces and you become the implementer for the bounced phase.

    Topology Contract
    - Phase order: `18`
    - Phase ID: `check_validator_final_senior_tester`
    - Phase type: `check`
    - Fixed top-level `bounce_target`: `impl_validator_final_clean_run`

    Preexisting Inputs
    - `validator/`
    - `validator-report.md`
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/check-validator-phase-results.py`
    - `safe/out/validator/artifacts/port-04-test/results/libzstd/`
    - `safe/out/validator/artifacts/proof/port-04-test-validation-proof.json`

    Specific Things To Verify
    - The Phase 6 diff is whitespace-clean from the recorded base commit and all work is committed or generated/ignored.
    - The recent commit history shows the phase commits needed for verifier reasoning.
    - validator/ is at a recorded commit and not modified by the parent workflow.
    - The all-completed phase-result checker passes, optional skip mechanics validate, and validator-report.md contains final run, commit, failure, fix, skip, and proof evidence.

    Commands To Run
    Run these commands exactly, unless an earlier command failure makes a later command impossible. Record any failure details in the verdict reason.

    {% raw %}
    ```bash
    bash -lc 'base=$(awk "/Phase 6 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'
    git status --short
    git log --oneline --decorate -8
    git -C validator rev-parse HEAD
    git -C validator status --short --branch
    VALIDATOR_RUNNER_STATUS=0 python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --completed-phase impl_validator_remaining_burn_down
    bash -lc 'if [ -f safe/out/validator/skip.env ]; then py=python3; if [ -x safe/out/validator/venv/bin/python ]; then py="$PWD/safe/out/validator/venv/bin/python"; else python3 -c "import yaml"; fi; set -a; . safe/out/validator/skip.env; set +a; test -d "$VALIDATOR_TESTS_ROOT/libzstd"; test -d "$VALIDATOR_TESTS_ROOT/tests/libzstd"; "$py" validator/tools/testcases.py --config validator/repositories.yml --tests-root "$VALIDATOR_TESTS_ROOT" --library libzstd --check --min-source-cases "$VALIDATOR_MIN_SOURCE_CASES" --min-usage-cases "$VALIDATOR_MIN_USAGE_CASES" --min-cases "$VALIDATOR_MIN_CASES"; fi'
    rg -n 'Final Run|Validator Commit|Checks Executed|Failures Found|Fixes Applied|Skips|Proof' validator-report.md
    test ! -e .gitmodules || ! rg -n 'validator' .gitmodules
    ```
    {% endraw %}

    Phase Source Content For The Implement Block Being Verified
    {% raw %}
    # Phase 6: Final Clean Validator Run and Release Gate

    ## Phase Name

    Final Validator Evidence And Review

    ## Implement Phase ID

    `impl_validator_final_clean_run`

    ## Preexisting Inputs

    - Outputs from Phases 1-5
    - All checked-in regressions and fixes
    - `safe/scripts/run-validator-libzstd.sh`
    - `safe/scripts/run-full-suite.sh`
    - `safe/scripts/build-dependent-image.sh`
    - `validator-report.md`

    ## New Outputs

    - Final clean validator artifacts under `safe/out/validator/artifacts/`
    - Final proof JSON under `safe/out/validator/artifacts/proof/port-04-test-validation-proof.json`
    - Final `validator-report.md` with complete run summary
    - A final git commit for Phase 6

    ## File Changes

    - Update `validator-report.md` final sections:
    - validator commit
      - safe commits included
      - commands executed
      - package filenames and hashes
      - canonical 85-case summary, plus adjusted executed-case summary if a generated validator-bug skip exists
      - all failures found across the workflow
      - fixes applied
      - regressions added
      - skips, if any, with justification
      - final validator proof path
      - final `safe/scripts/run-full-suite.sh` result
    - Do not make new code changes in this phase unless a final verifier finds a blocking issue. Any final verifier bounce must go only to this phase's fixed `bounce_target`, `impl_validator_final_clean_run`; the final implementor must document the failure, make the smallest necessary correction or report-only update in this phase, commit it, and rerun the final evidence commands.

    ## Implementation Details

    - Capture `Phase 6 Base Commit: $(git rev-parse HEAD)` before the final evidence run and record that value in the final report section.
    - Rerun the full validator matrix from a clean artifact root to avoid stale pass/fail data:

    ```bash
    rm -rf safe/out/validator/artifacts
    set +e
    bash safe/scripts/run-validator-libzstd.sh
    validator_status=$?
    set -e
    VALIDATOR_RUNNER_STATUS=$validator_status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --completed-phase impl_validator_remaining_burn_down
    test "$validator_status" -eq 0
    ```

    If `safe/out/validator/skip.env` exists, `safe/scripts/run-validator-libzstd.sh` must print the skipped testcase IDs, use the filtered tests root recorded in that file, and use the adjusted proof thresholds recorded there. Without `skip.env`, it must use the unmodified `validator/tests` root and the canonical 5/80/85 thresholds.

    - Then run local full release verification:

    ```bash
    bash safe/scripts/run-validator-regressions.sh
    bash safe/scripts/build-dependent-image.sh
    bash safe/scripts/run-full-suite.sh
    ```

    - Without a validator-bug skip, the final validator summary must show 85 executed cases, 5 source cases, 80 usage cases, and 0 failures. With a documented validator-bug skip, the final summary must show the adjusted executed counts, 0 failures, and `validator-report.md` must state the canonical 85-case inventory and the omitted testcase IDs.
    - The final report must be enough for a checker to reproduce the exact run from clone/pull through proof generation.

    ## Verification Phases

    - `check_validator_final_software_tester` - type: `check`; fixed `bounce_target: impl_validator_final_clean_run`; purpose: independently rerun the complete validator command and local release gates. Commands to run:
    - `bash -lc 'set +e; bash safe/scripts/run-validator-libzstd.sh; status=$?; set -e; VALIDATOR_RUNNER_STATUS=$status python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --completed-phase impl_validator_remaining_burn_down; test "$status" -eq 0'`
      - `bash safe/scripts/run-validator-regressions.sh`
      - `bash safe/scripts/build-dependent-image.sh`
      - `bash safe/scripts/run-full-suite.sh`
      - `python3 -m json.tool safe/out/validator/artifacts/proof/port-04-test-debs-lock.json >/dev/null`
      - `python3 -m json.tool safe/out/validator/artifacts/port-04-test/results/libzstd/summary.json >/dev/null`
      - `python3 -m json.tool safe/out/validator/artifacts/proof/port-04-test-validation-proof.json >/dev/null`
    - `check_validator_final_senior_tester` - type: `check`; fixed `bounce_target: impl_validator_final_clean_run`; purpose: final architectural review of evidence, commits, validator isolation, and report completeness. Commands to run:
      - `bash -lc 'base=$(awk "/Phase 6 Base Commit:/ {print \$5; exit}" validator-report.md); test -n "$base"; git diff --check "$base..HEAD"'`
      - `git status --short`
      - `git log --oneline --decorate -8`
      - `git -C validator rev-parse HEAD`
      - `git -C validator status --short --branch`
      - `VALIDATOR_RUNNER_STATUS=0 python3 safe/scripts/check-validator-phase-results.py --results-root safe/out/validator/artifacts/port-04-test/results/libzstd --report validator-report.md --completed-phase impl_validator_source_cli_regressions --completed-phase impl_validator_streaming_capi_regressions --completed-phase impl_validator_libarchive_usage_regressions --completed-phase impl_validator_remaining_burn_down`
      - `bash -lc 'if [ -f safe/out/validator/skip.env ]; then py=python3; if [ -x safe/out/validator/venv/bin/python ]; then py="$PWD/safe/out/validator/venv/bin/python"; else python3 -c "import yaml"; fi; set -a; . safe/out/validator/skip.env; set +a; test -d "$VALIDATOR_TESTS_ROOT/libzstd"; test -d "$VALIDATOR_TESTS_ROOT/tests/libzstd"; "$py" validator/tools/testcases.py --config validator/repositories.yml --tests-root "$VALIDATOR_TESTS_ROOT" --library libzstd --check --min-source-cases "$VALIDATOR_MIN_SOURCE_CASES" --min-usage-cases "$VALIDATOR_MIN_USAGE_CASES" --min-cases "$VALIDATOR_MIN_CASES"; fi'`
      - `rg -n 'Final Run|Validator Commit|Checks Executed|Failures Found|Fixes Applied|Skips|Proof' validator-report.md`
      - `test ! -e .gitmodules || ! rg -n 'validator' .gitmodules`

    ## Verification

    - Run the two check phases above.
    - Confirm all work is committed and the only untracked files are generated/ignored artifacts or unrelated preexisting bytecode.

    ## Success Criteria

    - The final validator run exits zero and produces valid port lock, summary, and proof JSON artifacts.
    - Without skips, the final summary records 85 total cases, 5 source cases, 80 usage cases, and 0 failures; with documented skips, adjusted counts and skipped IDs are recorded while the canonical 85-case inventory remains in the report.
    - `validator-report.md` summarizes validator commit, safe commits, commands, package hashes, failures, fixes, regressions, skips, proof path, and release-gate result.
    - `safe/scripts/run-validator-regressions.sh`, `safe/scripts/build-dependent-image.sh`, and `safe/scripts/run-full-suite.sh` pass after the final validator run.
    - All work is committed; any remaining untracked files are generated/ignored artifacts or unrelated preexisting bytecode.

    ## Git Commit Requirement

    The implementer must commit all Phase 6 work to git before yielding to the verifier phases.
    {% endraw %}

    Verdict Instruction
    End with exactly one verdict line:
    VERDICT: PASS
    VERDICT: FAIL: <reason>
